📋 Pulsar Plugins 仪表盘重构 - 开发文档
🎯 项目目标
将 Plugins 标签页从"静态配置页"升级为"动态仪表盘"，提供插件使用统计、健康监控、日志查看等增值功能，让用户主动想来查看。
---
📐 架构设计
1. 整体架构
┌─────────────────────────────────────────────────────────────┐
│                    Presentation Layer                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ SettingsPluginsPage.xaml (View)                        │ │
│  │  - 分组列表布局                                          │ │
│  │  - 搜索/筛选控件                                         │ │
│  │  - 插件卡片展示                                          │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ PluginManagerViewModel (ViewModel)                     │ │
│  │  - 插件列表管理                                          │ │
│  │  - 分组逻辑                                              │ │
│  │  - 搜索/筛选逻辑                                         │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ PluginViewModel (Item ViewModel)                       │ │
│  │  - 插件基本信息                                          │ │
│  │  - 使用统计                                              │ │
│  │  - 健康状态                                              │ │
│  │  - 日志摘要                                              │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                     Business Layer                           │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ PluginUsageTracker (Service)                           │ │
│  │  - 记录插件执行次数                                      │ │
│  │  - 记录成功/失败次数                                     │ │
│  │  - 记录最后使用时间                                      │ │
│  │  - 提供统计查询 API                                      │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ PluginHealthMonitor (Service)                          │ │
│  │  - 监听 Circuit Breaker 事件                            │ │
│  │  - 计算健康状态                                          │ │
│  │  - 提供健康报告 API                                      │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ PluginLogService (Service)                             │ │
│  │  - 收集插件日志                                          │ │
│  │  - 按插件分类存储                                        │ │
│  │  - 提供日志查询 API                                      │ │
│  │  - 日志轮转和清理                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ PluginRegistry (Existing, Enhanced)                    │ │
│  │  - 插件生命周期管理                                      │ │
│  │  - 执行插件时触发统计/日志记录                          │ │
│  │  - Circuit Breaker 集成                                 │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      Data Layer                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ PluginUsageStats.json                                  │ │
│  │  {                                                     │ │
│  │    "com.pulsar.winswitcher": {                        │ │
│  │      "totalExecutions": 127,                          │ │
│  │      "successCount": 125,                             │ │
│  │      "failureCount": 2,                               │ │
│  │      "lastUsed": "2026-03-01T10:30:00Z",             │ │
│  │      "dailyStats": { "2026-03-01": 15, ... }         │ │
│  │    }                                                  │ │
│  │  }                                                     │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Logs/Plugins/                                          │ │
│  │  ├── com.pulsar.winswitcher-20260301.log              │ │
│  │  ├── com.pulsar.pki-20260301.log                      │ │
│  │  └── com.pulsar.vbarunner-20260301.log                │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
---
📊 数据模型设计
1. PluginUsageStats (使用统计)
/// <summary>
/// 插件使用统计数据模型
/// 存储位置: %AppData%\Pulsar\PluginUsageStats.json
/// </summary>
public class PluginUsageStats
{
    /// <summary>
    /// 插件 ID
    /// </summary>
    public string PluginId { get; set; }
   
    /// <summary>
    /// 总执行次数（自安装以来）
    /// </summary>
    public int TotalExecutions { get; set; }
   
    /// <summary>
    /// 成功执行次数
    /// </summary>
    public int SuccessCount { get; set; }
   
    /// <summary>
    /// 失败执行次数
    /// </summary>
    public int FailureCount { get; set; }
   
    /// <summary>
    /// 最后使用时间
    /// </summary>
    public DateTime? LastUsed { get; set; }
   
    /// <summary>
    /// 首次使用时间
    /// </summary>
    public DateTime? FirstUsed { get; set; }
   
    /// <summary>
    /// 每日统计（最近 30 天）
    /// Key: "yyyy-MM-dd", Value: 执行次数
    /// </summary>
    public Dictionary<string, int> DailyStats { get; set; } = new();
   
    /// <summary>
    /// 平均执行时长（毫秒）
    /// </summary>
    public double AverageExecutionTimeMs { get; set; }
   
    /// <summary>
    /// 使用该插件的 Profile 列表
    /// </summary>
    public HashSet<string> UsedInProfiles { get; set; } = new();
}
2. PluginHealthStatus (健康状态)
/// <summary>
/// 插件健康状态枚举
/// </summary>
public enum PluginHealthStatus
{
    /// <summary>
    /// 健康：无错误，正常运行
    /// </summary>
    Healthy,
   
    /// <summary>
    /// 警告：有少量错误，但未触发 Circuit Breaker
    /// </summary>
    Warning,
   
    /// <summary>
    /// 严重：Circuit Breaker 已触发，插件被临时禁用
    /// </summary>
    Critical,
   
    /// <summary>
    /// 未使用：30 天内未使用
    /// </summary>
    Unused,
   
    /// <summary>
    /// 已禁用：用户手动禁用
    /// </summary>
    Disabled
}
/// <summary>
/// 插件健康报告
/// </summary>
public class PluginHealthReport
{
    public string PluginId { get; set; }
    public PluginHealthStatus Status { get; set; }
   
    /// <summary>
    /// Circuit Breaker 触发次数（最近 24 小时）
    /// </summary>
    public int CircuitBreakerTrips { get; set; }
   
    /// <summary>
    /// 最后一次错误时间
    /// </summary>
    public DateTime? LastErrorTime { get; set; }
   
    /// <summary>
    /// 最后一次错误消息
    /// </summary>
    public string? LastErrorMessage { get; set; }
   
    /// <summary>
    /// 错误率（最近 100 次执行）
    /// </summary>
    public double ErrorRate { get; set; }
   
    /// <summary>
    /// 健康评分（0-100）
    /// </summary>
    public int HealthScore { get; set; }
}
3. PluginLogEntry (日志条目)
/// <summary>
/// 插件日志条目
/// </summary>
public class PluginLogEntry
{
    public DateTime Timestamp { get; set; }
    public string PluginId { get; set; }
    public LogLevel Level { get; set; }
    public string Message { get; set; }
    public string? Exception { get; set; }
    public string? Action { get; set; } // 执行的动作名
    public Dictionary<string, string>? Args { get; set; } // 执行参数
    public long ExecutionTimeMs { get; set; } // 执行时长
}
/// <summary>
/// 日志级别
/// </summary>
public enum LogLevel
{
    Debug,
    Info,
    Warning,
    Error,
    Critical
}
---
🔧 服务设计
1. PluginUsageTracker Service
职责：记录和查询插件使用统计
接口设计：
public interface IPluginUsageTracker
{
    /// <summary>
    /// 记录插件执行
    /// </summary>
    void RecordExecution(string pluginId, bool success, long executionTimeMs, string? profileName = null);
   
    /// <summary>
    /// 获取插件统计数据
    /// </summary>
    PluginUsageStats GetStats(string pluginId);
   
    /// <summary>
    /// 获取所有插件统计数据
    /// </summary>
    Dictionary<string, PluginUsageStats> GetAllStats();
   
    /// <summary>
    /// 获取最常用的插件（Top N）
    /// </summary>
    List<PluginUsageStats> GetMostUsedPlugins(int count = 5);
   
    /// <summary>
    /// 获取未使用的插件（N 天内未使用）
    /// </summary>
    List<string> GetUnusedPlugins(int days = 30);
   
    /// <summary>
    /// 保存统计数据到磁盘
    /// </summary>
    Task SaveAsync();
   
    /// <summary>
    /// 从磁盘加载统计数据
    /// </summary>
    Task LoadAsync();
}
实现要点：
- 使用内存缓存 + 定期持久化（每 5 分钟或应用退出时）
- DailyStats 只保留最近 30 天数据，自动清理旧数据
- 线程安全（使用 ConcurrentDictionary 或锁）
---
2. PluginHealthMonitor Service
职责：监控插件健康状态
接口设计：
public interface IPluginHealthMonitor
{
    /// <summary>
    /// 记录插件错误
    /// </summary>
    void RecordError(string pluginId, Exception exception, string? action = null);
   
    /// <summary>
    /// 记录 Circuit Breaker 触发
    /// </summary>
    void RecordCircuitBreakerTrip(string pluginId);
   
    /// <summary>
    /// 获取插件健康报告
    /// </summary>
    PluginHealthReport GetHealthReport(string pluginId);
   
    /// <summary>
    /// 获取所有插件健康报告
    /// </summary>
    Dictionary<string, PluginHealthReport> GetAllHealthReports();
   
    /// <summary>
    /// 获取有问题的插件列表
    /// </summary>
    List<string> GetUnhealthyPlugins();
   
    /// <summary>
    /// 计算健康评分（0-100）
    /// </summary>
    int CalculateHealthScore(string pluginId);
}
健康评分算法：
HealthScore = 100 - (ErrorRate * 50) - (CircuitBreakerTrips * 10) - (DaysSinceLastUse * 0.5)
- ErrorRate: 0.0 - 1.0 (最近 100 次执行的错误率)
- CircuitBreakerTrips: 最近 24 小时触发次数
- DaysSinceLastUse: 距离最后使用的天数
状态判定逻辑：
- Disabled: 用户手动禁用
- Critical: Circuit Breaker 当前处于 Open 状态
- Unused: 30 天内未使用
- Warning: ErrorRate > 0.1 或 CircuitBreakerTrips > 0
- Healthy: 其他情况
---
3. PluginLogService Service
职责：收集、存储、查询插件日志
接口设计：
public interface IPluginLogService
{
    /// <summary>
    /// 记录插件日志
    /// </summary>
    void Log(string pluginId, LogLevel level, string message, Exception? exception = null, 
             string? action = null, Dictionary<string, string>? args = null, long executionTimeMs = 0);
   
    /// <summary>
    /// 获取插件日志（分页）
    /// </summary>
    List<PluginLogEntry> GetLogs(string pluginId, int skip = 0, int take = 100, LogLevel? minLevel = null);
   
    /// <summary>
    /// 获取插件错误日志摘要（最近 N 条）
    /// </summary>
    List<PluginLogEntry> GetRecentErrors(string pluginId, int count = 10);
   
    /// <summary>
    /// 获取所有插件的错误计数
    /// </summary>
    Dictionary<string, int> GetErrorCounts();
   
    /// <summary>
    /// 清理旧日志（保留最近 N 天）
    /// </summary>
    Task CleanupOldLogsAsync(int retentionDays = 30);
   
    /// <summary>
    /// 导出日志到文件
    /// </summary>
    Task ExportLogsAsync(string pluginId, string filePath);
}
日志收集策略：
| 日志类型                                                     | 收集内容                                         | 存储位置                                    |
| ------------------------------------------------------------ | ------------------------------------------------ | ------------------------------------------- |
| 执行日志                                                     | 每次插件执行的基本信息（时间、动作、参数、耗时） | 内存缓冲区 → 每日日志文件                   |
| 错误日志                                                     | 异常堆栈、错误消息、上下文信息                   | 立即写入日志文件 + 内存缓存（用于快速查询） |
| 性能日志                                                     | 执行时长超过阈值（如 1 秒）的记录                | 每日日志文件                                |
| Circuit Breaker 事件                                         | 触发时间、原因、恢复时间                         | 每日日志文件 + 内存缓存                     |
| 日志文件结构：                                               |                                                  |                                             |
| %AppData%\Pulsar\Logs\Plugins\                               |                                                  |                                             |
| ├── com.pulsar.winswitcher-20260301.log                      |                                                  |                                             |
| ├── com.pulsar.winswitcher-20260302.log                      |                                                  |                                             |
| ├── com.pulsar.pki-20260301.log                              |                                                  |                                             |
| └── ...                                                      |                                                  |                                             |
| 日志格式（JSON Lines）：                                     |                                                  |                                             |
| {timestamp:2026-03-01T10:30:15.123Z,pluginId:com.pulsar.winswitcher,level:Info,message:Activated window,action:activate,args:{app:CHROME},executionTimeMs:45} |                                                  |                                             |
| {timestamp:2026-03-01T10:31:20.456Z,pluginId:com.pulsar.winswitcher,level:Error,message:Failed to activate window,exception:System.InvalidOperationException: Window not found...,action:activate,args:{app:NOTEPAD},executionTimeMs:120} |                                                  |                                             |
---
🎨 UI 设计规范
1. 分组列表布局
XAML 结构：
<Page>
    <!-- 顶部工具栏 -->
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/> <!-- 搜索 + 筛选 -->
        <RowDefinition Height="*"/>    <!-- 分组列表 -->
    </Grid.RowDefinitions>
   
    <!-- 搜索和筛选栏 -->
    <Grid Grid.Row="0">
        <ui:TextBox PlaceholderText="Search plugins..." Icon="Search24"/>
        <ComboBox SelectedItem="{Binding GroupByMode}">
            <ComboBoxItem>Group by Status</ComboBoxItem>
            <ComboBoxItem>Group by Usage</ComboBoxItem>
            <ComboBoxItem>Group by Health</ComboBoxItem>
        </ComboBox>
    </Grid>
   
    <!-- 分组列表 -->
    <ScrollViewer Grid.Row="1">
        <ItemsControl ItemsSource="{Binding GroupedPlugins}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <!-- 分组头 -->
                    <Expander Header="{Binding GroupName}" IsExpanded="True">
                        <!-- 插件列表 -->
                        <ItemsControl ItemsSource="{Binding Plugins}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <!-- 插件卡片 -->
                                    <ui:CardControl>
                                        <!-- 插件信息 + 操作按钮 -->
                                    </ui:CardControl>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Expander>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Page>
2. 插件卡片设计
布局：
┌─────────────────────────────────────────────────────────────┐
│ 🚀 WinSwitcher                                    ✅ Enabled │
│ Switch to or launch applications                             │
│ ─────────────────────────────────────────────────────────── │
│ 📊 127 uses  •  2 profiles  •  Last used: 2 hours ago       │
│ ⚡ Avg: 45ms  •  Success: 98.4%  •  Health: 95/100          │
│ ─────────────────────────────────────────────────────────── │
│ [⚙️ Configure]  [📊 View Stats]  [📝 View Logs (2 errors)]  │
└─────────────────────────────────────────────────────────────┘
视觉元素：
- 状态徽章：
  - ✅ Enabled (绿色)
  - ⚠️ Warning (黄色)
  - 🔴 Critical (红色)
  - 💤 Unused (灰色)
  - 🔒 Core (橙色)
- 健康评分颜色：
  - 90-100: 绿色
  - 70-89: 黄色
  - 0-69: 红色
3. 日志查看器设计
弹出窗口布局：
┌─────────────────────────────────────────────────────────────┐
│ 📝 Plugin Logs: WinSwitcher                          [✕]    │
├─────────────────────────────────────────────────────────────┤
│ [All ▼] [Last 24h ▼] [🔍 Search]              [Export Logs] │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🔴 2026-03-01 10:31:20  ERROR                          │ │
│ │ Failed to activate window: NOTEPAD                     │ │
│ │ Exception: System.InvalidOperationException...         │ │
│ │ [View Details]                                         │ │
│ ├─────────────────────────────────────────────────────────┤ │
│ │ ℹ️ 2026-03-01 10:30:15  INFO                           │ │
│ │ Activated window: CHROME (45ms)                        │ │
│ ├─────────────────────────────────────────────────────────┤ │
│ │ ℹ️ 2026-03-01 10:25:10  INFO                           │ │
│ │ Activated window: EXCEL (38ms)                         │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ Showing 3 of 127 entries                    [Load More...]  │
└─────────────────────────────────────────────────────────────┘
---
🔄 集成点设计
1. PluginRegistry 集成
在 ExecuteAsync 方法中添加钩子：
public async Task<PluginResult> ExecuteAsync(string pluginId, string action, 
    IReadOnlyDictionary<string, string> args, PulsarContext context)
{
    var stopwatch = Stopwatch.StartNew();
    bool success = false;
    Exception? exception = null;
   
    try
    {
        // 执行插件...
        var result = await plugin.ExecuteAsync(action, args, context);
        success = result.Success;
        return result;
    }
    catch (Exception ex)
    {
        exception = ex;
        success = false;
        throw;
    }
    finally
    {
        stopwatch.Stop();
        
        // 记录统计
        _usageTracker.RecordExecution(pluginId, success, stopwatch.ElapsedMilliseconds, 
                                       context.ProcessName);
        
        // 记录日志
        if (success)
        {
            _logService.Log(pluginId, LogLevel.Info, 
                $"Executed action '{action}' successfully", 
                null, action, args.ToDictionary(k => k.Key, v => v.Value), 
                stopwatch.ElapsedMilliseconds);
        }
        else
        {
            _logService.Log(pluginId, LogLevel.Error, 
                $"Failed to execute action '{action}'", 
                exception, action, args.ToDictionary(k => k.Key, v => v.Value), 
                stopwatch.ElapsedMilliseconds);
            
            _healthMonitor.RecordError(pluginId, exception!, action);
        }
    }
}
2. Circuit Breaker 集成
在 Circuit Breaker 触发时记录事件：
private void OnCircuitBreakerTripped(string pluginId)
{
    _healthMonitor.RecordCircuitBreakerTrip(pluginId);
    _logService.Log(pluginId, LogLevel.Critical, 
        "Circuit Breaker triggered - plugin temporarily disabled");
}
---
📅 实施计划
Phase 1: 基础设施（2-3 天）
目标：搭建数据层和服务层
1. 数据模型（0.5 天）
   - 创建 PluginUsageStats.cs
   - 创建 PluginHealthReport.cs
   - 创建 PluginLogEntry.cs
2. PluginUsageTracker Service（1 天）
   - 实现 IPluginUsageTracker 接口
   - 实现 JSON 持久化
   - 实现内存缓存和定期保存
   - 单元测试
3. PluginHealthMonitor Service（0.5 天）
   - 实现 IPluginHealthMonitor 接口
   - 实现健康评分算法
   - 单元测试
4. PluginLogService Service（1 天）
   - 实现 IPluginLogService 接口
   - 实现日志文件写入（JSON Lines 格式）
   - 实现日志查询和分页
   - 实现日志轮转和清理
   - 单元测试
5. PluginRegistry 集成（0.5 天）
   - 在 ExecuteAsync 中添加统计/日志钩子
   - 在 Circuit Breaker 中添加健康监控钩子
   - 在 DI 容器中注册新服务
   验收标准：
- [ ] 插件执行时自动记录统计数据
- [ ] 统计数据正确持久化到 JSON 文件
- [ ] 插件错误时自动记录日志
- [ ] 日志文件按日期分割
- [ ] Circuit Breaker 触发时记录健康事件
---
Phase 2: UI 层（2-3 天）
目标：重构 Plugins 页面 UI
1. ViewModel 增强（1 天）
   - 扩展 PluginViewModel：添加 UsageStats、HealthReport、RecentErrors 属性
   - 扩展 PluginManagerViewModel：添加分组逻辑、筛选逻辑
   - 实现 GroupedPluginsCollection 类（用于分组显示）
2. XAML 重构（1 天）
   - 重构 SettingsPluginsPage.xaml：改为分组列表布局
   - 设计插件卡片样式（使用 ui:CardControl）
   - 添加搜索和筛选控件
   - 添加状态徽章和健康评分显示
3. 日志查看器（1 天）
   - 创建 PluginLogViewerDialog.xaml（弹出窗口）
   - 创建 PluginLogViewerViewModel
   - 实现日志列表展示（虚拟化滚动）
   - 实现日志筛选（按级别、时间范围）
   - 实现日志导出功能
   验收标准：
- [ ] 插件按状态/使用量/健康度分组显示
- [ ] 插件卡片显示使用统计和健康评分
- [ ] 搜索功能正常工作
- [ ] 点击"View Logs"打开日志查看器
- [ ] 日志查看器可以筛选和导出日志
---
Phase 3: 增值功能（2-3 天）
目标：添加智能推荐和趋势分析
1. Profile 关联分析（0.5 天）
   - 实现 GetProfilesUsingPlugin 方法
   - 在插件卡片中显示"Used in Profiles"
   - 添加"Jump to Profile"快捷链接
2. 使用趋势图（1 天）
   - 集成 LiveCharts 或 ScottPlot 库
   - 在插件详情中显示 7 天使用趋势（Sparkline）
   - 添加"View Full Stats"弹出窗口（详细图表）
3. 智能推荐（1 天）
   - 实现推荐引擎逻辑：
     - 检测未使用的插件 → 提示禁用
     - 检测频繁使用的应用 → 推荐添加到 Profile
     - 检测错误率高的插件 → 提示查看日志
   - 在页面顶部显示推荐卡片（可关闭）
4. 性能优化（0.5 天）
   - 实现虚拟化滚动（如果插件数量多）
   - 优化日志查询性能（索引、缓存）
   - 实现统计数据的增量更新
   验收标准：
- [ ] 插件卡片显示关联的 Profile 列表
- [ ] 点击插件可查看使用趋势图
- [ ] 页面顶部显示智能推荐
- [ ] 大量日志时查询性能良好
---
🧪 测试策略
1. 单元测试
测试覆盖：
- PluginUsageTracker：记录、查询、持久化
- PluginHealthMonitor：健康评分