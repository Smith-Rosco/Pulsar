# add-purpose-to-legacy-specs

Normalize 40 legacy delta-format specs in openspec/specs/: add required ## Purpose section, flatten ADDED/MODIFIED delta headers into a single ## Requirements section, so openspec validate --all passes.
