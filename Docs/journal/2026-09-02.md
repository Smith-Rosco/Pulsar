# 2026-09-02

> 回填来源：`.workbuddy/memory/2026-09-02.md`（WorkBuddy 原生工作区记忆，gitignored）。2026-09-04 决策将跨 harness 工作记忆统一到 `Docs/journal/`（ADR-019），本文件由原日志无损迁移，正文未改写。

## 文档目录整理（Docs 收敛 + 索引重建）

用户要求整理文档与目录结构。确认方案：中等力度（补索引 + 目录收敛），根目录 116MB 产物暂不动。

### 判定标准（重要，后续新增文档照此分类）

- **`Docs/lessons/` = 可复用教训**：`Scope: Lesson` + `Rule (TL;DR)` + Symptom/Root cause/Correct pattern 结构。回答"看到症状 X 就做 Y"。
- **`Docs/archive/` = 历史快照**：叙事体（"某日发现某问题、如何修的"）、带 `Status: ✅ Completed` 和具体日期，命名 `YYYY-MM-DD-DESCRIPTIVE_NAME.md`。

按此标准把 12 篇从 `lessons/` 移入 `archive/`（`DYNAMIC_*`×3、`QUICK_SWITCH_*`×2、`TUTORIAL_*`×3、`MULTI_WINDOW_SWITCHING_LOGIC`、`RDP_MODIFIER_KEY_STUCK`、`SUBMENU_SLOT_OPACITY_BUG`、`WINEVENT_HOOK_THREAD_CONTEXT`），保留 2 篇真 lesson（`WPF_COMBOBOX_SELECTEDVALUE_ONEWAY_BLANK`、`WPF_RADIOBUTTON_PROPERTYCHANGED_FEEDBACK_LOOP`，原为孤儿文档已补入 AGENTS.md 索引）。

### 目录变更（全部 git mv / git rm，可回滚）

- `Docs/handoff/` 2 篇 → `Docs/archive/`（目录删除）
- `Docs/design/UX_REFACTOR_PROPOSAL.md` → `Docs/proposals/`（目录删除）
- 删除 `Docs/Plugins/test_error_explanation.md`（一次性调试遗留）
- `Docs/` 子目录数 12 → 9

### 关键决策：`Docs/agents/` 保留，不可动

`domain.md` + `issue-tracker.md` 不是普通文档，是**机器消费的技能契约**。`.agents/skills/setup-matt-pocock-skills/SKILL.md` 硬编码了 `docs/agents/*.md` 路径；已安装的 `domain-modeling` / `grill-with-docs` / `improve-codebase-architecture` 会读 `domain.md`。删除会破坏这些技能，且重跑 setup 技能会重建文件。已改为在 AGENTS.md 与 Docs/README.md 中标注"配置非文档"。

### 踩坑

1. **`Docs/Plugins/` 8 个文件一度从工作区消失**（`git rm` 单个文件后整个目录不可访问）。已用 `git checkout -- Docs/Plugins/` 恢复，与 HEAD 逐字节一致。若再遇批量目录操作后目录整消失，先 `git status` 确认再 `git checkout` 恢复。
2. 归档 README 里 `../../../AGENTS.md` 类链接多写了一层 `../`（历史遗留，Docs/archive 下应为 `../../`）。

### 死链状态

写了 `Temp/check_links.py`（git 已忽略，可复用）：扫全仓 md 相对链接。
- 整理前 88 条断链 → 现 59 条，其中 **54 条在 `Docs/archive/` 历史快照里（2026-03 旧布局，有意保留原貌）、3 条 `.agents` 技能模板示例、2 条 CONTRIBUTING 占位符，均为误报或历史原貌**；活跃文档断链已清零。
- 顺带修正：`BasicCommand/SimpleCommandPlugin.cs` 已重命名为 `Command/CommandPlugin.cs`，`SimpleCommandPlugin.Refactored.cs` 已并入正式实现（两处文档引用已更新）。

### 未做（用户明确"暂时别动"）

根目录 `session.jsonl` 6.2MB / `export-*.mp4` 20MB / `Artifacts/` 90MB / `Temp/` 240KB，均已被 .gitignore 忽略。

### 已提交

`56594dc docs(docs): consolidate Docs tree and rebuild the documentation index`，33 文件 / +272 −372，git 正确识别全部 15 次 rename。提交规范沿用 conventional commits（`type(scope): 主题`，主题行小写、可带正文段落）。

当前 `main` 相对 `origin/main` ahead 2（另一条是此前的 `0eda23d` ci 提交），**均未推送**。

## 仓库健康性审查（9.0/10，无阻断级问题）

产出：`Docs/reports/REPO_HEALTH_REVIEW_2026-09-02.md`。

- 构建：3 项目 Release 全绿，0 警告 0 错误；测试 733/733 通过；`dotnet list package --vulnerable` 无已知漏洞；密钥扫描干净（仅测试假密码假阳性）。
- Git 卫生良好：.git 59M、garbage=0、跟踪 1064 文件、唯一大文件 6MB demo.gif、.gitignore 覆盖完整（bin/obj/Artifacts/.codegraph/Temp/.workbuddy）。
- 待办（P2）：`git push` 同步 ahead 2 提交；删除已合并的 `refactor/window-service-deepening` 分支。
- 观察项：net8 项目引用 `Microsoft.Extensions.DependencyInjection 10.0.9` / `Serilog.Extensions.Hosting 10.0.0`（跨大版本但构建测试通过，低风险）。
- 卫生（P3）：工作区 1.2G（Pulsar/bin+obj ~800M 含大量 `*_wpftmp` 中间产物、Artifacts 294M、.codegraph 37M、Temp 日志残留）→ 建议 `dotnet clean` + 删 bin/obj 释放 ~900M。
- 项目惯例确认：构建命令 `dotnet build Pulsar/Pulsar.sln`（根目录无 sln，只有 Pulsar/Pulsar.sln）；健康审查可用 `dotnet list package --vulnerable` + `git branch --merged main` 快速复检。

### 后续动作已执行（用户确认）

- `git push origin main` 成功（8349a80..56594dc），main 与 origin/main 完全同步。
- `git branch -d refactor/window-service-deepening` 删除已合并分支（was 5a75ebc），本地仅剩 main。
- 磁盘清理：用户**拒绝 rm -rf 批量删除**，选择 `dotnet clean` 官方机制 → 已对 Release + Debug 各执行一次（0 警告 0 错误），工作区 1.2G → 1.1G。
- 残留说明：主项目 bin/Release 270M + obj/Release 294M 为 `dotnet publish` 产物（含 win-x64 R2R），dotnet clean 不清 publish 目录；与 Artifacts/ 294M、export-*.mp4、session.jsonl 均属用户保留范围，未动。
- 审查报告输出：`Docs/reports/REPO_HEALTH_REVIEW_2026-09-02.md`（未跟踪，待用户决定是否提交）。

### 审查报告归档（已提交）

- 报告按 archive 命名规范归档为 `Docs/archive/2026-09-02-REPO_HEALTH_REVIEW.md`。
- 索引更新：Docs/README.md 升 v5.1.0（archive 计数 56→57 + 索引行）；Docs/archive/README.md 新增 Section 6「Repository Health Reports」。
- 提交 `51883d3 docs(docs): add repository health review snapshot (9.0/10)`，已推送 origin/main。
- 经验：新建文档目录含单文件违反 Docs 规则（"one file is a smell"）→ 审查快照类文档应直接进 `Docs/archive/` 用 `YYYY-MM-DD-` 前缀，而不是新建 `Docs/reports/`。迁移时先 `mv` 再提交（未跟踪文件不能 `git mv`）；沙箱会拦截含 `rmdir` 的复合命令，改成分步执行。

## 用户视角评测（Pulsar v1.8.x）

- 产出：`Docs/archive/2026-09-02-USER_PERSPECTIVE_REVIEW.md`（8 维度评分，综合 7.2/10；配套评分雷达图内嵌于会话）。
- 结论："工程强、产品弱"。可靠性 8.5 / 产品定位 8.5 / 日常使用 8.0 为强项；视觉定制 5.0 / 上手体验 6.0 为弱项。
- 用户侧关键痛点：README 截图与 demo 视频全是占位符（3 秒生死线）；无安装器/自动更新/签名；CHANGELOG 空白且 README 徽章 v1.8.0 与制品 v1.8.1 不一致；默认 Ctrl+Q 与 Chrome 退出键冲突、无冲突检测；无用户手册/FAQ（Docs 全面向开发者）。
- 快速改进 Top3：截图+视频入 README（半天）→ 补 CHANGELOG 并对齐版本（1h）→ 下载页解释 full/portable 差异 + SHA256（2h）。
- 过程纠错：评测报告一度误放入已删除的 `Docs/reports/`，违反"单文件目录"规范；已按规范迁移至 `Docs/archive/2026-09-02-USER_PERSPECTIVE_REVIEW.md` 并删除空目录（复现并验证了此前教训）。
- 待用户决定：是否按上午流程归档提交（Docs/README.md 升版 + Docs/archive/README.md 增 Section）。
