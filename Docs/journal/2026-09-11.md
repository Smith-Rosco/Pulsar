# 2026-09-11

> 本日前序会话（00:0x–12:3x，轮盘贴边校正 / 使用分析显示名 / W1–W4 等）已轮转至 [archive/2026-09-11.md](./archive/2026-09-11.md)（ADR-021，文件超 15KB 上限，sha256 逐字校验一致）。

## Session (12:44 · 消息对话框溢出 + resx 字面 \n 显示)

**做了什么**
- 用户报告：弹出的对话框内容经常溢出、且出现 "/n" 字样。根因定位（grill auto → full-auto）后同会话修复。
- **根因 1（"/n"）**：双语 resx 9 个 `Notification.*` 键在 `<value>` 里写了 C# 风格 `\n`，XML 不解析转义 → WPF 原样渲染两字符。修复：全部改 `&#10;`（真实换行实体），全文件 grep 证实仅这 18 处含该序列，replace_all 精准（diff 9+9 行/文件）。
- **根因 2（溢出）**：`ShowMessageAsync`/`ShowConfirmationAsync` 固定 XSmall（350×200）无滚动容器，长文（尤其拼接异常消息）被窗口裁切。修复：`DialogSizeConstraints` 新增 `AutoHeight` + `Message` preset（宽 420、高度随内容增长、MaxHeight 480 封顶）；`DialogService.ApplySizeConstraints` 支持 `SizeToContent.Height`；`sys:String` 模板包 ScrollViewer（超封顶滚动）。**自定义内容对话框零行为变更**（滚动容器在字符串模板内，不碰 12 个自带滚动的 Content）。
- 顺带修 `Strings.resx` `Notification.CleanCacheBody` 的 14 空格缩进异常。
- TDD：守卫测试红先行（resx 守卫红：18 处全数抓到）→ 修复后绿。验证：build **0/0**；针对性 **4/4**；全量 **1585/1585**（= 上会话基线 1581 + 4），失败 0 跳过 0，36s。

**关键决策 / 坑**
- **resx 里写 `\n` 是经典坑**：`<value>` 是 XML 文本，C# 转义不被解释。已由 `DialogMessageOverflowGuardTests.ResxValues_ShouldNotContainLiteralBackslashN` 永久钉死（双语全键值扫描）。
- **修复面克制（Q2/Q3 裁决）**：只给消息类对话框换 `Message` preset + 字符串模板加滚动；不嵌套外层 ScrollViewer 到 DialogHostWindow（会改变 12 个自带滚动 Content 的粘性布局行为）。
- **测试环境坑**：STA 裸线程 `DataTemplate.LoadContent()` 解析 `StaticResource Pulsar.Type.Subtitle` 失败（无 App 级合并字典）——测试里补桩 `Application.Current.Resources[key]=16d`，非产品缺陷。
- **沙箱噪声**：`dev.ps1` 跑完偶发 Explorer iconcache 写入被沙箱拦（exit 1），构建/测试输出本身 0 失败——判读时看测试行，勿被 exit code 误导。

**相关引用**
- 文件: `Pulsar/Pulsar/Resources/Strings.resx` + `Strings.zh-CN.resx`（9 键 `\n`→`&#10;`）、`Models/DialogSizeConstraints.cs`（AutoHeight + Message）、`Services/DialogService.cs`（ApplySizeConstraints + 两处 preset）、`Themes/DialogTemplates.xaml`（字符串模板 ScrollViewer）
- 测试: `Pulsar/Pulsar.Tests/Dialogs/DialogMessageOverflowGuardTests.cs`（+4）
- 未决 / 待办: 真机验收（见 Docs/journal/NEXT.md）

---

## Session (13:21 · publish skill 本地默认只产 full)

**做了什么**
- 用户要求修改 publish skill：本地版本（local-artifact/local-version）只需要 full。
- 事实核查：CI `release.yml` 独立跑 `dotnet publish` 产 full+portable，**不依赖 publish.py**（L39-40 仅注释引用参数惯例）→ 改本地脚本零 CI 影响；`watch` 的 Release assets 双包校验保留不动。
- `scripts/publish.py`：`build/pack/all` 新增 `--portable` 开关（默认关）；构建/build-info/断言/打包 ZIP/终态白名单全部按开关条件化；portable 跳过时走 `REPORT.skip`（L1「本次未做」区自动说明）；docstring、parser help、dry-run 计划同步。
- SKILL.md 同步：子命令表、§2（默认 full + 三例命令 + 断言/终态/冒烟改写）、§6 回退路径（手动上传必须 `--portable --installer`）。publish skill 无镜像副本（`.opencode/skills/publish/` 不存在）。
- 验证：`py_compile` OK；dry-run 默认/`--portable` 双路径输出正确；实跑 `all --version 1.14.0 --build 2` **7/7 PASS / 37s**（原双包 46s），仅交付 `Pulsar-1.14.0.2-full.zip` 69.4MB，portable 构建/打包两项 SKIP。

**关键决策 / 坑**
- **保留 `--portable` 而非删除能力**：CI 不可用回退（ADR-026 手动 gh 上传）仍需本地复现 portable；用户诉求是默认行为，不是能力删除。
- **全局参数位置坑（既有）**：`--dry-run` 必须在子命令前（`publish.py --dry-run all …`），放后面 argparse 报 unrecognized。
- 验证产物 `Pulsar-1.14.0.2-full.zip`（与 .1 同 commit，脚本改动不影响 Pulsar 代码）按并存设计保留，用户可自行删除。

**相关引用**
- 文件: `scripts/publish.py`（cmd_build/cmd_pack/argparse/dry-run）、`.agents/skills/publish/SKILL.md`
- CI: `.github/workflows/release.yml`（独立构建，未改）
- 未决 / 待办: 无；消息对话框真机验收仍在 NEXT.md

---

## Session (14:0x–14:1x · 架构审查 round + Secret 写入路径收敛到单一 owner)

**做了什么**
- 按 `improve-codebase-architecture` skill 跑一轮架构扫描：4 个并行 Explore 子代理分头查「设置编辑器簇 / 对话框+启动簇 / 插件元数据簇 / 轮盘渲染+教程簇」，逐条自查（grep 全仓计数 + 行号核对）后产出 **9 个候选**的自包含 HTML 报告（`%TEMP%\architecture-review-20260911-1402.html`，Tailwind + Mermaid CDN，每候选带 before/after 图、证据行号、强度徽章、Top recommendation）。
- 用户指定用 `grill auto`（auto-with-guardrails）完成第一个候选。Top 候选 = **Secret 选取器 / 暂存写入收敛到单一 owner**；其余 8 个候选已登记进 `NEXT.md`。
- 红先行：先写 17 个测试（工作区暂存面 4 / 会话删除 2 / 选取器 + 纯投影 11）→ build 红（`ISecretPickerSeam` 等 4 个编译错误）→ 实施 → 绿。
- 实施：新增 `ISecretPickerSeam`（5 成员）+ 生产 adapter `SettingsSecretPickerSeam`（组合 workspace / session / resolver，自身零状态）+ 纯投影 `SecretEntryProjection`；`SlotEditorWorkspace.PendingSecrets` 由 public 可变 `Dictionary` 收为 `IReadOnlyDictionary` 视图并新增 `UnstageSecret`（`StageSecret` 从零生产调用点变回唯一入口）；`SettingsEditorSession` 新增 `RemoveSecretAsync`（Settings 面恢复唯一写者）；`SecretPickerViewModel` 丢掉 `ISecretStore` + 两个活字典（7 参 → 4 参，首次可无 WPF 直测）；`SettingsViewModel` 删掉两个随之变死的字段。
- 验证：build **0 警告 0 错误**；全量 **1602/1602**（基线 1585 + 17，精确对账），失败 0 跳过 0，38s。

**关键决策 / 坑**
- **根因比「重复」更深**：`SettingsEditorSession.CommitAsync` 的 secret 管线是**只增**合并（没有删除语义）——「删除」无处可去，caller 只能穿过 seam 自己落盘写 store。所以深化的关键不是「把写收敛」，而是**给 seam 补上删除这一格**（`RemoveSecretAsync`）。
- **删除语义保持不变**（原为立即落盘、不可撤销）：本候选只搬 seam 不搬行为，避免引入需要用户裁定的行为分叉；「暂存删除 → 随会话提交」是一个独立的后续决定。
- **顺带清掉死写**：原 `SecretPickerViewModel` 对 `labelMap` 的两处写入是**死写**——`BuildLegacySecretLabelMap()` 每次返回新字典，写进去无人读。已随重构删除。
- **接口面最小化裁决**：一度新增 `TryGetPendingSecret`，但 `IReadOnlyDictionary` 本身已提供 `TryGetValue` → 撤回。workspace 面只留「只读视图 + 两个命名变更方法」。
- **⚠ 工具坑（新增，跨会话有效）**：**同一条消息里对同一个文件发两处 Edit 会互相覆盖**（后写的赢，前一处静默丢失，但两处都回 "Successfully edited"）。本次因此丢了两处改动（构造器里的 seam 创建 + 字段声明删除），是靠 `git diff --numstat` 对账才发现的。→ **同一文件的多处编辑必须串行（一条消息一处）**，改完立即 `git diff` 对账。

**相关引用**
- 新增: `Pulsar/Pulsar/ViewModels/Dialogs/ISecretPickerSeam.cs`、`Pulsar/Pulsar/ViewModels/Settings/SettingsSecretPickerSeam.cs`（含 `SecretEntryProjection`）、`Pulsar/Pulsar.Tests/ViewModels/SecretPickerSeamTests.cs`
- 修改: `ViewModels/Dialogs/SecretPickerViewModel.cs`（重写）、`ViewModels/Settings/SlotEditorWorkspace.cs`、`ViewModels/Settings/SettingsEditorSession.cs`、`ViewModels/SettingsViewModel.cs`、`Pulsar.Tests/ViewModels/SlotEditorWorkspaceTests.cs`、`Pulsar.Tests/ViewModels/Settings/SettingsEditorSessionTests.cs`
- 术语: `CONTEXT.md` 新增 **Secret Picker Seam**（待用户确认后提交）
- 未决 / 待办: ① 9 个候选余下 8 个（NEXT.md）；② 真机验收（secret 新增/编辑/删除/选取四条路径）；③ CONTEXT.md 术语条目是否随下个 commit 入库 —— **已入库 `df0062b`（14:2x）**

---

## Session (14:5x · 架构审查候选 #2 —— 插件参数规格升到共享 Module)

**做了什么**
- 用户指定继续用 `grill auto`（auto-with-guardrails）完成候选 #2。进入时会话工作区已有上一轮的**未提交半成品**（`SlotParameterSpecs.cs` 未跟踪 + 4 个插件 + 测试改动）；本会话先事实核查、设计树裁定，再收口。
- 事实核查（决定节点前自己取证）：`PluginCapabilities.MinPulsarVersion` 生产读取点 **0**（全仓 grep 只命中新测试；`PluginLoader.cs:426/581` 只写；无 JSON 序列化路径）；`PluginTemplate/ExamplePlugin.cs` **根本不声明 metadata**（报告所称「样板依赖内部模块」的张力比原描述弱）；真正的作者契约面是 `PLUGIN_DEVELOPMENT.md:115-173`。
- 三条 guardrail 提问（用户全部采纳推荐）：**范围裁定**回退越界改动 / **作者文档**写入定位 / **CONTEXT.md** 新增术语。
- 收口实施：`Core/Plugin/Metadata/SlotParameterSpecs.cs`（`FilePathParameter` + `ArgumentsParameter` 两个多消费者形态工厂 + `ScriptPathKey`/`ArgumentsKey` 常量；结构字段定死、用户可见文案入参）；WinSwitcher 私有 `ArgumentsParameter` 拆除迁入工厂；VbaRunner / Bookmarklet `scriptPath` → `FilePathParameter`；Command `arguments` → `ArgumentsParameter`；`BuiltInPluginMetadataTests` 新增 4 条跨插件守卫（6 个内建插件首次全量进入 metadata 断言面）。
- 验证：build **0 警告 0 错误**；全量 **1606/1606**（基线 1602 + 4，精确对账），失败 0 跳过 0，40s。

**关键决策 / 坑**
- **⚠ 回退别人的越界改动**：工作区里的 `MinPulsarVersion = null!` → `"1.0.0"` 及其守卫测试与 #2 无亲缘关系，且守卫对象是**零读者字段** —— 为死字段加断言，方向与本仓「删死元素」的文化相反（候选 #9 正是干这个的）。裁定：回退，发现单独登记（NEXT.md 末条）。
- **共享模块的准入规则**：只收 **≥2 消费者**的形态。SecretFill 的 `secretId` / `autoEnter` 各只有 1 个消费者 → 刻意不合入；WinSwitcher 的 `app` / `path` 变体同理。**单消费者的「共享」规格就是多一个没有变化点的 seam** —— 由测试里的 `HaveCountGreaterThan(1)` 哨兵持续看守。
- **外部插件可见性必须显式表态**：`SlotParameterSpecs` 与外部插件必用的 `SlotParameterMetadata` 同 public 命名空间，技术上可引用 → 定位为「内建便利入口、不构成稳定性契约，外部插件自写元数据」，写进 `PLUGIN_DEVELOPMENT.md` 才算了结报告标记的设计张力。
- **工具坑（本会话）**：bash 工具**完全不可用**（`dirname` / `ls` / `head` / `cat` 全 command not found，PortableGit coreutils 缺失）→ 全程 PowerShell + `*>` 重定向 + Read 读回。

**相关引用**
- 新增: `Pulsar/Pulsar/Core/Plugin/Metadata/SlotParameterSpecs.cs`（**未跟踪** —— `dev.ps1 commit` 默认 `git add -u` 不含未跟踪文件，提交前须显式 `git add`）
- 修改: `Plugins/Core/WinSwitcher/WinSwitcherPlugin.Metadata.cs`、`Plugins/Extensions/VbaRunner/VbaRunnerPlugin.cs`、`Plugins/Extensions/BookmarkletRunner/BookmarkletRunnerPlugin.cs`、`Plugins/Extensions/Command/CommandPluginMetadata.cs`、`Pulsar.Tests/Plugins/Core/BuiltInPluginMetadataTests.cs`
- 文档: `PLUGIN_DEVELOPMENT.md`（新增「内建共享规格（SlotParameterSpecs）」小节）、`CONTEXT.md`（新增 **Slot Parameter Spec** 术语）
- 未决 / 待办: NEXT.md（#2 已划线；另登记新发现「`MinPulsarVersion` 只写不读」）

---

## Session (16:5x · 架构审查候选 #4 —— 对话框目录 Module，ADR-033)

**做了什么**
- 用户批准设计树（`grill auto-with-guardrails`，Q1 命中护栏：跨模块接口变更）后实施候选 #4。
- 新增 `Services/DialogCatalog.cs`：`DialogId`（独立值类型）/ `DialogIds`（19 个常量）/ `DialogRegistration`（record：标题键 + 内容类型 + 尺寸 + 按钮 + 主题 + `TitleIsFormat`）/ `DialogCatalog`（静态登记表，19 行覆盖 15 个内容类型）。
- `IDialogService` **纯增量**新增一个重载 `ShowCustomAsync<TViewModel>(DialogId, TViewModel, params object[] titleArgs)`；现有 3 个 `string` 重载原样保留（消息 / 确认 / 输入 / 取色四类 API 仍走它们，它们本就自带标题、无目录行）。
- 迁移 **20 个** `ShowCustomAsync` 调用点（18 处直接 + 2 处 `SettingsDialogFlows` 内部）与其 **5 个** `RunAsync` 调用者：改为传 `DialogId`，调用点不再自选标题键 / 尺寸 / 按钮。4 处内联自定义尺寸（WindowInspector 780×560 / ExampleLibrary 620×480 / ScriptEditor 720×560 / PluginSettings 550×500）**逐字**搬进目录行。
- `SettingsDialogFlows.RunAsync` 由 `(string title, …, DialogButtons, DialogSizeConstraints?)` 收为 `(DialogId, vm, onConfirmed)`；不再向下游透传呈现选择。
- 守卫测试红先行：新增 `Pulsar.Tests/Dialogs/DialogCatalogTests.cs`（8 例）—— id 唯一非空 / 行结构完整 / 查表命中与未知 id 报错 / 标题键**双语**存在 / `TitleIsFormat` 与 resx **值**一致（双语）/ 内容类型有模板 / **模板必须有目录行或豁免（反向钉）**。
- 验证：build **0 警告 0 错误**；全量 **1613/1613**（基线 1606 + 8 新增 − 1 净减），失败 0 跳过 0，36s。

**关键决策 / 坑**
- **⚠ 为什么 id 必须是独立类型而非 `string`**：若新重载签名为 `ShowCustomAsync(string dialogId, TViewModel, params object[])`，**两参调用会被既有重载吃掉**（normal form 优先于 expanded form）→ `dialogId` 被当作字面标题静默渲染。`DialogId` 值类型让重载永不歧义，并把「把标题误传给 id」变成编译错误。这是本次唯一无法靠测试兜住的设计点，必须靠类型。
- **⚠ 目录键不能用 VM 类型**（报告原案即此）：`InputDialogViewModel` 一型三注册（AddSecret Medium / EditSecret Medium / BackupPassword **Small**）、`ProcessPickerViewModel` 一型两尺寸 —— 类型 →（标题, 尺寸）**两个方向都不是函数**。
- **⚠ 侦察漏了一处跨行尾巴**：`SettingsViewModel:1000` 的 `RunAsync` 调用尾部还有 `}, DialogButtons.OkCancel, DialogSizeConstraints.LargeResizable);`（在锚点下方 3 行）。我起初据被截断的 grep 命中行把 `PickProcess` 记成 Medium；编译器报错后读完整段对账 —— 它实际是 `LargeResizable`，与 `InputProfileViewModel` 的 `SelectApplication` **完全同构** → 两个 id 合并为一个。**教训：`grep -n` 的命中行 ≠ 完整调用形态，多行调用必须读完整段再落账。**
- **发现一处既有失真 —— 保持行为不变、不顺手改**：`Dialog.PluginAnalyticsDetail.Title` 的 resx 值（EN `Plugin Details` / ZH `插件详情`）**不含 `{0}`**，而调用点在做 `string.Format(..., item.DisplayName)` → 插件名每次都被静默丢弃。本次**逐字保持现状**：目录行 `TitleIsFormat = false`，删掉那个死实参并留注释指向该 resx 值。修文案会改变用户可见文本，属独立改动。新守卫（`TitleIsFormat` ↔ resx 值）正是让这类漂移在 CI 可见的机制：译者补 `{0}` 会立刻测试失败，直到翻转标记并补实参。
- **`TitleIsFormat` 刻意保留生产读取点**（`DialogService.ResolveTitle` 据它决定是否 `string.Format`），不是「只有测试读的字段」—— 主动避开本仓「为死元素加守卫」（`PluginCapabilities.MinPulsarVersion`）的反模式。
- **工具坑（沿用）**：bash 全程不可用（coreutils 缺失）→ PowerShell + `*>` 重定向 + Read 读回。**日志是 GBK**：Grep 匹配 ASCII 词（warning/error）会全 miss，必须直接 Read 才看到「0 个警告 / 0 个错误」。

**相关引用**
- 新增: `Pulsar/Pulsar/Services/DialogCatalog.cs`、`Pulsar/Pulsar.Tests/Dialogs/DialogCatalogTests.cs`、`Docs/decisions/033-dialog-catalog-single-registration-surface.md`
- 修改: `Services/Interfaces/IDialogService.cs`、`Services/DialogService.cs`、`ViewModels/Settings/SettingsDialogFlows.cs`、`ViewModels/SettingsViewModel.cs`、`ViewModels/Settings/SettingsAnalyticsPageViewModel.cs`、`ViewModels/Settings/PluginViewModel.cs`、`ViewModels/Dialogs/{SecretPickerViewModel, PluginSettingsDialogViewModel, InputProfileViewModel, EditProfileViewModel}.cs`、`ViewModels/AboutViewModel.cs`、`Services/AppStartupCoordinator.cs`；测试 4 文件（`SettingsDialogFlowsTests` 重写、`SettingsAnalyticsPageViewModelTests` / `SecretPickerSeamTests` / `SettingsViewModelDirtyStateTests` 改指新重载）
- 文档: `Docs/README.md`（ADR 索引 033）、`CONTEXT.md`（新增 **Dialog Catalog** 术语）、`Docs/architecture/DIALOG_SYSTEM.md`（v1.3.0）、`AGENTS.md` + `AGENTS.zh-CN.md`（「新增对话框」路由两处修正 —— 旧指引指向 `DialogHostWindow.xaml` 注册模板，实为 `Themes/DialogTemplates.xaml`）
- 未决 / 待办: NEXT.md #4 划线；「`PluginAnalyticsDetails` 标题缺 `{0}`」登记为独立候选；真机验收（对话框外观应逐字不变）

---

## Session (17:0x · 架构审查候选 #3 —— 相对时间格式化单一 owner，ADR-034)

**做了什么**
- 按已确认推荐序推进候选 #3，先侦察后动工（`grill` 单问裁定：阈值统一取 30 天）。
- 新增 `Pulsar/Pulsar/Core/Formatting/RelativeTimeFormatter.cs`（`Pulsar.Core.Formatting`）：`Format(utcTimestamp, utcNow, loc, keyPrefix, absoluteFormat)`。四条阶梯边界（1 分钟 / 60 分钟 / 24 小时 / `RecentDaysThreshold`）、UTC 口径、桶顺序全在模块内；调用点只声明两件**分面**的事——resx 键族前缀与绝对日期格式。
- **「N 天前」上限统一为 30 天**（用户裁定）：`Settings.Analytics` 表格 1–29 天显示相对时间，第 30 天起显示 `MM-dd`。这是本次唯一的用户可见文案变化。
- 三处实现收敛到 1 份：`PluginAnalyticsFormatter.FormatTimeAgo` → 单行委托；`PluginViewModel.FormatTimeAgo` **删除** + `_formatter` 去可空标注 + **8 处不可达 `??` 右支删除**（`:75/:76/:77/:78/:111/:112/:119/:123`）；`UsageStatsReadModel.FormatLastUsed` → 委托，并把时钟读数从方法内部的 `DateTime.Now` 改为 `Project()` 已算出的 `now` 传入（`BuildRow` 增加一个参数，投影首次在注入时钟下确定）。
- 测试：新增 `Pulsar.Tests/Core/Formatting/RelativeTimeFormatterTests.cs`（16 例：阶梯边界矩阵 / 阈值上下沿 / 未来时间钳制 / 键族参数化 / 校验 / 两条 resx 守卫）、`Pulsar.Tests/Helpers/PluginAnalyticsFormatterTests.cs`（4 例，`Plugin.*` 族**首次有覆盖**）、`UsageStatsReadModelTests` +2 条消费级断言。
- 验证：build **0 警告 0 错误**；全量 **1635/1635**（基线 1613 + 22，精确对账），失败 0 跳过 0。

**关键决策 / 坑**
- **红证明（本次刻意做）**：把 `RecentDaysThreshold` 临时改回 7、并把 `UsageStatsReadModel.RelativeTimeKeyPrefix` 改成 `Settings.Bogus`，窄范围跑 → **恰好 5 条预期失败**：`Ladder_(29d)` / `(29d23h)` 吐出 `2026-08-13`、resx 守卫列出 8 个 `Settings.Bogus.*` 缺失键、两条消费级 8 天断言吐出 `09-03` / `2026-09-03`。回滚后复跑 **1635/1635** —— 两条守卫都确实会咬。
- **测试环境 CurrentUICulture 是 zh-CN**：消费级断言用 `string.Format(_loc["Settings.Analytics.DaysAgoFormat"], 8)`（≈`8天前`）而非硬编码英文，红证明的失败信息实证了这点——写死英文会假红。
- **阈值为什么不做成入参**：参数化只会把「漂移」改名成「声明」。真正的机制保证是**调用点无处安放第四个数字**（模块里只有一个常量）。分面差异（键族 + 绝对格式）才留参数——那是「详情卡详述 vs 密集表格紧凑」的真实排版差异。
- **删不可达分支属 #3 而非越界**：`PluginViewModel._formatter` 是唯一构造器无条件赋值的 `readonly` 字段 → `LastUsedSummary` 的 `??` 右支不可达，而私有 `FormatTimeAgo` 的唯一调用点就是该右支。删掉 `FormatTimeAgo` 后 8 处 `??` 必然连带删除，否则编译不过 —— 这是「3 份 → 1 份」的完整收口。
- **缺键不再靠内联英文兜底**：`ILocalizationService` 缺键返回**键名本身**（`LocalizationService.cs:48`），可被守卫发现。改为守卫断言「formatter 请求的每个键在两个 resx 都存在」，且**键名从实现推导**（每桶记录一次请求）→ 守卫不会与实现一起漂移。两条 `RelativeTimeKeyPrefix` public const 是守卫的绑定锚点。
- **测试命名空间**：测试目录镜像生产树 `Core/Formatting/`，但命名空间**去掉 `Core` 段**（`Pulsar.Tests.Formatting`）——`Pulsar.Tests.Core.*` 会遮蔽 `Pulsar.Core.*` 致生产类型解析失败。同 `Tests/Core/Rendering` → `Pulsar.Tests.Rendering` 的既有约定。
- **工具坑（沿用）**：bash 本次可用（`cat` / `grep` / `find` 正常），但 `PowerShell` 工具的 stdout 仍不回传 → 一律 `*>` 重定向后 Read 日志（GBK，Grep 匹配 ASCII 词会全 miss）。

**相关引用**
- 新增: `Pulsar/Pulsar/Core/Formatting/RelativeTimeFormatter.cs`、`Pulsar/Pulsar.Tests/Core/Formatting/RelativeTimeFormatterTests.cs`、`Pulsar/Pulsar.Tests/Helpers/PluginAnalyticsFormatterTests.cs`、`Docs/decisions/034-relative-time-single-owner.md`
- 修改: `Helpers/PluginAnalyticsFormatter.cs`、`ViewModels/Settings/PluginViewModel.cs`、`ViewModels/Settings/UsageStatsReadModel.cs`、`Pulsar.Tests/ViewModels/UsageStatsReadModelTests.cs`
- 文档: `Docs/README.md`（ADR 索引 034）
- 未决 / 待办: NEXT.md #3 划线；分析页表格 8–29 天区间的文案变化（`08-30` → `12d ago`）属已裁定变更，待真机过目

---

## Session (18:2x · 架构审查候选 #8 —— 组合根守卫 + 删 3 条死注册)

**做了什么**
- 按推荐序推进 #8。侦察事实：140 条注册**内联**在 `OnStartup`（`:187-534`）、**组合根零测试**（`Pulsar.Tests/` 里引用 `ServiceCollection` 的 5 个文件全是局部测试容器）、`InternalsVisibleTo("Pulsar.Tests")` **已存在**、注册区间是**纯注册**且捕获的局部变量只有 2 个（`levelSwitch` / `debugOptions`）→ 可测化成本远低于预期。grill auto-with-guardrails，Q1（⚠ high-leverage：范围分叉）经用户裁定取 **A = 提取为具名方法 + 守卫测试**。
- 行级手术把 `App.xaml.cs:187-534` 的注册块**逐字**移入新的 `internal static IServiceCollection RegisterApplicationServices(IServiceCollection, LoggingLevelSwitch, DebugModeOptions)`（同文件、**缩进不变**、**保持原顺序**），`OnStartup` 收为 3 行。
- **删 3 条死注册**（报告只发现 2 条，第 3 条由新守卫抓出）：
  - `IDialogService` —— 删 Tutorial 组里误插的那条，保留核心服务组的那条。
  - Tutorial `StartupCoordinator` —— 删 `Pulsar.Features...` 全限定那条（被 `Features...` 简写那条覆盖）。
  - **`ILocalizationService`（新发现）** —— `AddPluginFoundation`（`PluginFoundationServiceCollectionExtensions.cs:27`）也注册它，且该扩展在 `:504` 被调用 ⇒ **晚于** App 的注册 ⇒ App 那条恒被覆盖。该扩展自陈是「WPF app 与 Pulsar.Simulator 共用的组合根」且**确有第二消费者**（`Pulsar.Simulator/Program.cs:106`）⇒ **不动扩展**，删 App 的重复并留注释。
- 新增 `Pulsar.Tests/Composition/CompositionRootTests.cs`（7 个方法 → **11 个用例**）：完全重复检测 / factory 重复检测 / 关键服务恰好一次（`IDialogService`、`ILocalizationService`、`IAppStartupCoordinator`、Tutorial `StartupCoordinator`）/ 刻意多实现计数 / **Default renderer 注册在最后** / debug factory fail-closed / 容器可构建。
- 验证：build **0 警告 0 错误**；全量 **1646/1646**（基线 1635 + 11，精确对账），失败 0 跳过 0。

**关键决策 / 坑**
- **守卫判据不能是「ServiceType 唯一」**：`IRadialRenderer` ×3、`ISubMenuStrategy` ×2 是**刻意**的 `IEnumerable<T>` 多实现。判据取 `(ServiceType, ImplementationType, Lifetime)` **三元组唯一** → 抓「完全重复」，放行多实现；多实现的**数量**另有断言钉住（防将来「顺手去重」把渲染器/策略删掉）。
- **框架自带注册必须豁免**：`AddLogging` → `AddOptions` 会合法注册多个 `IConfigureOptions<>`（options 本就是集合语义）。首次窄跑就撞上这条假阳性 → 守卫加 `IsFrameworkService`（`Microsoft.` / `System.` 命名空间）过滤，只守「本应用自己的接线」。
- **顺序不变量首次变成可执行断言**：`:265-271` 那条「Default renderer 必须注册在最后，否则 legacy `GetService<IRadialRenderer>()` 回退会取到别的渲染器」过去**只写在注释里** → 新守卫 `RadialRenderer_DefaultImplementation_ShouldBeRegisteredLast` 钉死它。
- **fail-closed 断言要调用到内层 Func**：`Func<IDebugStatePublisher>` 的外层 factory 返回的是**内层 Func**，`throw` 在内层；首次写的 `factory(provider)` 不抛（测试自身错误，第一次窄跑暴露）→ 改为取出内层再调用。
- **真红先行**：提取完成但**尚未删重复**时窄跑 → **恰好 5 条失败**，`RegisteredImplementations` 直接列出 3 条重复（含报告未发现的 `ILocalizationService`）—— 守卫的**发现能力**被实证，而非事后补写。删重复后 11/11 绿。
- **二次红证明**：故意把 Default renderer 移到首位 + 把 debug factory 的 `throw` 改成返回 `null!` → 窄跑**恰好 2 条预期失败**（顺序守卫 + fail-closed 守卫），9/11 通过；回滚后全绿。
- **140 行逐字搬运的施工纪律**：Default-LAST 与 fail-closed `throw` 都是顺序/语义敏感的注释不变量 → 脚本按行号整块剪切（方法体内同为 12 空格缩进，故无需改缩进），改完立即用 `git show HEAD:` + 行集合对比做**反向证明**（140 条逐行/顺序一致，唯一差异是 `Services = BuildServiceProvider()` 的位置，属预期结构重排）。
- **工具坑**：bash 本次**再次劣化**（`grep`/`sed`/`ls` 返回 127）→ 切 PowerShell + Python 做核对；`PowerShell` 工具 stdout 仍不回传 → 一律 `*>` 重定向后 Read。

**相关引用**
- 新增: `Pulsar/Pulsar.Tests/Composition/CompositionRootTests.cs`
- 修改: `Pulsar/Pulsar/App.xaml.cs`（提取 `RegisterApplicationServices` + 删 3 条死注册 + `ILocalizationService` 所有权注释）
- 未决 / 待办: NEXT.md #8 划线；**观察（非缺陷）**：`ILocalizationService` 的所有权落在名为「Plugin Foundation」的扩展里略显错位——但那是 WPF app 与 Simulator 防漂移的正当理由，暂不动

---

## Session (19:4x · 架构审查候选 #9 —— 删页面注册面上的死元素 + MinPulsarVersion 全链清扫)

**做了什么**
- 按推荐序推进 #9（与 #4 同子系统的收尾）。先侦察后动工，**两处死元素由全仓 grep 实证零读取点**：
  - `SettingsPageRegistration.PageType` —— 构造函数存储 + `CloneAsTransientEntity` 内部自传递，**全仓 `.PageType` 零命中**（克隆出来的值同样无人读）。
  - `SettingsPageFactory.RegisterCreator` —— **零调用点**（仅定义）。
- 删除两条死元素 + 顺带清 `using`：`PageType`（ctor 参数 / 属性 / Clone 自传递 / 9 处调用点实参 = `SettingsPageCatalog` 6 行 + `App.xaml.cs` 2 处临时页定义 + `SettingsTransientPagesTests` 3 处）、`RegisterCreator` 方法体 + XML 注释、`SettingsPageRegistration` 的 `using System;`、`SettingsPageCatalog` 的 `using Pulsar.Views.Pages;`。
- **MinPulsarVersion 全链删除（用户 19:3x 裁定「全删」= 选项 A）**：`PluginCapabilities.MinPulsarVersion` 只写不读，删它会级联到**公开插件作者契约** `IPulsarPlugin.MinPulsarVersion`（`PluginLoader:581` 是该成员唯一读者）→ 一次性清掉整条链：`PluginCapabilities` 字段 + `IPulsarPlugin` 成员 + `PluginBase` 覆写 + 9 个生产写入点（`PluginLoader` 2 / `PresetInstallService` 1 / 6 个内建插件）+ 14 个测试写入点（12 文件）。
- 验证：build **0 警告 0 错误**；全量 **1646/1646**（与基线一致 —— 删除零测试引用，即零调用方实证），失败 0 跳过 0；`verify-rules` OK。

**关键决策 / 坑**
- **保留 `PluginManifest.MinPulsarVersion`**：它经 `PluginLoader.IsManifestVersionCompatible`（`:334-342`）做**外部 manifest 的真实版本下限判定**，是唯一有生产读取点的同名字段。删除后全仓 `MinPulsarVersion` 残留**恰好只剩这一处 + 3 个 manifest 测试夹具**（逐条核对过）。
- **红证明（编译器即裁判，本次两段）**：
  1. **删定义、留写入点 → 让编译器枚举**：先只删三处定义，`dev.ps1 build` 报出**恰好 9 个生产写入点**（CS0117）+ `PluginLoader:581,47` 的 CS1061（证明 `plugin.MinPulsarVersion` 的读取确实存在）。这比 grep 更权威 —— 漏一个就会多报一条。
  2. **删完后再写回一处 → 确认删除被类型系统强制**：把 `MinPulsarVersion = "1.0.0"` 写回 `SecretFillPlugin`，build **恰好 2 条 CS0117**（同处 wpftmp 双编译），回滚后复跑全绿。证明该字段**无法被悄悄漂移回来**。
- **23 处删除走脚本 + dry-run 双保险**（本仓铁律）：`_clean9.py` 先 dry-run 打印每条删除的「上一行 / 删除行 / 下一行 / 是否需逗号修正」，逐条核对后 apply。守卫 = 目标行必须含 `MinPulsarVersion =` **且**上溯 12 行内出现 `PluginCapabilities`（自动排除 3 个 `PluginManifest` 夹具，即便行号漂移也不会误删）。逗号修正规则：仅当下一非空行以 `}` 开头时，剥掉上一行的尾逗号 —— 两处「句中删除」（WinSwitcher / Bookmarklet）正确判为免修正。用完即删。
- **`PageType` 是「双点维护」的一半**：目录曾同时写 `typeof(Page)` 与工厂里的 `new Page(...)`。删掉 `PageType` 后目录只剩导航元数据（标题键 / 图标 / 分组 / 旧视图名），页面构造的唯一来源回到 `SettingsPageFactory` —— **「新增常驻页仍需双点」的一半（类型）随之消失**，剩下的是 id 与构造器两处不同信息，属正常按 id 注册表。
- **`SettingsPageFactory` 仍无直接测试**（`CreatePage` 会构造真实 WPF `Page`，需 STA 线程，测试宿主不满足）—— 本次未强行补测，与既有结论一致；这是「覆盖率观察」不是「死元素」，刻意不混入本候选。
- **工具坑**：`SettingsTransientPagesTests.cs` 首次 MultiEdit 撞 E 盘 `EBUSY`，**原样重试即成功**（安全软件扫描抖动的已知现象）；`dev.ps1` 日志经 Python 按 UTF-8 解码后取数字行（失败 0 / 通过 1646）。

**相关引用**
- 修改: `Models/Settings/SettingsPageRegistration.cs`、`Services/SettingsPageCatalog.cs`、`Services/SettingsPageFactory.cs`、`App.xaml.cs`、`Core/Plugin/Metadata/PluginCapabilities.cs`、`Core/Plugin/IPulsarPlugin.cs`、`Core/Plugin/PluginBase.cs`、`Core/Plugin/PluginLoader.cs`、`Features/Presets/Services/PresetInstallService.cs`、6 个内建插件 metadata、12 个测试文件（共 27 文件）
- 未决 / 待办: NEXT.md #9 划线 + 末条「MinPulsarVersion 只写不读」关闭；**架构审查 round 9 候选全数裁决收口**（#1/#2/#3/#4/#8/#9 落地；#5/#6/#7 为「暂缓/待定」态，非本次推荐序）
