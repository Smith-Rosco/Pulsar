# 2026-09-05

## Session (09:39 · push + 待办收尾 + E2E 运行时验证 PASS)

**做了什么**
- push 15 commits 到 origin/main（`9f1e203..b4bd660`），main 与远端同步。
- dogfood `dev.ps1 build`（按需确认模式无沙箱可直接跑）：0 警告 0 错误，6.8s。
- **Pulsar.E2E 运行时端到端验证 PASS**（run `20260905-013738`，18.6s）：`radial-menu-open-via-command.json` 12 步全过——debug 实例启动（PID 22220）、fixture 注入、录屏、命令通道开菜单、`Pulsar.Slot.0` UIA 断言、截图、关闭。visual-ai-ui-automation 收尾完成。
- 核实 `ValidateOnboardingInvariants` gap 已闭环：`ConfigService.cs:300` 已接线并在配置加载路径打 `[ConfigInvariants]` 警告日志（此前 NEXT 记「无人调用」已过时）。
- `.workbuddy/memory/` 检查合规：仅一行指向 journal 的指针（183B），无正文复发（ADR-019）。
- NEXT.md 更新：dev.ps1 / E2E / ValidateOnboardingInvariants 三项划入已完成，仅剩 .workbuddy 长期观察项。

**关键决策 / 坑**
- 坑：PowerShell 控制台把 git stderr 进度渲染成红色"错误"行——push 实际成功（以结果/exit code 为准）。
- 观察（更正）：E2E 截图右下角**可见径向菜单轮盘**（用户视觉确认），UIA 断言与截图一致；菜单跟随召唤时鼠标位置显示，非居中。
- 按需确认模式 = 无沙箱，`dev.ps1` 可直接跑；旧 journal「沙箱禁 native spawn」记录仅适用于旧环境。
- 提交经 `scripts/dev.ps1 commit -All` dogfood（含 untracked 新日文件）。

**相关引用**
- 文件: `scripts/dev.ps1` · `Pulsar/Pulsar.E2E/`（Workflows/radial-menu-open-via-command.json）· `Pulsar/Pulsar/Models/ProfilesConfig.cs` · `Pulsar/Pulsar/Services/ConfigService.cs`
- 产物: `Pulsar/Pulsar.E2E/artifacts/20260905-013738/`（radial-menu-open.png + recording.mp4，gitignored）
- NEXT: `Docs/journal/NEXT.md`

---

## Session (10:04 · rules-stack 落地：AGENTS.md v4.0.0 + verify-rules + ADR-022)

**做了什么**
- grill auto-with-guardrails 全流程：差距复核 → 设计树（Q1–Q10 auto 接受）→ 完整实现。
- **AGENTS.md 瘦身 248→192 行（24.4→17.4KB）v4.0.0**：§3 pitfalls 表 19→Top-5+链接 `Docs/lessons/`；§5/6/7 压缩；§6 加条件加载原则（ADR-022）。
- **`scripts/dev.ps1` 新增 `verify-rules` 子命令**：核验 AGENTS.md 全部 36 个本地引用路径 + 10 个关键文件存在，fail-fast；实测通过（正则确认非空转）。
- 新增 `Docs/agents/harness-matrix.md`（opencode/豆包/WorkBuddy 三 harness × rules/skills 加载矩阵，ADR-022 配套）。
- 新增 `Docs/decisions/022-agents-md-always-on-kernel-and-conditional-loading.md`。
- DEVELOPER.md 三处"897 个测试"→"1000+，以 dotnet test 输出为准"；更新调研报告加复核横幅。
- 验证：`dev.ps1 build` 0 警告 0 错误 7.2s。

**关键决策 / 坑**
- pitfalls 表选 **A（Top-5+链接）**：遵循 SRI 研究 ~150-200 指令可靠性上限；全表属条件加载范畴，留在 `Docs/lessons/`。
- 行数目标 ~160 实际落 192：再砍会伤 invariant/路由/仪式，ADR-022 记录取舍。
- 提交本地不推送（push 留用户复核 audit log 后确认）。

**下一步**
- 见 `Docs/journal/NEXT.md`（2 项待办：复核 audit log 后 push；观察瘦身后 agent 是否经指针取坑位全文）。

**相关引用**
- ADR: `Docs/decisions/022-agents-md-always-on-kernel-and-conditional-loading.md`
- 文件: `AGENTS.md` · `scripts/dev.ps1` · `Docs/agents/harness-matrix.md` · `DEVELOPER.md`
- 报告: `Docs/reports/rules-ai-coding-research-2026-09-04.html`
- NEXT: `Docs/journal/NEXT.md`

---

## Session (10:15 · push + journal 整理)

**做了什么**
- 用户复核 grill audit log **无否决** → push 4 commits（`76389cb`..`53a964c`）到 origin/main（`9dc4ae7..53a964c`）。
- 整理 journal：NEXT.md 把「复核+push」从待办划入已完成（~~划线~~保留历史）；追加本 Session。

**关键决策 / 坑**
- push 前按 §10 复查 git status + fetch：工作区无其他 harness 改动。
- 坑（已知假象）：PowerShell 把 git stderr 进度渲染成红色"错误"行——push 实际成功（exit=0）。
- journal 遵守 ADR-019/021：只追加、不重写不删旧条目；NEXT.md 完成即划线。

**下一步**
- 见 `Docs/journal/NEXT.md`（2 项观察类待办：瘦身后 agent 取坑位行为；.workbuddy 长期观察）。

**相关引用**
- 文件: `Docs/journal/NEXT.md` · `Docs/journal/2026-09-05.md`
- ADR: `Docs/decisions/022-agents-md-always-on-kernel-and-conditional-loading.md`

---

## Session (11:06 · archify 插件系统架构图)

**做了什么**
- 用户指示使用本机 archify v2.17（`~/.agents/skills/archify/`）生成 Pulsar 插件系统架构图。
- 环境：`doctor` 全 15 项 ok（Node v22.22.2）；`validate --repo-root` 启用后核验了 12 处 `sources` 引用（行号与文件路径全部对照 HEAD `8df4281` 真实存在）。
- 产出：`Docs/diagrams/pulsar-plugin-system.architecture.{json,html}`，showcase 9/9 artifact check pass（composition errors=0, warnings=0）；`deliver` 确定性哈希：spec `9a7582b4...` (11 861B) → HTML `cd3021c8...` (732 627B)；`visual-check` 用系统 Chrome 在 1440×900 / 1600×1000 / 1920×1080 / 2048×1320 明暗双主题 8 个视口全 pass（含 `scrollWidth==innerWidth`、`minimumProjectedNodeTextPx≥6`）。

**关键决策 / 坑（archify 几何经验）**
- **sublabel 字号有 9px 上限**（不随文本缩短而变大），`minimumProjectedNodeTextPx` 的下界约束是 `scale≥6/9≈0.6667` → `viewBoxWidth ≤ 1395`（1440 视口 + sidebar）。这是可读性失败的真正约束。
- 标签默认锚点：竖直 `bottom→top` 边的标签锚在「源节点底边上方 6px」（压住源节点），需 `labelDy` 推入 gap；水平边的标签锚在路径 20% 处，过宽时会反压源节点，需 `labelDx`。
- `pipeline→perm` 与 `pipeline→loader` 共用 pipeline 右侧端口，靠 port spread 仍只拉开 15px → loader 标签用 `labelDy` 推走避让 perm 路由水平段。
- `ops→state` 边若走竖向走廊会横穿 `executor→pipeline` → 把 seam 列重排为 registry(top)/ops(mid)/executor(bottom)，ops 与 state 几乎水平对齐。
- 列间距按各标签实测宽度差异化分配（78/78/106/88/93），非均匀 gap 反而更省 viewBox 宽度。
- repo evidence 自动验证是杀手锏：`--repo-root` 让 archify 校验 `sources.path` + `line` 真实存在（不再「标注=装饰」）。
- 不读 renderer 源码也能修完：所有几何修改都从校验诊断的 `supportedFixes` 和 `evidence` 推断。

**相关引用**
- 文件: `Docs/diagrams/pulsar-plugin-system.architecture.json` · `Docs/diagrams/pulsar-plugin-system.architecture.html` · `Docs/diagrams/pulsar-plugin-system.architecture.visual-check.{json,html}` · 4 张 PNG 截图（明暗 × 1440/2048）
- 规范: `~/.agents/skills/archify/SKILL.md` · `schemas/architecture.schema.json` · `examples/web-app.architecture.json`
- 真值源: `Pulsar/Pulsar/Core/Plugin/Runtime/PluginRuntimeKernel.cs` · `Pulsar/Pulsar/Core/Plugin/{PulsarContext,PluginLoader,PluginLoadContext,PluginPermissions}.cs` · `Pulsar/Pulsar/Services/Interfaces/{IPluginRegistry,IPluginExecutor,IPluginRuntimeOps}.cs` · `Pulsar/Pulsar/Models/ProfilesConfig.cs`

---

## Session (11:35 · 提交 archify 图 + 跨 harness 提交纪律)

**做了什么**
- 提交 `7ecb5bd` `docs(diagrams)`：archify 架构图规格 + 独立 HTML（2 files, +15 515）。只暂存这两个确定性交付件。
- 提交 `dadc7b2` `chore(gitignore)`：`Pulsar/graphify-out/` 与 `Docs/diagrams/*.visual-check.*` 入 gitignore（6 个本地产物、约 794KB 从 status 消失）。
- OpenWiki 四项**暂不提交**（用户裁决）：`AGENTS.md`(M) · `.github/workflows/openwiki-update.yml` · `CLAUDE.md` · `openwiki/`。

**关键决策 / 坑**
- **坑（跨 harness，重要）：HEAD 在本会话内被别的 harness 移动**。archify 起步时 `git rev-parse HEAD` = `8df4281`，提交时已是 `fec3ecc`（ADR-023，11:34:12）。我 11:31 写入的 journal archify 记录被该 commit 顺带提交（`fec3ecc` 含 `Docs/journal/2026-09-05.md | 48 ++`）。→ 教训：并行 harness 下提交前必重新 `git status` + `git log -1`，**不能凭会话开始时的 HEAD 判断改动归属**；journal 写入与 commit 之间可能插入他人提交。
- 坑：`dev.ps1 commit -All` = `git add -A`，会把 10:23 graphify / 10:57 OpenWiki 的产物一起卷进本次提交 → 改为手动精确 `git add <具体文件>`。
- 决策：visual-check 截图与 JSON 回执不入 git——可再生本地证据，与 `Docs/media/`「UI 验证截图 never commit」同源；只提交 spec + HTML 这一对确定性交付件（spec 可重编译出 HTML）。
- guardrail 命中：新增 GitHub Actions workflow = 外部契约 → 按 §「暂停是真暂停」停下问用户，未自作主张；用户裁「全部暂不提交」。

**下一步**
- 见 `Docs/journal/NEXT.md`（新增 1 项：OpenWiki 四项待复核入库）。

**相关引用**
- commit: `7ecb5bd` · `dadc7b2` ·（参照）`fec3ecc`
- 文件: `.gitignore` · `Docs/diagrams/pulsar-plugin-system.architecture.{json,html}`
- NEXT: `Docs/journal/NEXT.md`

---

## Session (11:31 · improve-codebase-architecture candidate 1: Page Provider Factory seam)

**做了什么**
- Architecture review 2026-09-05: 扫描菜单执行路径，发现 20 处 GetService/GetRequiredService 服务定位器（MenuSession 3 + CommandPageProvider 7 + ProcessPageProvider 9 + CreateProfileStrategy 1），PageProvider 零测试，CommandPageProvider 构造在 MenuSession 复制两遍且已分歧。
- 实现候选 1 (Strong): 新增 IPageProviderFactory + PageProviderFactory；CommandPageProvider/ProcessPageProvider 改显式构造依赖；CreateProfileStrategy 改 Func<SettingsWindow>；MenuSession ctor 替换 IServiceProvider 为 IPageProviderFactory + 3 个显式可选协作者（IDebugStatePublisher/ISubMenuLayoutEngine/IEnumerable<ISubMenuStrategy>），移除 IPluginRegistry；提取 InsertCreatorSlot 私有 helper。
- 更新 9 个测试文件（10 处 new MenuSession）；TwoPhaseOpen 测试工厂返回真实 CommandPageProvider/ProcessPageProvider；GroupedSlotInteraction/CascadeSubMenuEntry 测试传入 subMenuStrategies。
- 构建 0 警告 0 错误；完整测试 1059 全部通过。
- 文档: CONTEXT.md 新增 "Page Provider Factory" 术语；ADR-023 记录架构决策。

**关键决策 / 坑**
- grill auto-with-guardrails 模式: Q1 (seam 形状) + Q3 (移除 IPluginRegistry) 触及跨模块 interface，auto-accept 并在 audit log 标注供 veto。
- TwoPhaseOpen 测试最初用 mock IPageProvider 返回 no-op provider → 9 个测试失败（slot 未应用）。改为工厂返回真实 provider（用测试 mock 构造）后全部通过。
- GroupedSlotInteraction/CascadeSubMenuEntry 测试的 serviceProvider mock 仅用于注入 IEnumerable<ISubMenuStrategy>；改为显式 ctor 参数 subMenuStrategies。
- PageProviderFactory 有 13 个 ctor 参数 — 宽组合模块，可接受（职责就是接线，无逻辑，singleton，测试 mock 接口）。

**相关引用**
- 新文件: Pulsar/Pulsar/ViewModels/Strategies/{IPageProviderFactory,PageProviderFactory}.cs
- 修改: Pulsar/Pulsar/ViewModels/MenuSession.cs · Strategies/{CommandPageProvider,ProcessPageProvider,CreateProfileStrategy}.cs · App.xaml.cs
- 测试: Pulsar/Pulsar.Tests/ViewModels/{MenuSessionTests,MenuSessionGestureTests,MenuSessionTwoPhaseOpenTests,GroupedSlotInteractionTests,CascadeSubMenuEntryTests,RadialMenuRendererSelectionTests,RadialMenuWindowHandleWiringTests,RightDragGestureIsolationTests,RightDragGestureLeakTests}.cs
- ADR: Docs/decisions/023-page-provider-factory-seam.md
- CONTEXT: CONTEXT.md (Page Provider Factory 术语)
- 架构报告: %TEMP%\architecture-review-20260905-110027.html
---

## Session (13:05–16:10 · OpenWiki init 四连障排查 + 本地补丁 + 入库)

**做了什么**
- 用户安装 langchain-ai/openwiki v0.5.0（openai-compatible → api.teamorouter.cn），`openwiki --init` 连续踩 4 个错，逐一定位修复后跑通：**81m49s 生成 6/9 页**（676 reads / 450 searches），提交 `359dd29` `docs(openwiki)`。
- 错误链与修复：
  1. `Repository planning worker exited without submit_plan` → openai-compatible 通道**默认非流式**，长生成被中转掐断（源码注释明示的已知失败模式）→ `~/.openwiki/.env` 加 `OPENWIKI_OPENAI_COMPATIBLE_STREAMING="true"`。
  2. 换 `glm-5.3-flash` + 流式 → `expected AIMessage or Command, got object` → 给 langchain `AgentNode.js` 插桩抓到非法对象是 **`ChatMessageChunk`** = openwiki issue #659（GLM reasoning 增量流式聚合）→ **GLM 系与该开关不兼容**，换回 `deepseek-v4-flash`。
  3. `This OpenWiki run already has a different persisted plan` → 两个触发源：a) 中断续跑时仓库有新 commit（targetGitHead 8df4281→113aa76），重规划与持久化 plan 不一致（状态文件移至 `~/.openwiki/state-backup-20260905/` 解决一次）；b) **deepseek-v4-flash 同一轮内双提交不同 plan**（.run.json phase=generating + 新 runId 实锤）→ 上游 0.5.0 无修复 → **本地补丁**（见下）。
- 补丁（`npm 全局包` `dist/agent/repository-runner.js`，备份 `.bak-20260905`）：submit_plan 的 `invalid_state("different persisted plan")` 与 submit_page 双提交均降级为软拒绝（告知模型已接受、立即收工），运行继续。
- 迷你仓库回归：规划 5 页 + 全部生成 + EXIT:0，补丁不破坏 happy path。
- 网页版：`openwiki visualize ./openwiki --port 4173`（127.0.0.1，后台常驻）。

**关键决策 / 坑**
- 坑：工具调用能力要用「流式+非流式 × 模型」矩阵实测——端点 curl 单测全过 ≠ langchain 聚合层没问题（GLM 精确卡在聚合而非 API）。
- 坑：flash 级小模型提交后会忍不住再"优化"提交一次；openwiki 防覆盖保护直接击杀整个 run，对小模型不宽容。
- 决策：11:35 session 裁决「四项暂不提交」的 OpenWiki 产物，本次经用户确认**全部入库**（359dd29）：openwiki/（含 .claims/.page-manifest/.last-update/.run.json，续跑必需）+ AGENTS.md + CLAUDE.md + `.github/workflows/openwiki-update.yml`。NEXT.md 对应项完成。
- ⚠️ 该 CI 工作流每天 08:00 UTC 定时跑：推到 GitHub 且配了 key 会**每日消耗 API 额度**——余额紧张期间注意。
- 本地补丁会被 `npm update -g openwiki` 覆盖，更新后需重打或确认上游修复。

**下一步**
- 余额恢复后 `openwiki --update` 补 3 缺页：architecture/system-overview · architecture/window-switching · quickstart（worker 未交稿被跳过，补丁使降级为跳过而非击杀）。
- 反复跳过的页考虑临时切 deepseek-v4-pro 单独补写。

**验证步骤**
- 环境矩阵：`~/.openwiki/.env`（deepseek-v4-flash + OPENWIKI_OPENAI_COMPATIBLE_STREAMING=true；勿与 glm 系同用）。
- 网页版：`openwiki visualize ./openwiki --port 4173`。
- 补丁健康检查：`repository-runner.js` 内搜 "LOCAL PATCH (2026-09-05)" 应有 2 处。

**相关引用**
- commit: `359dd29`
- 文件: `openwiki/`（8 md + 状态）· `.github/workflows/openwiki-update.yml` · `~/.openwiki/.env` · `~/.openwiki/state-backup-20260905/`
- 上游: langchain-ai/openwiki issue #659（GLM 流式聚合）；repository-runner.js.bak-20260905

---

## Session (16:24 · Pulsar × StarPie 再次对比 + 发展方向 HTML 报告)

**做了什么**
- 用户要求「再次对比该项目和本项目，规划后续发展方向」→ 产出 `Docs/reports/2026-09-05-STARPIE_COMPARISON.html`（自包含 HTML，含 8 维雷达 SVG）。
- 对比基线：前次 = `Docs/roadmap/README.md`（08-31 首次对比）+ `Docs/reports/2026-09-03-{MARKET_EVALUATION,REPOSITIONING_PLAN}.html`；本次差分对象 = Pulsar v1.10.0 @ `d7b9753` vs StarPie 本地 Ref 仓库 v1.6.8/v1.6.9（CHANGELOG 全文 + PRD + AGENTS.md 研读）。
- 核心结论：两日演进完全分化——StarPie 火力在用户可感知层（更新三级容灾/30-60ms 冷启动/OCR/托盘），Pulsar 在工程不可感知层（E2E PASS/1059 测试/rules-stack/ADR-022/023）；**分发/触达 1.5 vs 8 是唯一在扩大的差距**；roadmap 借鉴红利已吃完，M1（产品外皮）升格 P0 且更新方案可直接移植 StarPie 源码；渲染器插件化（Neon 载体）为 P1 首位；4/8/12 扇区建议显式决策不采纳。

**关键决策 / 坑**
- conversation_search 未检索到更早的 StarPie 对比会话（HTTP 400 一次）；改以仓库内文档（roadmap/03-报告/journal 归档）为「前次」锚点，口径可差分。
- StarPie README 徽标（v1.4.3、19 测试）已过时——以 CHANGELOG v1.6.8 与 releases/ 目录为准；评分沿用 09-03 十分制口径以便纵向差分。

**相关引用**
- 产物: `Docs/reports/2026-09-05-STARPIE_COMPARISON.html`
- 前序: `Docs/roadmap/README.md` · `Docs/roadmap/IMPLEMENTATION_VERIFICATION.md` · `Docs/reports/2026-09-03-*.html`
- 参考仓库: `E:\8_Project\10_C#\Ref\StarPie`（v1.6.8）

---

## Session (17:21 · P0 任务拆分为 5 个 openspec change 并依次 propose)

**做了什么**
- 将对比报告（2026-09-05-STARPIE_COMPARISON.html）中 P0 两项拆为 5 个 openspec change，逐一经 `/opsx-propose` CLI 流程（new change → instructions → proposal/specs/design/tasks）创建并 validate 全过：
  1. `2026-09-05-cascade-submenu-fan-qa`——Fan 人工 QA 收尾（`skip_specs: true`，QA 验证 + 缺陷回归，不造需求）
  2. `2026-09-05-repositioning-narrative-rollout`——README 重写 + E2E 管线截图 + 三支 demo 视频 + 插件显示名叙事对齐（delta: `plugin-display-identity` ADDED「叙事对齐」需求）
  3. `2026-09-05-in-app-auto-update`——三级容灾检测（API→Atom→302）+ 多镜像故障转移 + SHA256 校验 + About 页 UI（new capability `app-update-service`，5 条 requirement 全带 Scenario）
  4. `2026-09-05-installer-and-portable-packaging`——Inno Setup 安装版 + self-contained Standalone（禁 trim：插件系统反射依赖）+ `dev.ps1 publish`（new capability `app-packaging-distribution`）
  5. `2026-09-05-user-manual-release-assets`——中英手册（任务导向）+ RELEASE_CHECKLIST + 首个 GitHub Release 组装（`skip_specs: true`）
- 依赖顺序：2 → 3 → 4 → 5 为发布链；1 独立。NEXT.md 观察类待办未做成 change（非实现任务）。

**关键决策 / 坑**
- 纯 QA / 纯文档变更按 CLI 约定走 `skip_specs: true`（禁止为过校验虚构需求）；叙事对齐是真实需求级变更，故 change 2 保留 delta 而非 skip。
- 事实核实：Pulsar 全仓无 HttpClient（更新链路是真正的从零新增）；内置插件显示名走 `GetMetadata()` 覆写非 manifest；配置目录 `%AppData%\Pulsar`（ConfigService.cs:118）。
- openspec CLI 1.12.0：`validate` 子命令吃位置参数不吃 `--change`；`.openspec.yaml` 在变更根（archive 后无此文件）。

**下一步**
- 待用户 review 后逐个 `/opsx-apply`（建议顺序：fan-qa → narrative-rollout → auto-update → packaging → manual/release）。

**相关引用**
- 变更: `openspec/changes/2026-09-05-{cascade-submenu-fan-qa,repositioning-narrative-rollout,in-app-auto-update,installer-and-portable-packaging,user-manual-release-assets}/`
- 依据: `Docs/reports/2026-09-05-STARPIE_COMPARISON.html` §4 · `Docs/reports/2026-09-03-REPOSITIONING_PLAN.html` M0–M5

---

## Session (16:27 路 使用分析页 PM 审视 + 暗色主题文字修复)

**做了什么**
- Debug：暗色模式下「暂无使用数据…」仍为黑色 → 根因 `EmptyState.xaml` 的 Title TextBlock 未设 `Foreground`（WPF 默认 `Brushes.Black`），主题无隐式 TextBlock 样式兜底；`SettingsAnalyticsPage` 的 `ApplyTheme()` 时序本身正确（在 InitializeComponent 之后）。
- 修复：EmptyState Title TextBlock 补 `Foreground="{DynamicResource TextFillColorPrimaryBrush}"`（连带修复 Slots/Plugins/SecretPicker/LogViewer 共 5 处复用点）。
- 验证：`dev.ps1 build` 0 警告 0 错误。
- PM 审视（建议未改码）：时间范围筛选为死 UI（`SelectedIndex` 绑 enum 转 int 失败 + `SetFilter` 无 XAML 接线）；热力图/今日·本周 KPI 不随筛选联动；「本周/本月」是滚动 7/30 天而非自然周/月；`BuildTrendData` 用 UTC 键而 tracker 写本地日期（UTC+8 凌晨错位 1 天）；ViewLogs 命令为空实现；CSV 导出在 VM 直用 SaveFileDialog+File.Write（违反可 mock seam）；错误消息硬编码英文。

**关键决策 / 坑**
- 坑：动态主题下任何 TextBlock 必须显式绑 TextFill*Brush；WPF 默认前景是黑色，不设即黑。
- 坑：`SelectedIndex` 绑 enum 会静默失败，筛选下拉不可用——枚举绑定需转换器或改 SelectedItem。

**相关引用**
- 修改: `Pulsar/Pulsar/Views/Controls/EmptyState.xaml`
- 页面: `Views/Pages/SettingsAnalyticsPage.xaml` · `ViewModels/Settings/SettingsAnalyticsPageViewModel.cs` · `ViewModels/Settings/UsageStatsReadModel.cs`

---

## Session (16:4x 路 使用分析页 P0/P1 实现落地)

**做了什么**
- P0#1 时间筛选修复：ComboBox 弃用 `SelectedIndex`（enum→int 转换失败）→ `SelectedValuePath="Tag"` + `SelectedValue` 绑定 + 每项 `Tag="{x:Static models:AnalyticsTimeRange.X}"`；删除从未接线的死命令 `SetFilter`。
- P0#2 ViewLogs 落地：VM 注入 `IPluginLogService?`/`IDialogService?`（可选，兼容 DI 默认值），复用 `PluginLogViewerViewModel` + `ShowCustomAsync(Large)` 打开日志对话框；删除原空实现。
- P1#4 自然周/月：`Project()` 口径改为本周一零点 / 本月 1 号零点（`StartOfWeek`），不再是滚动 7/30 天。
- P1#5 时区：`BuildTrendData` 由 `DateTime.UtcNow` 改走实例时钟 `_clock()`（本地日期，与 tracker 的 DailyStats 键一致）。
- P1#3+#6 联动：`PluginUsageStats` 新增 `DailySlotUsage`/`DailyHourlyUsage`（按日 slot/hour 计数，旧 JSON 缺省为空、向后兼容）；tracker 记录/清理/深克隆同步；热力图与"今日/本周"KPI 现按所选范围从按日数据聚合（AllTime 仍读全量字典）。ReadModel 增加可注入时钟 `Func<DateTime>?` 供确定性测试。
- 测试：tracker 3 处断言扩展；read model 新增自然周/月/联动/本地日期 4 个测试并给 KPI 测试固定时钟；新增 `SettingsAnalyticsPageViewModelTests`（筛选重投影、ViewLogs 空服务不抛、打开对话框 1 次）。全量 1060 通过，0 警告 0 错误。

**关键决策 / 坑**
- 坑：MS.DI 支持可选参默认值（现有 `PluginViewModel` 同款），新增可选服务注入不需要改注册。
- 坑：`PluginLogViewerViewModel` 无公开 `PluginId`，测试验证用 `PluginName`；其 ctor 会 fire-and-forget `LoadInitialAsync` → mock `IPluginLogService.GetLogs` 必须返回空 List 防 NRE。
- 决策：热力图"联动"依赖新按日维度——旧 JSON 在非 AllTime 下热力图为空属预期（`HasHeatmap=false` 自动隐藏卡片），不会显示误导性旧数据。

**相关引用**
- 修改: `Models/PluginUsageStats.cs` · `Services/PluginUsageTracker.cs` · `ViewModels/Settings/UsageStatsReadModel.cs` · `ViewModels/Settings/SettingsAnalyticsPageViewModel.cs` · `Views/Pages/SettingsAnalyticsPage.xaml`
- 测试: `Pulsar.Tests/Services/PluginUsageTrackerTests.cs` · `Pulsar.Tests/ViewModels/UsageStatsReadModelTests.cs` · 新增 `Pulsar.Tests/ViewModels/SettingsAnalyticsPageViewModelTests.cs`

---

## Session (17:5x 路 分析页 E2E 运行时验证打通)

**做了什么**
- 补上上一轮明确留口子的运行时验证：用 Pulsar.E2E 真机驱动调试实例，走到设置→分析页。此前 E2E 够不到设置页（命令管道只有 menu-open/close、导航项/页面无 AutomationId、统计文件未隔离），本轮打通最小通路：
  - App（debug 作用域）：`DebugCommandServer` 新增 `open-settings` 命令（DI 解析 SettingsWindow + Show/Activate）；`SettingsWindow.BuildNavigationItems` 给导航项加 `Pulsar.Settings.Nav.<Id>` AutomationId；分析页加 `Pulsar.Settings.Analytics.EmptyState/FilterCombo/RefreshButton`；`PluginUsageTracker` 在 `--ui-debug` 下重定向到 `%AppData%\Pulsar.Debug\PluginUsageStats.json`（与 ConfigService 同款隔离，绝不读写用户真实统计）。
  - E2E：新增 `command` 步骤类型（WorkflowModel/Parser/Runner）；`AppLauncher` 支持同名 `PluginUsageStats.json` fixture（无则删残留，保证空态确定性）；修复 `WorkflowRunner` 未创建 `artifacts\<runId>` 目录导致截图/录屏必挂的 bug。
- 新增 2 个工作流 + 2 个 fixture：`settings-analytics-empty-dark`（暗色空态）、`settings-analytics-data-dark`（暗色 + 3 插件 stats fixture，含自然周口径验证数据）。两轮均 PASS（23.5s / 20.9s）。
- 验证结论：①暗色空态标题文字——裁剪区域像素统计 max=255 亮像素 2754 个、背景 min=32，白字深底，EmptyState Foreground 修复运行时生效；②数据视图 KPI 70/3/12/50 与手算 fixture 期望完全一致（自然周口径运行时正确），插槽/小时热力图数值正确；③筛选 ComboBox 与刷新按钮在数据态可见、空态仅空态卡可见。
- 补测试：`WorkflowParserTests` 覆盖 command 步骤（解析 + 缺 command 报错），13/13 通过。全量 1060/1060 仍通过。

**关键决策 / 坑**
- 坑：`Pulsar.Settings.NavView` 的 AutomationId 在 Wpf.Ui NavigationView 复合控件上不暴露，改用真实存在于 UIA 树的 `Pulsar.Settings.SaveChangesButton` 断言窗口。
- 坑：stats fixture 首版写成字典，实际 tracker 持久化格式是 camelCase JSON **数组**（`List<PluginUsageStats>`）→ 反序列化抛 JsonException，页面回退空态；改数组格式后数据态 PASS。
- 坑：AppLauncher 只认同名 `PluginUsageStats.json`，首个 fixture 命名 `stats-sample.json` 未安装；E2E 脚手架本身还有 run 目录未创建 bug，一并修复。
- 决策：所有改动 debug 作用域——生产路径零行为变化（AutomationId 为纯附加元数据，不用户可见）。

**相关引用**
- App: `Services/DebugCommandServer.cs` · `Views/SettingsWindow.xaml.cs` · `Views/Pages/SettingsAnalyticsPage.xaml` · `App.xaml.cs`
- E2E: `Workflow/WorkflowModel.cs` · `Workflow/WorkflowParser.cs` · `Runner/WorkflowRunner.cs` · `Driver/AppLauncher.cs` · `Workflows/settings-analytics-*-dark.json` · `Fixtures/default-profiles-dark.json` · `Fixtures/PluginUsageStats.json`
- 测试: `Pulsar.Tests/E2E/WorkflowParserTests.cs`；产物 `Pulsar.E2E/artifacts/20260905-analytics-*-dark*`（gitignored）

---

## Session (17:5x openspec 排序 + apply 第一个 change)

**做了什么**
- 现存 5 个 openspec change 全部 `in-progress`、0 任务完成。给出推荐执行顺序（依赖 + 风险）：
  1. `cascade-submenu-fan-qa`（15 任务，无外部依赖，M1 阻塞项，IMPLEMENTATION_VERIFICATION 未落地清单 #1）
  2. `repositioning-narrative-rollout`（产出 RELEASE_NOTES 模板与截图，Change 5 依赖）
  3. `in-app-auto-update`（代码主体，不依赖打包）
  4. `installer-and-portable-packaging`（Change 5 依赖其产物）
  5. `user-manual-release-assets`（依赖 2 + 4，收尾）
- apply 第 1 个 change 的自动化部分（任务 1.1 / 1.2，2/15）：
  - `openspec/changes/2026-09-05-cascade-submenu-fan-qa/qa-checklist.md`：A–H 八组用例（Fan 渲染/命中、Ring 回落、分页叠加、双主题、高 DPI、外甩取消、窗口切换回归），含准备步骤、通过标准、Change History，沿用 `renderer-plugin-registry` 格式。
  - 两个 E2E fixture：`Pulsar/Pulsar.E2E/Fixtures/cascade-submenu-fan-qa.json`（Global 下三个根 slot：2/3/5 个子动作，子动作用 `com.pulsar.command/sendkeys` 输出 ASCII 标记 `QA-2A`…）、`-paged.json`（仅 `slotsPerPage: 4`，供分页用例）。
  - §2 八项人工 QA 标记为待人工执行后暂停（requires human）；§3/§4 依赖 QA 结果。

**关键决策 / 坑**
- 坑（分页）：`SlotsPerPage` 是根环与子环**共享**页大小（ADR-011 决策 2），默认 8 时 5 子项只有 1 页、根本不会分页 → 必须降到最小值 4 才能造出 2 页（4+1）。故单靠一个 fixture 覆盖不了 2.4，做成两个文件而不是让 QA 手改设置。
- 坑（序列化）：`SubMenuLayoutStyle` 枚举**没有** `JsonStringEnumConverter`，配置里是数字（Ring=0/Fan=1）——fixture 选择**省略** `layoutStyle`（null → 读者按 Fan 处理），既贴合真实用户配置也避开魔法数字。
- 坑（进入方式）：级联是**左键单击**根 slot 进入，不是悬停（`cascade-submenu-entry` spec）；子项可左键单击或松修饰键执行；子菜单内右键或中心 Back 返回根环。
- 坑（外甩）：flick-out 只对手势唤出生效且以**根环**半径为基准（ADR-011 决策 7），fixture 里 `enableRightDragSummon=false`，2.7 需 QA 临时打开开关。
- 决策：子动作用 `sendkeys` + 唯一 ASCII 标记（而非 `open-settings`），否则无法区分"命中了哪个子项"；标记必须 ASCII（SendKeys 送中文受输入法影响）。checklist 明确要求焦点先落在空白记事本。
- 决策：加载方式走 `%AppData%\Pulsar.Debug\Profiles.json` + `Pulsar.exe --ui-debug`（隔离配置，不碰用户真实 `Profiles.json`）。

**相关引用**
- 新增: `openspec/changes/2026-09-05-cascade-submenu-fan-qa/qa-checklist.md` · `Pulsar/Pulsar.E2E/Fixtures/cascade-submenu-fan-qa.json` · `...-paged.json`
- 修改: `openspec/changes/2026-09-05-cascade-submenu-fan-qa/tasks.md`（1.1/1.2 勾选）
- 依据: `openspec/specs/cascade-submenu-layout/spec.md` · `cascade-submenu-entry/spec.md` · `Docs/decisions/011-cascade-submenu-layout-and-paging.md`

---

## Session (18:4x 起 QA 环境 + checklist 通俗化)

**做了什么**
- 把 change `2026-09-05-cascade-submenu-fan-qa` 的「准备」真正跑起来：
  - 构建 `dotnet build Pulsar/Pulsar.sln` → **0 警告 0 错误**（10s）。
  - fixture 装入 `%AppData%\Pulsar.Debug\Profiles.json`（先备份为 `Profiles.json.bak-before-fan-qa`），校验：slotsPerPage 8 / theme Light / onboarding Complete / 三个 slot 子动作 2-3-5。
  - 启动调试实例（PID 28796 存活）：`Pulsar.exe --ui-debug --ui-debug-hooks`，日志确认 `Hotkey service initialized (ui-debug-hooks opt-in)` + 配置校验 0 错误 0 警告。
- `qa-checklist.md` 改写为**通俗口径第二版**：开头加「一句话背景」解释 Fan/Ring，每个用例改成「怎么做 + 该看到什么」，去掉术语堆砌（死区/外甩/页作用域都给了白话解释），补操作口诀表（唤出/展开/执行/返回/翻页）。
- G 组按用户选择「先跑 A–F+H，G 之后再说」标注为 🚫 本轮不执行，并写清「切真实配置补跑 + 还原」的三行命令。

**关键决策 / 坑（本轮最有价值的两条）**
- 坑：`--ui-debug` **不注册全局热键**（`UI debug mode: skipping hotkey, mouse-gesture and keyboard-hook registration`），只加 `--ui-debug` 的话 `Ctrl+Shift+Q` 唤不出轮盘，QA 直接进行不下去。加 **`--ui-debug-hooks`** 才注册真实热键（配置仍隔离在 `Pulsar.Debug`）——`AppStartupCoordinator` 第 204 行 `registerHotkeys = !IsUiDebug || EnableHotkeyHooks`。
- 坑：**右键拖拽手势在任何 debug 实例里都关**，无开关（`the mouse-gesture hook stays off in every debug run`）→ G 组只能靠真实配置的普通实例测，隔离方案天然覆盖不到。
- 坑：NuGet `error : Value cannot be null. (Parameter 'path1')` 又是老坑（bash 缺 Windows env）。bash 下构建必须带 env 前缀：`USERPROFILE/APPDATA/LOCALAPPDATA/ProgramData/ProgramFiles/ProgramFiles(x86)/CommonProgramFiles/CommonProgramFiles(x86)/SystemRoot`。
- 坑：`scripts/dev.ps1 build` 在本环境退出码 **127**（脚本自己已补 env、也找到了 dotnet 回退路径，但 native 子进程没跑起来 → `Get-StepExitCode` 判 127）。结论：**别在 WorkBuddy 里用 dev.ps1，直接用 bash + env 前缀的 dotnet**。
- 坑：用 `Start-Process` 起 GUI，进程会被环境回收（日志停在启动完成、无关机记录）。需要 `dangerouslyDisableSandbox` 才能存活（本次 PID 28796 存活）。

**相关引用**
- 修改: `openspec/changes/2026-09-05-cascade-submenu-fan-qa/qa-checklist.md`（通俗化 + G 组标注 + `--ui-debug-hooks` 说明）
- 代码依据: `Services/AppStartupCoordinator.cs:195-226`（debug 模式钩子策略）· `Core/Debug/DebugModeOptions.cs:38`（`--ui-debug-hooks`）
- 环境: `%AppData%\Pulsar.Debug\Profiles.json`（fixture 已装入，备份同名 `.bak-before-fan-qa`）

---

## Session (19:1x PowerShell 复制 & 实例生命周期)

**做了什么**
- 用户反馈按 checklist 跑第三行后 `copy` 报错 `A positional parameter cannot be found that accepts argument '%APPDATA%...'`（PS 里 `copy` = `Copy-Item` 别名，不展开 `%VAR%`）。立刻把 checklist 准备段重写为 PowerShell 原生（`Copy-Item + $env:APPDATA`，detached `Start-Process`），并解释 cmd/bash 各自改写。
- 我之前起的实例（PID 28796）已下线 —— 不是用户的问题，单纯是它跑到 ~18 min 时被环境回收了；和上次一样的回收行为。在用户自己的 PowerShell 里启应该活更久，但还是给了 `Stop-Process` 这一步以便任何时候重启都能干净覆盖 fixture。
- 重新拉起（PID 31136），日志确认热键 opt-in + 配置校验 0 错误。

**关键决策 / 坑**
- 坑：checklist 用 `bat` 代码块 + `copy /Y ... %APPDATA%` 对 PowerShell 用户不可直接复制。把准备段从 `bat` 改成 `powershell` + 用 `Copy-Item/$env:APPDATA`，是最不容易翻车的写法。
- 修正：上一次 journal 写「dev.ps1 在本环境 127 → 用 bash + env 前缀」是 WorkBuddy 沙箱的现象，**用户在本地终端 dev.ps1 build 是正常返回的**（输出 `[dev] done in 3.4s`）。memory 里的 scoped 描述（WorkBuddy）正确，journal 这条只是补充上下文。
- 决策：启动调试实例现在统一用 detached `Start-Process ... --ui-debug --ui-debug-hooks`，不会卡 PowerShell 终端。

**相关引用**
- 修改: `openspec/changes/2026-09-05-cascade-submenu-fan-qa/qa-checklist.md`（准备段改 PowerShell 原生）

---

## Session (19:2x 环境方案变更：debug 实例无鼠标交互，切真实配置)

**做了什么**
- 用户实测发现：隔离调试实例里 `Ctrl+Shift+Q` 能唤出轮盘，但**左键单击毫无反应**。回源排查：
  - 左键链路：`GlobalMouseHook`(WH_MOUSE) → `GlobalMouseService.OnMouseEvent` → `RadialMenuViewModel.OnGlobalMouseEvent(RadialMenuViewModel.cs:226)` → `MenuSession.HandleGlobalMouseClickAsync(MenuSession.cs:1438)` → slot.SubSlots>0 → EnterSubMenuAsync(CascadeSubMenuDescriptor)。
  - 根因：`AppStartupCoordinator.cs:213` — `GlobalMouseService.Initialize()` 仅在**非** ui-debug 时调用，设计注释明确「A debug run must never capture the user's actual desktop input by surprise」，无任何开关。
  - 结论：`--ui-debug` 下左键/悬停/滚轮全部失效（OnMouseMove 同源）——不止 G 组，**整套人工鼠标 QA 都不可能在隔离实例做**。之前「先跑 A–F+H，G 之后再说」的前提不成立。
- 用户确认切换方案：**备份真实配置 → 装入 fixture → 正常实例（无任何 --ui-debug）跑 A–H 全部用例 → 验完还原**。
  - 备份 `%APPDATA%\Pulsar\Profiles.json` → `Profiles.json.bak-before-fan-qa`（校验：1 命令槽 + 2 切换槽俱在）。
  - 正常实例 PID 39736，日志确认 `Hotkey service initialized`（无 ui-debug 后缀）+ **`GlobalMouseService initialized.`**（鼠标钩子已装）+ 配置校验 0 错误。
- checklist 更新（第三版）：准备段改为真实配置四步流程 + 「验完还原」三行命令；G 组去掉 🚫 恢复必测；通过标准恢复 G3；Change History 记录环境方案变更。

**关键决策 / 坑**
- 坑（本轮最重要）：**人工鼠标 QA 与 ui-debug 实例天然互斥**。隔离配置 + 鼠标交互二选一，Pulsar 没有第三个选项；别再试图用 `--ui-debug-hooks` 救鼠标钩子（它只管热键/键盘钩子）。唯一通路是真实配置 + 备份/还原。
- 坑：QA fixture 里 `enableRightDragSummon=false`，G 组开始前要在设置里手动打开「右键拖拽唤出」。
- 决策：fixture 内容不动（它同时是 E2E 可复用的隔离 fixture）；QA 环境的隔离性由「备份/还原」流程保证，而非 debug 模式。

**相关引用**
- 代码: `Services/AppStartupCoordinator.cs:213-218`（鼠标钩子门控）· `Services/GlobalMouseService.cs:24-32`（Initialize 订阅钩子）· `ViewModels/RadialMenuViewModel.cs:226-290`（点击事件唯一入口）
- 修改: `qa-checklist.md`（第三版：真实配置流程 + G 恢复必测）
- 环境: `%APPDATA%\Pulsar\Profiles.json`（已换 fixture）· `Profiles.json.bak-before-fan-qa`（还原点）

---

## Session (19:4x–20:0x Fan 方向几何缺陷：QA 首个修复)

**做了什么**
- 用户人工 QA 首轮 A 组全挂：「Fan2 展开后前 2 个 slot 消失、其余空白；Fan3 前 3 个；Ring5 前 5 个；形状都是圆形」。
- 定位（回源 + 新定位性测试实证）：
  - 先查渲染链（`CommandPageProvider.RefreshVisuals` 的 SubSlots 接线 → `CascadeSubMenuStrategy.ConfigureSubMenu` 填充 → `SetColor("")` 走 null 画刷 → `SlotOrb` IsEnabled 触发器只是 Opacity 0.3+blur → XAML Canvas.Left/Top 直绑 X/Y → `RefreshAnimationTargets` 只缓存磁吸目标），全部排除。
  - 写 `CascadeSubMenuLayoutRuntimeTests`（真 `SlotLayoutEngine` + 真 `SubMenuLayoutEngine` + 真 `MenuSession`）：Ring5（54 半径均布）与 filler（留根环 90）**通过**、Fan2 **失败**——实测 child0 中心 x=296.77 = 250+54·cos(−30°)，即 Fan 翼被放到了绝对 −30°/+30°。
  - 根因：`SubMenuLayoutEngine.ComputeChildPositions` 的 **Fan 分支漏加 `DirectionRadians`**（翼角相对父方向 ±30°/0° 被当绝对角）；Ring 分支正确加了。且 `HitTestFan` 是把鼠标点减去 DirectionRadians 再比对的（正确）→ **布局与命中在非朝东父槽位上全面分家**。Fan 子项叠进右侧根环槽位 →「消失」；filler 留在根环 →「其余空白、都是圆形」。
  - 单测为什么没拦住：`SubMenuLayoutEngineTests` 的 `Pose.DirectionRadians = 0`，相对角==绝对角，bug 完美掩盖；Ring 有 `ShouldRespectParentDirection` 测试而 Fan 没有。
- 修复：Fan 分支翼角加 `DirectionRadians`（一行 + 注释）。
- 回归测试（+6，全量 **1066/1066** 绿，原基线 1060）：
  - `SubMenuLayoutEngineTests.ComputeChildPositions_Fan_ShouldRespectParentDirection`（direction=−90° 时三翼落 −120°/−90°/−60°）
  - `SubMenuLayoutEngineTests.Fan_LayoutAndHitTest_ShouldAgreeAtEveryChildCenter`（4 个父方向 × 1–3 子项，布局位置命中回自身）
  - `CascadeSubMenuLayoutRuntimeTests` ×3（Fan2 落小环 / Ring5 均布小环 / filler 留根环）
- tasks.md 3.1 勾选；checklist Change History 记录 A 组首轮结果；重启真机实例（PID 38472）待用户重验。

**关键决策 / 坑**
- 坑（本轮最典型）：**几何默认值掩盖方向 bug**——pose direction=0 时「忘加方向角」类缺陷在单测里完全不可见。教训：凡「相对某方向」的几何，必须有「方向 ≠ 0」的测试；`Fan_LayoutAndHitTest_ShouldAgreeAtEveryChildCenter` 这种布局↔命中对账测试是防分家的通用武器。
- 坑：QA 缺陷的第一反应别奔着「渲染/绑定/透明度」去（我先后排除了 5 个渲染层假设），先用最小定位测试把「数据对不对」和「画得对不对」分开——这次 10 分钟就锁定在几何层。
- 决策：Ring5 位置链路被证实正确（测试通过），用户对 Ring5 的「消失」观感同样由 Fan 分支错误的兄弟现象（54 小环与中心圆 6 DIP 重叠 + 槽位拥挤）混合造成，待重验时再单独评估；若重验后仍觉得小环太贴中心，那是 ADR-011 决策 6 的 `SubMenuRingRadiusRatio=0.6` 调参问题，需走 Amendment 而非顺手改。

**相关引用**
- 修改: `Services/SubMenuLayoutEngine.cs`（Fan 分支 + DirectionRadians）· 新增 `Pulsar.Tests/ViewModels/CascadeSubMenuLayoutRuntimeTests.cs` · `Pulsar.Tests/Services/SubMenuLayoutEngineTests.cs`（+2 测试）
- 更新: `qa-checklist.md`（A 组首轮结果 + 重验要求）· `tasks.md`（3.1 勾选）
- 环境: QA 实例已用修复后二进制重启（PID 38472），真实配置 fixture 仍在位

---

## Session (20:1x Fan 修复重验 + QA 暂停收尾)

**做了什么**
- 修复后重验：用户确认**子项可见、方向正确**——Fan 方向几何缺陷修复生效（A1/A2 核心）。
- 用户反馈「位置仍有问题」但决定本轮到此为止：记录为遗留项（初步指向 ADR-011 决策 6 `SubMenuRingRadiusRatio=0.6` 的小环半径参数——54 DIP 小环贴中心圆、Ring5 拥挤），**需走 ADR-011 Amendment**，不在本 change 顺手改；具体现象下轮 QA 明确。
- 文档收口：`qa-checklist.md` Change History 补重验结果；`tasks.md` 文件头加「执行状态（QA 暂停点）」块，1.2 的载入方式从过时的 Pulsar.Debug 方案改为真实配置方案；`CHANGELOG.md` Unreleased/Fixed 加 Fan 方向缺陷条目（机制 + 测试矩阵 + 基线 1066）。
- 提交：两个逻辑 commit——① QA 物料 + Fan 修复 + 回归测试（fix(submenu)），② 今日 4 个 M0/M1 提案 change + StarPie 对比报告（docs(openspec)）。change **不归档**（QA 未收口）。

**关键决策 / 坑**
- 决策：「位置仍有问题」不阻塞本 change 的缺陷修复收口——3.1 的修复范围是「行为回归既有 spec」（Fan 翼随父方向），位置参数口径属 ADR-011 修订，二者分开走。下轮 QA 先拍下具体现象截图再定 Amendment 内容。
- 备忘：QA 恢复时直接读 qa-checklist 准备节（备份 → 拷 fixture → 正常实例 → 验完还原），当前真实配置仍是 QA fixture（`Profiles.json.bak-before-fan-qa` 是还原点）。

**相关引用**
- 更新: `CHANGELOG.md`（Fixed 条目）· `qa-checklist.md` · `tasks.md`（执行状态块）
- 提交: fix(submenu) + docs(openspec) 两个 commit（main）
