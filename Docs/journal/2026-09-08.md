# 2026-09-08

## Session (09:48)

### 做了什么
- git push 收尾：fetch 后发现 ahead 148 虚高（跟踪引用曾被外部进程回写），实为 ahead 13；`git push origin main` 成功（`6b91e79..09a7f1d`，文档结构治理系列 13 commits）。遗留：`Docs/reports/2026-09-07-DOC_STRUCTURE_AUDIT.html` 未暂存改动（data-page-node-id 属性噪声）待定夺。
- 架构深化审查（improve-codebase-architecture）：4 并行分片扫 5 热点 → 13 条发现收敛 8 候选 + 4 次要观察；HTML 报告交付并打开；Top 指向 C1。
- **C3 落地（本会话主任务）**：grill auto-with-guardrails 跑完决策树（Q2 用户裁定"选择删除"），TDD 实施：
  - `QuickSwitchEngine` 成为 Window History 单一权威：新增 `SetMenuSnapshot/GetMenuSnapshot`（Interlocked/Volatile，不过滤），与既有 MRU 栈 + 5s switch pair 双视图并存；
  - `WindowService` 5 处 `_trackingService.PreviousWindowHandle` 读写点全部重指引擎槽；`GetPreviousWindow` 门面不变；
  - 删死代码：`IFocusHistory`（接口+FocusManager 显式实现+DI）、`IFocusManager.QuickSwitchAsync`、`QuickSwitchResult`、`IWindowFocusContextService.RecordPreviousWindow`、`WindowService.RecordPreviousWindow`、`WindowTrackingService.RegisterOrUpdateWindow`（+其单测重写为首见/激活语义）；
  - 测试：引擎 +6（槽位往返/双视图独立/分叉钉死/容量裁剪/多层回退），门面 +1 双轨钉死（不合格窗口→快照进、历史拒）；全量 **1257/1257 通过**，build 0 警告 0 错误。
- 文档：CONTEXT.md 增 **Window History** 术语；ADR-028 新建；Docs/README 决策表 +028 行。

### 关键决策·坑
- C3 关键 reframing：不是"三状态机"，是"一个活跃双轨 + 一个死代码子系统"（FocusManager.IFocusHistory 状态永不写入、QuickSwitchAsync 零调用方）。双轨非冗余——不过滤槽供 PulsarContext，过滤栈供快速切换。
- PowerShell 环境 `&&` 不可用（ParseException），del 多参数需 `Remove-Item -Path "a","b"`。
- TDD 反模式规避：未 horizontal slicing，按 vertical slice 逐个红→绿；删除类改动靠编译回归 + 全量测试验证。

### 相关引用
- ADR-028（本会话新建，关闭 ADR-010 follow-up）；ADR-004（栈语义不变）；CONTEXT.md Window History
- 源码：`QuickSwitchEngine.cs`（SetMenuSnapshot/GetMenuSnapshot）、`WindowService.cs`（5 处重指）、`FocusManager.cs`（IFocusHistory 移除）

## Session (14:30)

### 做了什么
- C3 提交与推送（用户「遗留项按建议处理，然后push」）：`git restore` 丢弃 `Docs/reports/2026-09-07-DOC_STRUCTURE_AUDIT.html` 未暂存噪声（147+/147- 纯 data-page-node-id）；commit `b3619ab`（17 文件 +282/−142）；push 遇瞬时 TLS 错误退避 5s 重试成功（`09a7f1d..b3619ab`）。工作树干净。
- 成本收益图示（用户「图示化」）：ECharts 散点（x=实施成本 y=架构收益 气泡=紧迫性）+ 明细表，推荐路线 S2 → C4 → C6+C8 → C1 → C5/C7 → C2。
- **低成本批次（用户「grill auto 完成低成本所有项目，剩余写 handoff」）**：低成本清单 = S2/S3/C7/C6/C8/S4。grill 决策树：Q1–Q5/Q7 auto-accept，**Q6（S4 删 DemandPermission 死链）护栏 1 命中暂停等用户**。已实施 5 项：
  - **S2** Hotkey hook 模式刷新：`RebuildCache()` 末尾补调 `ConfigureHookMode()`（修启动后改 ModifierStateMode 不生效）；+1 测试（ConfigUpdated 后 hook mode 跟随）。
  - **S3** wizard 分支 :432 改走 `_dispatcherProvider()` seam（与 tutorial 分支同构，消除 `Application.Current` 直接依赖）。
  - **C7** 插件本地化约定收口：`PluginLocalization.ConventionLookup(loc, prefix, value, keySource?)` 公开单点，SlotAction./SlotParam./Plugin.* 三处内联实现全部重指；+6 测试。
  - **C6** 对话框 DataTemplate 映射校验：`DialogService.HasTemplate`（internal static）+ ShowCustomAsync 对 IDialogViewModel 缺失模板抛 InvalidOperationException（替代静默 ToString）；集成测试反射枚举 17 VM ↔ DialogTemplates.xaml 一一映射；+2 测试。
  - **C8** usage tracker clock seam：构造加可选 `Func<DateTime> clock`，写侧三处（日键/未使用阈值/30 天清理）统一 `_clock()`，与读侧 UsageStatsReadModel 对齐；+3 测试。
  - 全量 **1269/1269 通过**（+12），build 0 警告 0 错误。**改动未提交**（待用户确认）。
- S4 删除范围已探明（接口+2 adapter+异常+上下文属性+kernel attach ≈75 行；`PluginPermissionEvaluator`/`IsKnown` 属真实权限门保留），Q6 待用户裁决。
- **剩余项目 handoff**（用户「剩余写入 handoff」）：`%TEMP%\pulsar-handoff-2026-09-08.md`——C1/C2/C4/C5 四候选 + S1 附注 + 覆盖缺口 + 推荐顺序 + suggested skills。

### 关键决策·坑
- grill auto-with-guardrails：护栏 1（外部契约/公共接口）命中即暂停（S4 删 `IPluginPermissionInterceptor` 属 public 插件 SDK）。
- 测试命名空间坑：`Pulsar.Tests.Core.*` 会遮蔽 `Core.*` 简写解析（`Core.Plugin.PluginTier` 报 CS0234）——沿用项目惯例 `Pulsar.Tests.<Domain>`（如 `Pulsar.Tests.Localization`）。
- `InputSettings.IsHybridMode` 是只读派生属性，测试须改 `ModifierStateMode` 值。
- C6 集成测试用 `Application.LoadComponent` 加载 `DialogTemplates.xaml`（UseWPF 测试项目可行，无 Application 实例亦可）。

### 相关引用
- handoff：`%TEMP%\pulsar-handoff-2026-09-08.md`；C3 提交 `b3619ab`；架构报告 `%TEMP%\architecture-review-2026-09-08-075853.html`
- 源码：`HotkeyService.cs`（RebuildCache）、`AppStartupCoordinator.cs`（:432 seam）、`PluginLocalization.cs`（ConventionLookup）、`DialogService.cs`（HasTemplate）、`PluginUsageTracker.cs`（_clock）

## Session (14:55)

### 做了什么
- **S4 落地**（用户 Q6 裁决「同意删除」）：删除 DemandPermission 死链——`PluginPermissions.cs` 的 `IPluginPermissionInterceptor` + `PluginPermissionDeniedException` + `GrantedPluginPermissionInterceptor` + `AllowAllPluginPermissionInterceptor`（-77 行）；`PluginExecutionContext` 的 `PermissionInterceptor` 属性/私有 ctor 参数/`BeginScope` 参数/`DemandPermission` 方法；`PluginRuntimeKernel:500-508` attach。`PluginPermissionService`/`PluginPermissionEvaluation`/`IsKnown` 真实 manifest 权限门保留。顺手修正 `TESTING_GUIDE.md` 两处过时示例（PermissionInterceptorTests 行 + 模式 4 改真实 `PluginPermissionService.Evaluate` API）。
- 全量 **1269/1269 通过**，build 0 警告 0 错误。
- **低成本批次 6 项全部完成并 commit+push**（用户「同意删除和push」）。

### 相关引用
- ADR-007（S4 原 Future Work 项，本批关闭）；`PluginPermissions.cs`（真实权限门保留）

## Session (11:51)

### 做了什么
- README 新增「🆚 与同类工具对比」章节（对照表：Quicker / Flow Launcher / PowerToys / Kando / StarPie / RadialActions / Power Automate Desktop / 影刀 RPA；一句话定位 + 「✨ 四个不可替代点」），README.md 与 README_EN.md 双语同步，顶部导航同步加锚点；原文裸 URL 改为可点击链接。改动未提交。

### 关键决策·坑
- GitHub 锚点规则（emoji 剥离、中文保留）：中文 `#-与同类工具对比`、英文 `#-vs-similar-tools`。

### 相关引用
- `README.md` / `README_EN.md`（未提交，随低成本批次或单独 commit）

## Session (12:05)

### 做了什么
- README「去 AI 味」重写，`README.md` + `README_EN.md` 双语同步（未提交）：
  - 删除全部装饰性 emoji（标题、顶部导航、列表项、表格；徽章与 img alt 保留）；
  - 删除「🌐 多语言支持」「📌 项目状态」独立小节（信息并入「上手」与「路线图」）；
  - 删除「✨ 四个不可替代点」（与功能段重复），其中真实信息下沉：插件 Core/Extension 分层 + 熔断器（1 分钟 3 次崩溃 → 停用 60 秒）并入开发者段；
  - 删营销腔：「我们尊重每一位同行」「贴心功能」「省心首选」「一气呵成」「开箱即用」「唯一」「一句话：」×3 的三段排比；
  - 破折号插入语改分句；引号统一「」；中英之间补空格；
  - 章节重排为：这是什么 / 快速开始 / 它能做什么 / 与同类工具对比 / 演示 / 开发者 / 社区与贡献 / 路线图 / 许可证。
  - 事实零改动：版本号、下载链接、体积、热键、DPAPI、图片路径、内置工具清单全部原样保留。

### 关键决策·坑
- GitHub 锚点：标题去掉 emoji 后，锚点里 emoji 占位产生的 `-` 前缀也消失（`## 💡 这是什么？` → `#-这是什么`，改为 `## 这是什么` → `#这是什么`）。**改标题必须同步改导航链接**，否则顶部目录全部失效。

### 相关引用
- `README.md` / `README_EN.md`（未提交）


## Session (12:28)

### 做了什么
- **清空工作树批次**：README 双语（对比章节 + 去 AI 味重写）+ journal/NEXT + `Design/icon-concepts/` 纳管（9 文件 2.3MB：dark/light ico 成品 + `build_ico_solid.py` 生成脚本 + 3 张对比图；仓库内无引用但属可复现设计资产）。
- **NEXT 裁决落账**：Demo 视频录制取消（用户：现有动图已足够）；OpenWiki 补 3 页取消（用户：太贵、效果不好）。
- 启动剩余候选 C1 → C4 → C5 → C2（grill auto-with-guardrails + TDD，handoff `%TEMP%\pulsar-handoff-2026-09-08.md`）。

### 相关引用
- handoff：`%TEMP%\pulsar-handoff-2026-09-08.md`；NEXT.md 取消项划线留痕

## Session (13:00)

### 做了什么
- **工作树清空两连提交**：`2d58a46`（README 双语去 AI 味 + Design/icon-concepts 纳管 + NEXT 取消项）、`3f15d07`（并行会话的主题感知托盘图标——Assets/Brand 迁移 Assets/Icons、TrayIconService 双 ico 逻辑；提交前 build 0 警告 + 全量 1269/1269 验证）。
- **C1 落地** `19c7f43`：MenuSession 4 处快速切换路径（:797/:1666/:1721/:1754）手写 mark→switch→notify 收束进 private `ExecuteQuickSwitchAsync()`；IsVisible=false 时序留在调用点（两处 D4 提前隐藏）。ProcessGroupStrategy:281 / BackActionStrategy:355 直写 IsVisible **保留**（无目标/取消不是动作执行，mark 会破坏 E2E ActionExecuted 语义）。无新增测试（快速切换路径依赖 BeginSessionAsync surface 流程，_showVisibleTime 仅 :778 赋值，测试可达成本高；由既有 BeginExecution 钉死测试 + 编译回归保护）。1269/1269。
- **C4 落地** `091f575`：TDD 红→绿。`PluginExecutionPipeline.AcquireExecutionGateAsync(pluginId, timeout)`（持有期间新执行走既有 WaitAsync(0)→Blocked，锁对象不删避免竞态）+ kernel ctor 注入 `executionDrainTimeout`（默认 ExecutionTimeout+5s）+ `DeactivatePluginAsync` 全 teardown 包进 gate，drain 超时抛 InvalidOperationException fail-close（ExternalPluginLifecycleOps 的 DeactivateFailed 分支自动中止删文件）。+5 并发交叉测试（gate 阻断新执行 / 等 in-flight / 超时 null / deactivate 等 in-flight / 超时抛且无 teardown）。1274/1274。
- **C5 落地** `c4f20ce`：护栏 1 暂停 → 用户选方案 D（internal 收口，非破坏）。`ImplementationType { get; set; }` → `{ get; internal set; }`（Pulsar.Tests 有 InternalsVisibleTo 可构造；外部 SDK 只读）。CS9032 坑：required 成员 setter 可见性不得低于类型 → 去掉 required（ActivatePlugin 缺 Type 仍 throw）。1274/1274。
- **C2 落地**（本 session 主任务）：TrayIconService 390 行 7 职责拆分——
  - `IAutoStartService`/`AutoStartRegistryService`：HKCU Run 键读写独立，键路径/appName 可注入（测试专用子键自清理），OpenSubKey null 时 CreateSubKey 兜底；
  - `TrayMenuBuilder`：菜单构建纯函数化（TrayMenuRequest = 本地化+状态快照+动作回调 → ContextMenu），5 个 AutomationId 结构钉死测试（STA 线程，StaTestRunner 惯例）；
  - `TrayIconService` 编排化：删 IServiceProvider 服务定位，注入 `Func<SettingsWindow>`（复用 App.xaml.cs:365 tutorial 既有注册，避免重复注册同一服务）；用户今日主题图标逻辑原样保留；
  - ITrayService 接口 + 12 处消费方零改动。+5 测试。

### 关键决策·坑
- CS9032：required 成员的 setter 可见性不能低于包含类型——`required` 与 `{ get; internal set; }` 不共存，去 required 换 internal set。
- grilling C5 护栏 1（public SDK 契约）命中 → AskUserQuestion 三方案（D internal 收口 / A EntryPoint string 彻底重构 / E 跳过），用户选 D。
- App.xaml.cs 已有 `Func<SettingsWindow>` 注册（tutorial 用），新代码直接复用；重复注册同类型虽不炸（后注册生效）但是脏的。
- `SemaphoreSlim.WaitAsync(timeout)` 在 in-flight 挂死（不响应取消）的 plugin 上是唯一可靠 drain 上限——pipeline 的 CancelAfter(ExecutionTimeout) 依赖 plugin 配合取消。
- xUnit1031：测试里 `.Result` 阻塞 → 改 await（项目 0 警告基线）。

### 相关引用
- C1 `19c7f43` / C4 `091f575` / C5 `c4f20ce` / C2（见 NEXT）；ADR-028（C3 已存）、CONTEXT.md Execution Handoff 术语不变（BeginExecution 语义未被 C1 改动）
- 源码：`MenuSession.cs`（ExecuteQuickSwitchAsync）、`PluginExecutionPipeline`（AcquireExecutionGateAsync）、`PluginRuntimeKernel`（drain）、`PluginDescriptor.cs`（internal set）、`AutoStartRegistryService.cs` / `TrayMenuBuilder.cs` / `TrayIconService.cs`（编排化）

## Session (14:00)

### 做了什么
- push main（`b5c0ed4..0418637`）。发布 v1.12.0（用户确认版本与 notes 草稿）：
  - **pwsh 未安装**（publish 脚本 PS7-only，PS 5.1 下 `Cannot run a document in the middle of a pipeline`），改手动执行发布流程：csproj 1.12.0 + CHANGELOG 补全今天条目并固化 `[1.12.0] - 2026-09-08`；
  - 本地构建 full（75MB 自包含单文件 ✅）/ portable（11MB ✅）；打包 3 zip（full/portable/**Standalone** = full 自包含包）+ SHA256SUMS，System32 bsdtar 打包 PK 魔数校验通过；
  - annotated tag v1.12.0（`-c core.commentChar=§` 防 `###` 被剥离；notes 首三字节 23 23 23 无 BOM 核验）；push main + tag 触发 release.yml；CI 由匿名 GitHub API 轮询（gh 在本 harness 未登录）。
- SKILL.md 修正：第 2 节产物断言 cor3 → exe 体积（脚本 `d346b3f` 已改，文档未同步的漂移）。
- **本机产物**（用户拷去新机验证两个遗留项）：`E:\8_Project\10_C#\Pulsar_Project\Artifacts\Pulsar-v1.12.0-Standalone-win-x64.zip`（72.4MB，SHA256 `d58019c3…`）。

### 关键决策·坑
- PowerShell 工具本会话输出流损坏（Write-Output 无回显）+ gh 未认证 + pwsh 缺失 → 发布链路走 bash + 匿名 API 的手动执行。gh auth 需要用户在常规终端登录后 Watch-Release 才能复用。
- git tag -F 不认 Git Bash 的 `/c/` 路径，需 `C:/` 形式。

### 相关引用
- tag v1.12.0（`0418637`）；notes `C:\Users\milo\AppData\Local\Temp\pulsar-notes-1.12.0.md`；产物 `Artifacts/`；CI run（release.yml, v1.12.0）

## Session (14:20)

### 做了什么
- **v1.12.0 发布收口**：第一轮 Release CI 失败（Publish Portable NETSDK1176——本地复现：csproj RuntimeIdentifier 条件属性开单文件压缩，framework-dependent 不支持；本地 skill 显式传 `-p:EnableCompressionInSingleFile=false` 而 CI workflow 漏了）。修复 `release.yml`（portable 加参数 + Verify Full 的 cor3 断言改 exe ≥50MB）commit `f8d1328`；tag 移至修复 commit 重打重推；第二轮 run `34193461175` **success**（4 分钟）。
- GitHub Release v1.12.0 验证通过：非 draft，5 资产齐全（full 72.6MB / portable 5.2MB / Setup.exe 72.5MB / Standalone 72.1MB / SHA256SUMS），中文 notes 经文件导出核对无 mojibake。

### 关键决策·坑
- **push workflow 文件被拒**（OAuth credential 缺 `workflow` scope）→ SSH 通道可用（`git push git@github.com:Smith-Rosco/Pulsar.git`），非 workflow 文件走 https、workflow 文件走 SSH。
- 发布 release.yml 以 **tag 指向 commit** 的 workflow 定义执行——修 workflow 后必须把 tag 移到含修复的 commit 再触发。
- 本 harness 内 gh 未认证、pwsh 未安装、PowerShell 工具输出流损坏——发布链路全程 bash + 匿名 GitHub API + 写文件回读。

### 相关引用
- Release: https://github.com/Smith-Rosco/Pulsar/releases/tag/v1.12.0；tag commit `f8d1328`；run `34193461175`

## Session (14:35)

### 做了什么
- **发布复核（用户「再检查下」）**：Release 资产 5 件齐全非 draft；CI SHA256SUMS 下载核对（Standalone `f3792f17…`、Setup `d9d9909c…`）；本地三包 SHA 稳定复现 + Python zipfile CRC 全过 + 关键文件齐全（exe/pdb/build-info/20 Assets）；tag/csproj/CHANGELOG 三者一致（v1.12.0）；Release run 与 main CI 全绿。
- **发现并修复 CI Standalone 缺陷**：release.yml 的 Build Standalone 步骤只拷 `Pulsar.exe`，GitHub Release 上的 Standalone 自 v1.11.0 起一直缺 `Assets/`（Presets/Scripts/TutorialSteps 等 20 个运行时文件）。改为 `Compress-Archive publish\full\*` 完整打包（与 skill 本地产物对齐），随下个 tag 生效。
- **origin/main tracking ref 再次被外部进程回写**（fetch 更新到 650728b 后又被回写成 2101f1e，`status -sb` 虚报 ahead 160）；`git ls-remote`（SSH）核实远端真实 main = `650728b` = 本地 HEAD，**实际完全同步**。远端新增 `openwiki/update` 分支。

### 关键决策·坑
- 本机到 GitHub 的 TLS 连接不稳定（release 资产下载、ls-remote 多次 `unexpected eof`），api.github.com JSON 查询稳定——大流量下载用 API asset 端点 + 多次重试；ref 核实用 SSH ls-remote。
- bsdtar 对含 `#` 的 zip 路径行为怪（-tf 打不开但 python zipfile 正常）——验证 zip 用 Python zipfile testzip 为准。

### 相关引用
- workflow 修复（Standalone 完整打包）待随下个 tag 生效；本发布不受影响（用户验证用本地产物）

## Session (14:40)

### 做了什么
- **两个遗留项真机验证通过（用户新机器）**：① installer 1.3 干净 VM 冒烟——v1.12.0 Standalone zip（`b5c0ed4` 本地包，含 Assets）无 .NET 环境解压运行通过；② 窗口切换真机回归（C2 收口 P1 项 3）——随冒烟一并验证 OK。NEXT 两项划线收口，NEXT 上需外部环境的待办清零。

### 相关引用
- NEXT.md 划线两条；v1.12.0 tag `f8d1328`







## Session (15:10) — 设置页三处布局/配色缺陷（用户报告 → diagnosing-bugs skill）

### 报告
1. 使用分析页「使用频次前五」列表：#1~#3 的序号消失，只有 #4 #5 可见。
2. 折叠侧边栏后，每个 Tab 按钮坍缩为仅图标，但按钮被上下拉伸，文本越多拉伸越大。
3. 关于页卡片宽度没占满视图，折叠/展开任意卡片会导致所有卡片宽度变化，内容越多变化越明显。

### 做了什么
- **#1 已确认根因并修复**：`SettingsAnalyticsPage.xaml` 排名徽章 Top-3 铺 `SystemFillColorAccentBrush`，文字却硬编码 `Value="White"`。深色主题 accent = `#4CC2FF`（浅蓝），白字对比度 ≈1.7:1 → 不可见；#4/#5 是灰底+次级文字所以还在。改 `TextOnAccentFillColorPrimaryBrush`（Light=白 / Dark=黑，随填充亮度翻转，对 accent 与 critical 填充都适用）。**同类硬编码再修 3 处**：`DialogHostWindow.xaml`（危险按钮 / `SystemFillColorCriticalBrush`）、`AddSlotContent.xaml`、`SlotConfigurationDialogContent.xaml`（分段控件选中态 / `AccentFillColorSecondaryBrush`）——这 3 处在深色主题下同样是"看不见"，属同一缺陷族。
- **#3 已确认根因并修复**：`SettingsAboutPage` / `SettingsAnalyticsPage` 的 `ScrollViewer` 内容 `StackPanel` 写成 `MaxWidth + HorizontalAlignment="Left"` → 面板宽度 = 最宽子项所需宽度（内容驱动），于是①卡片占不满，②展开/折叠改变了最宽项就带动所有卡片一起变宽。改 `Stretch`（`MaxWidth` 退化为阅读宽度上限）。`SettingsPluginsPage` 早就有 `Width="{Binding ViewportWidth}"` 的同类绕法，可互为印证。
- **#2 做了缓解，机制未完全确认**：读 Wpf.Ui 4.3.0 源码确认 `NavigationView.Base.cs:459` 把 `ItemTemplate`（`LeftCompactNavigationViewItemTemplate`）直接赋给每个 `NavigationViewItem.Template`；该模板行高只有 `MinHeight=40`，标题列是 `Width="*"`，折叠后标题列宽度为 0、行高由内容测量决定。据此做了两道保险：①标题用显式 `ContentTemplate`（`NoWrap` + `CharacterEllipsis`），行高恒为单行；②`item.MaxHeight = 40` 钉死行高。**但没能在本机复现/测量**：无头环境下 `Window.Show()` 的 `ActualWidth` 恒为 0，VSM 的 `PaneGrid.Width` 折叠动画（250→40）不触发，STA 探针（独立可视树 / Frame / 真窗口 + DispatcherFrame 泵）全部拿不到真实尺寸。此项需用户真机目视确认。

### 关键决策·坑
- **无头环境无渲染 seam**：`StaTestRunner` + 独立可视树能渲染单控件（需 `ThemesDictionary` **和** `ControlsDictionary` 都合并——只合并前者时 Wpf.Ui 控件不套模板，量出来全是 0），但 Page 需要真实 HWND、Wpf.Ui 的 VSM 动画需要消息泵，二者在测试主机里都拿不到。故布局回归改为**静态 XAML 扫描**守卫。
- **守卫正则差点假绿**：`HorizontalAlignment="Left"` 与 `MaxWidth` 的声明顺序是 `MaxWidth` 在前，单向正则匹配不到 → 用「先抓整个起始标签、再在属性串里分别匹配」的顺序无关写法。已用 `git show HEAD:` 验证两条守卫在修复前命中、修复后 0 命中（red-capable）。
- **改配色打翻了一个旧测试**：`ButtonThemeContrastTests.SegmentedRadioButton_CheckedAfterFirstLayout_UpdatesTextColor` 断言 `R > 180`，前提是该值是字面量 `White`；改成 `DynamicResource` 后未合并主题的独立树解析不到 → 失败。是测试前提过时，不是修复有问题：给 `RenderIsolated` 合并 `BuildLightTheme()`，测试反而更真实。

### 验证
- `dotnet build Pulsar/Pulsar.sln`：0 错误 0 警告。
- `dotnet test Pulsar/Pulsar.sln`：**1281 通过 / 0 失败**（新增 2 条守卫）。

### 相关引用
- 新增 `Docs/lessons/WPF_SETTINGS_PANEL_WIDTH_CONTENT_DRIVEN.md`；`Docs/lessons/WPF_FLUENT_ACCENT_TOKENS_UNRESOLVED.md` 追加 v1.1.0 审计段（append-only）。
- 新增 `Pulsar/Pulsar.Tests/UI/SettingsLayoutGuardTests.cs`。
- 待办：**#2 需真机目视确认**（折叠侧栏后 5 个 Tab 高度是否一致、是否仍为 40）。
## Session (15:50) — 追加：Top-3 序号仍看不清（根因二段式）+ 关于页首卡居左

### 反馈
用户确认 #2（侧栏 Tab 拉伸）与 #3（卡片宽度跳变）已修复；但序号仍看不清；另提新需求：关于页第一张"应用标识"卡片内容居中，改居左。

### 根因（这次是二段式）
- 用 `TryFindResource` 探针实测两主题的页面资源链：**`SystemFillColorAccentBrush` 在 WPF-UI 4.3.0 里根本不存在**（Light/Dark 都是 `<null>`），`SystemFillColorAccentBackground3` 同样不存在。DynamicResource 缺键**静默失败**→ 徽章从来没铺上 accent 底色，白字直接压在浅色卡片上；我上一轮只改了文字色（Light 下 White→TextOnAccent=White），等于没改。
- 运行时 accent 管理器（`ApplicationAccentColorManager` → `ThemeService.BridgeAccentResources`）注入的配对是 `AccentFillColorDefaultBrush`（随主题/系统色派生）+ `TextOnAccentFillColorPrimaryBrush`（按填充亮度算出），与 `PulsarPrimaryButtonStyle`、`SlotOrb` 徽章完全同源——这就是项目里被生产验证过的正确配对。

### 修复（共 9 处死令牌迁出）
- `SettingsAnalyticsPage.xaml`：Top-3 徽章底色、3 条柱状图填充 → `AccentFillColorDefaultBrush`；3 处 accent 色文字/图标 → `AccentTextFillColorPrimaryBrush`。
- `PluginSettingsDialogContent.xaml`：选中态 ToggleButton 的 `SystemFillColorAccentBackground3` → `AccentFillColorDefaultBrush` + `TextOnAccentFillColorPrimaryBrush`（v1.1.0 审计当时判断"保留 AccentText 是对的"是基于底色令牌存在的前提，该前提不成立，已纠正）。
- `SettingsAboutPage.xaml`：首卡 `CardControl` 加 `HorizontalContentAlignment="Stretch"`（默认内容收缩居中，与其余卡片不一致）。
- 注意：`AccentFillColorDefaultBrush` 在 Light 的静态字典里也是 null，靠运行时桥接取值——所以主按钮在 Light 下正常就是证据。

### 守卫
- `SettingsLayoutGuardTests` 新增第 3 条：禁止引用 `SystemFillColorAccent*`（4.3.0 不存在的令牌）。**首跑就红**，抓出 PluginSettingsDialogContent 的漏网 + 我自己在注释里写令牌名导致的误报 → 守卫改为剥离 XML 注释后再匹配。
- 守卫正则坑：`SelectMany(file => StripComments(...))` 把 string 当 IEnumerable<char>，编译错——改显式 foreach。

### 验证
- `dotnet test Pulsar/Pulsar.sln`：**1282 通过 / 0 失败**（3 条守卫）。
- 待用户真机确认：Top-3 徽章是否可见、三条柱状图是否有填充、关于页首卡是否居左、插件设置弹窗选中 tag 是否有 accent 底。

### 相关引用
- `Docs/lessons/WPF_FLUENT_ACCENT_TOKENS_UNRESOLVED.md` v1.2.0（附两主题令牌实测表）；CHANGELOG [Unreleased] 已更新。

## Session (16:20) — 追加：Top-3 序号真机仍不可见（第三段根因：元素本地字典遮蔽）+ 关于页首卡居左（CardControl Header 槽位）

### 反馈
用户确认柱子已显示、tag 修复生效；但 Top-3 序号真机仍不可见，关于页首卡仍居右。tag 疑问已当面澄清（"核心"黄底 tag = PluginSettingsDialogContent 选中态 ToggleButton）。

### 第三段根因（序号）
- 上一轮把令牌迁到 `AccentFillColorDefaultBrush` 后所有探针都过、真机仍黑——因为探针走的是 Application 级查找，真机走的是**元素本地**查找：`ApplyStandardTheme` 给每个 Page/Window 合并了元素本地 `ThemesDictionary`/`ControlsDictionary`，WPF 解析顺序元素本地优先于 Application；Light 静态字典里**没有** `AccentFillColorDefaultBrush` → 元素上解析为空。
- 附带陷阱：`TextOnAccentFillColorPrimaryBrush` 虽然静态存在（Light=白），但运行时管理器会按实际 accent 亮度重算；静态值在非默认 accent（如黄）下就是白字黄底。

### 修复
- `ThemeService.ApplyStandardTheme` 末尾新增 `CopyRuntimeAccentResources(element.Resources)`：把 `UiApplication.Current.Resources` 里含 "Accent"/"SystemAccent" 的键复制到元素本地资源顶部。唯一性坑：方法引用实例字段 `_logger`，最初写成 `static` → CS0120，去掉 static 即可。
- 关于页首卡：读 Wpf.Ui `CardControl` 源码确认模板三列 `Auto|*|Auto`，`Content` 落最右 Auto 列（右对齐收缩）；`HorizontalContentAlignment="Stretch"` 救不了（列本身是 Auto）。把图标+文字移入 `<ui:CardControl.Header>` 槽位（中间 `*` 列）→ 居左占满。
- 新增回归测试 `ThemeServiceTests.ApplyTheme_ShouldCopyRuntimeAccentResourcesOntoElement`（元素级断言，非 Application 级）。

### 验证
- 构建一次失败：①CS0120（static 方法碰 `_logger`）；②Pulsar.exe（PID 11000）锁文件 MSB3027——结束开发实例后重建。
- `dotnet build Pulsar/Pulsar.sln`：0 错误 0 警告。
- `dotnet test Pulsar.Tests`：**1283 通过 / 0 失败**。

### 文档
- `Docs/lessons/WPF_FLUENT_ACCENT_TOKENS_UNRESOLVED.md` v1.3.0（遮蔽根因 + CardControl 三列模板坑）；CHANGELOG [Unreleased] 对应条目改写为三段根因。
- 待用户真机确认：Top-3 序号、关于页首卡居左。

## Session (16:35) — 追加：序号第 4 段根因 = 本地属性压制 Style 触发器；"小条" = 迷你趋势图

### 反馈
用户确认关于页首卡居左 ✅；Top-3 序号仍不可见；分析页卡片下出现不明"小条"。

### 第 4 段根因（序号，最终）
- 徽章 Border 同时写了**本地** `Background="{DynamicResource ControlFillColorDisabledBrush}"` 和 `<Border.Style>` 里的 `IsTopThree` 触发器。WPF 优先级：**本地值 > Style 触发器** → accent Setter 从未生效，徽章恒为浅灰底；而 TextBlock 的文字触发器无本地属性干扰、正常生效 → 白字压浅灰底。#4/#5 默认深灰文字有对比度所以一直正常。
- 修复：删除本地 Background 属性，默认底色保留为 Style Setter（`SettingsAnalyticsPage.xaml` 排名徽章处，附注释）。全 Views 扫描（本地 Background/Foreground 属性紧邻 `.Style` 元素且触发器同属性）→ 无其它实例。
- **教训**：三轮探针全在验证"令牌能否解析"，没验证"有效值优先级"；"触发器不生效"与"令牌解析不到"外部表现相同。lesson v1.4.0。

### "小条"澄清
- 卡片底排 4px 宽小竖条 = `TrendData` 迷你趋势图（sparkline，近期使用趋势），一直都在；此前 accent 填充因死令牌不可见、本轮修复后更加显眼。非 bug；如用户觉得干扰可再隐藏。

### 验证
- `dotnet build`：0 错误 0 警告；`dotnet test`：**1283 / 0 失败**。

### 文档
- lesson v1.4.0（DP 优先级根因 + 扫描方法）；CHANGELOG 条目改为四段根因。
- 待用户真机确认：Top-3 序号（accent 底徽章 + OnAccent 白字）。

## Session (16:47) — 设置窗口保存按钮溯源 + 槽位轮盘滚轮翻页 + 中心 Slot 文本底衬删除

### 任务 1：保存按钮位置变更溯源（仅调查，待用户决策）
- 现状：保存按钮在 `SettingsWindow.xaml` 的 `TitleBar.TrailingContent`（右上角）。
- 变更来源：commit `c5c3592`（2026-09-01，feat(ux): Fluent-aligned UX refactor per design proposal P0-P3），把按钮从 `NavigationView.FooterMenuItems`（侧边栏左下角页脚）上移到 TitleBar。
- 动机：`Docs/planning/UX_REFACTOR_PROPOSAL.md` 的 **[P1]「保存操作被藏在导航页脚」**——评审判定页脚是"非常规位置"、First-Timer 找不到、且当时未保存提示仅 8px 红点。修复建议"提到内容区底部操作栏或 TitleBar 右侧"，实现选了 TitleBar。
- 后续 `ee3ecc3`（9-03）将文字徽章简化为小红点 badge；`0b54bcc`/`3b9155b` 加图标+文字。
- **结论：这是有提案文档背书的有意设计决策，不是意外回归。** 是否回退到侧边栏底部（FooterMenuItems）由用户决定。

### 任务 2：槽位设置页轮盘滚轮翻页（已实施）
- `SlotWheelEditor.xaml`：`WheelHost` 挂 `PreviewMouseWheel="WheelHost_PreviewMouseWheel"`。
- `SlotWheelEditor.xaml.cs`：新增 handler——拖拽中不响应；`TotalPages <= 1` 不拦截；向上滚=上一页、向下滚=下一页（`Vm.GoToPage`，内部 clamp）；标记 `e.Handled` 阻止外层滚动。

### 任务 3：径向菜单中心 Slot 文本白色背景（已实施）
- 根因：`SlotOrb.xaml` 文本标签 backplate Border 用 `Theme.Orb.Label.Background`（浅色主题 `#E6FFFFFF`，近纯白）。
- 修复：`SlotOrb` 新增 `ShowLabelBackplate` DP（默认 true，保持环上文本 Slot 可读性）；Border Style 加 DataTrigger False→Transparent；`RadialMenuWindow.xaml` 中心 SlotOrb 设 `ShowLabelBackplate="False"`。仅中心关闭，环上文本 Slot 底衬保留。

### 验证
- `dotnet build Pulsar/Pulsar/Pulsar.csproj`：0 错误 0 警告。
- `dotnet test --filter SlotWheelEditor|SlotOrb`：25 通过 / 0 失败。
- 待用户真机确认：滚轮翻页手感、中心标签可读性（浅色主题深色文字直出）。

### 任务 1 决策落地（16:55）
- 用户选择**回退到侧边栏底部**：`SettingsWindow.xaml` 移除 `TitleBar.TrailingContent` 保存按钮，恢复 `NavigationView.FooterMenuItems`（NavigationViewItem + Save24 图标 + InfoBadge 未保存红点）。
- 保留后续迭代成果：AutomationId `Pulsar.Settings.SaveChangesButton`（E2E/a11y 依赖）；红点颜色由硬编码 `#E74856` 改为 `SystemFillColorCautionBrush`。
- 回退后 `dotnet build`：0 错误 0 警告。未 commit，待用户真机确认三处改动。

### 滚轮命中范围修正（16:59）
- 用户反馈：滚轮翻页只在圆环处生效，中心空白区不触发。根因：WPF 中 `Background=null` 的空白区不参与命中测试，滚轮事件无法路由到 WheelHost——handler 挂载点没问题，缺的是命中填充。
- 修复：`SlotWheelEditor.xaml` 在导引环下加同尺寸 `Fill="Transparent"` 命中填充椭圆（Transparent 可命中、null 不可），滚轮有效范围扩展为整个圆形（含中心）。
- `dotnet build`：0 错误 0 警告。

## Session (17:10–18:40) 动态标签页：调研 → grill → openspec 提案 → 实施

### 产出链
1. **调研报告**：`Docs/planning/2026-09-08-dynamic-settings-tabs-research.md`（VS Code preview-tab / Raycast per-extension tabs / settings-registry 共识 + Pulsar 现状 8 项事实 + P0–P3 路线）。
2. **Grill**：interactive 第 1 轮 Q1–Q8 → 用户"全部按推荐来"转 auto-with-guardrails → Q9–Q17 自动收敛 → 审计通过。核心裁决：按类型单例、干净页导航离开即回收、脏页不阻塞跳转（关窗统一提示）、不做钉住、窗口级统一保存、外观=常驻独立页、P0+P1 同期。
3. **Openspec change** `2026-09-08-dynamic-settings-tabs`：proposal + specs×3（新增 settings-transient-pages；MODIFIED settings-shell-navigation、settings-dirty-state-guard——**导航提示仅限常驻页来源**，显式 delta 非静默覆盖）+ design D1–D11 + tasks 7 组。validate 通过。
4. **实施（/opsx-apply）**：
   - 基座：`SettingsPageRegistration.IsTransient`；catalog 可变列表 + `RegisterTransient`（语义分组末尾插入）/`UnregisterTransient`/`IsTransient` + 注册/注销事件；`SettingsPageFactory` 字典化 + `RegisterCreator`；`ITransientPageService` + `SettingsTransientPageService`（组合根登记 gesture 定义）。
   - Shell：`NavigateAsync` 脏守卫分流（临时页来源跳过提示）+ 临时页不写 LastOpened 偏好；`SettingsWindow` 注入服务、订阅 catalog 事件、`RebuildNavigationPreservingSelection` 统一重建（语言切换共用）、临时项模板（斜体 + hover 关闭钮 + AutomationId `Pulsar.Settings.NavCloseTab`）、`NavigateToCurrentShellPage` 挂 `NotifyNavigatedAwayFrom` 回收钩子、OnClosed 注销全部临时页（会话级）。
   - 页面：新建 `SettingsGesturePage`（手势配置整体迁入，绑定路径逐控件一致，`ProfileSettings` 不动）+ `SettingsAppearancePage`（常驻；预设色块网格，色值移植 RadialThemePresetCatalog，语言留 General）；General 页两卡瘦身（总开关/语言 + 配置详情入口）。
   - 本地化：9 个新键（`SettingsPage.*`、`SettingsWindow.CloseTab`）双语补齐。
   - 测试：新增 `SettingsTransientPagesTests` 20 例（单例/回收/关闭确认/守卫分流/LastOpened）+ 目录测试更新（System 组含 Appearance）。
   - 文档：ADR-029（注意：ADR 已编到 028，tasks.md 初稿误写 022 已更正）、DIALOG_SYSTEM.md + AGENTS.md 新范式（重配置=临时页，模态只留确认/选择器）、CHANGELOG。

### 验证
- 构建 0 错误 0 警告；全量测试 **1303/1303**（基线 1283 + 新增 20）。`openspec validate` 通过。
- 过程教训：Moq 宽松 mock 对 `Task<bool>` 未配置方法返回 false（守卫桩必须显式 ReturnsAsync(true)，否则导航被拦）；WPF-UI `SymbolRegular` 枚举名是 `Dialpad24`（XAML 字符串大小写不敏感、C# 不行）。

### 待办（本 change 收尾）
- GUI 手动冒烟：打开/回收临时页、指示条位置、中英切换、20 次开回收内存（tasks 2.1/2.2/4.3/4.4/5.1/5.2/6.2 的剩余验证项）。
- 7.3：`/opsx-archive` + 本地 commit——**规则文件（DIALOG_SYSTEM.md / AGENTS.md）变更按约定暂停，待用户确认后提交**；push 由用户执行。

### 冒烟修复（18:48，用户真机反馈 4 项）
1. **关闭钮遮挡/不在最右**：NavigationViewItem 内容默认 `HorizontalContentAlignment=Left`，模板 Grid 收缩到文本宽度。修复：临时项设 `Stretch`，模板改双列（文本 `*` 截断、按钮 `Auto` 贴右缘）。
2. **自动回收后指示器停原地**：重建后立刻测量，新条目 bounds 无效 → 动画/定位提前 return。修复：`RebuildNavigationPreservingSelection` 补 `RootNavigation.UpdateLayout()`；`InitializeNavIndicator` bounds 无效时 Loaded 优先级重试一次；`AnimateNavIndicatorAsync` 两条 early-return 改为兜底 `RepositionNavIndicator()`。
3. **语言设置**：移入外观页主题卡（用户决策，覆盖审计 Q15）；General 外观卡只剩入口按钮。
4. **外观页改动无法保存**：预设 ListBox `SelectedValue="{Binding ThemePreset, RelativeSource=Page}"` 源是 Page 对象自身（无此属性）→ 静默失败。VM setter 本有 MarkDirty；全部绑定统一改 `DataContext.*` 路径。
- 回归：构建 0/0，全量 1303/1303。文档同步：CHANGELOG / design.md D8 / tasks.md。

### 冒烟修复第 2 轮（19:20–19:35，用户复测反馈 2 项）

用户复测：第 1 轮 ①③④ 确认修复；遗留 ②指示器关闭标签后仍不动 + 新症状"外观页主体列表全空白"。已核实用户实测的是 18:55:47 构建（含第 1 轮全部修复），两症状均为现行代码真实缺陷。

1. **外观页预设列表全空白（根因锁定）**：第 1 轮修复④把绑定"统一"加 `DataContext.*` 前缀时误伤了 Page 自有属性——`SettingsViewModel.General.cs:97` 存在同名 `ThemePresetOptions`（`IReadOnlyList<string>`，旧 UI 遗留），ListBox 实际绑到字符串集合；ItemTemplate 的 `{Binding Swatch}`/`{Binding DisplayName}` 在 string 上解析失败 → 每个色块渲染为空 → "列表全空白"（resx 键齐全，排除本地化原因）。修复：`SettingsAppearancePage.xaml` 的 ItemsSource 改绑 Page 自身属性（去掉 `DataContext.` 前缀），XAML 注释记录同名属性陷阱。
2. **指示器仍不跟随（换策略：自愈式重定位）**：对照 WPF-UI 4.3 源码（NavigationViewItem.Activate/Deactivate 走 `SetCurrentValue`、IsActive 为普通 DP）逐路径推演仍"应该能动"，说明此前"一次测量即可用"的假设在 WPF-UI 内部 ListView 容器延迟生成/动画 HoldEnd 覆盖基础值等时序下不成立。放弃继续猜时序，改为收敛式设计：`RootNavigation.LayoutUpdated` 钩子（非动画期）在每次布局稳定后校准指示器——`CurrentPageId` 优先解析活动条目（单一事实来源，重建后依然成立）、写值前摘除 Top/Height 残留 HoldEnd 动画（否则 `Canvas.SetTop` 被活动动画覆盖，即"停在原地"）、定位去重（|Δ|<0.5）终止回环；`AnimateNavIndicatorAsync` finally 先落基础值（clear=false 保住 snap 收尾）再解除动画标志。删除 `_indicatorRetryQueued` 重试机制（LayoutUpdated 自愈取代）。

- 回归：构建 **0 错误 0 警告**，全量 **1303/1303**。CHANGELOG 已补"冒烟修复（第 2 轮，2 项）"。
- 教训（新增一条到流程层）：**同名属性 + RelativeSource 绑定是静默失败高发区**——第 1 轮的修复④（DataContext.* 统一化）本身就制造了第 2 轮的缺陷⑥；WPF 绑定错误无日志，必须核对绑定源类型而非只看路径拼写。
- 待用户 GUI 冒烟确认后 → 7.3 `/opsx-archive` + 本地 commit（规则文件变更暂停等确认）。

### Commit（19:40）
- `538e727` feat(settings): 动态标签页（临时页）机制——32 文件，+2104/-267；工作树清零。规则文件（AGENTS.md / DIALOG_SYSTEM.md）变更经用户"commit"指令确认后一并入库。push 由用户执行。
- 遗留：change 目录尚未 `/opsx-archive`（spec 落地到主 specs 后可归档）；GUI 冒烟第 2 轮修复项待用户复测确认。

### /opsx-archive（20:00，用户实测确认后归档）
- 用户复测第 2 轮修复项通过（"实测OK"）。按 opsx-archive 工作流执行：
  1. status：spec-driven schema，3 delta spec 全部在位；tasks 8 项未勾——其中 7 项（2.1/2.2/4.3/4.4/5.1/5.2/6.2）均已实施且经两轮实测确认，据实勾选；7.3 随归档完成勾选。
  2. delta 同步（Sync now，推荐路径，`openspec instructions specs` 快照 exit 0）：
     - 新建主 spec `openspec/specs/settings-transient-pages/spec.md`（Purpose + 5 需求转正）；
     - `settings-shell-navigation`：Centralized Registration 替换（+运行时注册/移除场景）+ 追加 2 需求（Sidebar 动态增删、语言重建保留临时项）；
     - `settings-dirty-state-guard`：Unsaved Changes Guard 替换（导航提示仅限常驻页来源 + 关闭脏临时页仍提示）。
  3. 校验：`openspec validate --specs` 94/94 通过；change valid。归档移动 → `openspec/changes/archive/2026-09-08-dynamic-settings-tabs/`（无日期堆叠冲突）。
- 归档 commit：见本条 git log（delta 同步 + 归档移动 rename，11 文件 +130/-11）。
