# 2026-09-10

> 本日前序会话（07:4x–12:3x，resx 治理 / ADR-030 / 两个 tab 修复 / 技能沉淀 / 文档债务治理 / 发布）已轮转至 [archive/2026-09-10.md](./archive/2026-09-10.md)（ADR-021，文件超 15KB 上限）。

---

## Session (12:5x · Agent 工作流消融实验 → 按结论落地)

**做了什么**
- 用户给 8 条思维要点（第一性原理/对抗审查/消融实验/奥卡姆/不自信点/独立思考/批判性/高内聚低耦合），要求融入 agent coding 工作流。先出消融报告（非侵入、只读）：`artifacts/agent-workflow-ablation-20260910.html`。
- 按报告优先级落地 4 项：P0 journal 轮转 · P1 grill 存根（**撤回**）· P2 AGENTS.md 路径矛盾 · 新增 2 个交付前挂载点。

**关键决策 / 坑**
- **P0 轮转（ADR-021）**：6 个文件静态超限，逐日核实轮转史 → **5 个真欠账**（09-05/07/08/09/10）`git mv` verbatim 到 `archive/`（sha256 逐一比对一致）+ 建指针头；**09-06 已合规轮转过**（在库 179 行是新会话累积）→ 改为**追加 7 个新 block 到 archive**（25316→43114B，18 个 Session 零丢失）+ 重建指针头。⚠️ 判据纠正：**ADR-021 是"append 时超限才轮转"，不是"只能轮转一次"**；轮转后新会话增长属正常，需按"在库 vs archive 是否重叠"决定追加还是新建。
- **⚠️ P1 判断错误 → 撤回**：`grill-me`/`grill-with-docs` 各 7 行**不是未合并的存根**，而是**vendored 别名转发器**（`disable-model-invocation: true` + 一行 `Call the Skill tool "grilling"` / `"grilling"+"domain-modeling"`）。三条铁证：① `AGENTS.md:186` 明令 `Do not relocate or delete — re-running setup-matt-pocock-skills recreates them`；② `skills-lock.json` 登记（`productivity/grill-me`、`engineering/grill-with-docs`）；③ `grill-with-docs` 的独有语义＝**编排两个 skill**，`grilling` 无法覆盖。**教训：消融判据不能只看"行数+description 相似度"，必须读内容 + 查引用 + 查 lock。删除会被重建＝无效操作。**
- **P2 修复**：`AGENTS.md:189` 原写 `CONTEXT.md + docs/adr/` 标注为 single-context —— 但 `docs/adr/` 是 `domain.md` 里 **multi-context** 的默认路径（本仓库 single-context 用 `Docs/decisions/`）→ 改 `Docs/decisions/` + 括注"不是小写 docs/adr/"。
- **新增交付门（AGENTS.md §8 `Delivery Gate`）**：只加**可验证**的 2 个动作 ① 最强反方三问（怎么失败/隐含假设/测试盲区）② 三段标注【已验证/推断/假设】（把"禁止编造测试数字"从反向禁止升级为正向标注）。**未把不可验证条目（第一性原理/独立思考）写进去**——它们已由 §2 不变量 / ADR-022 / `grilling` 结构性内建。
- 验证：`dev.ps1 verify-rules` **OK**（AGENTS.md 全部链接 + key files 解析）；`git status` = **5 R + 5 ?? + 2 M**；全部文件 LF 合规。

**相关引用**
- 报告: `artifacts/agent-workflow-ablation-20260910.html`
- ADR: `Docs/decisions/021-journal-rotation-and-parallel-worktree-discipline.md`（轮转依据）
- 文件: `AGENTS.md`（§8 Delivery Gate + :189 路径）、`Docs/journal/archive/{2026-09-05,07,08,09,10}.md`（新轮转）、`Docs/journal/archive/2026-09-06.md`（追加）、`Docs/journal/{2026-09-05..10}.md`（指针头）
- 待办：见 `Docs/journal/NEXT.md`

