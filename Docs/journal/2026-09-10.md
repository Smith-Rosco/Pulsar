# 2026-09-10

## Session (07:4x) — 嫌疑区核查

### 做了什么
- **resx 父前缀嫌疑区 126 键逐键核查完成（只读，未删键）**：报告 `Docs/reports/2026-09-10-RESX_SUSPECT_ZONE_VERDICT.html`。结论 **126/126 死键**：0 存活 / 3 仅测试陈旧引用（CommandSlot.AutoSignIn、FirstLaunch.GeneralProductivityDesc、FirstLaunch.SelectScenarioError——mock/allowlist 引用，生产 0 命中）/ 123 纯 0 引用。
- 关键排除证据：动态拼接三模式（`"Fam." +` / `$"Fam.{…}"` / `Format("Fam.{0}")`）全语料扫描唯一命中 TutorialStepLoader.cs:251/260 `"Tutorial." + suffix`，其派生集合实测仅 8 键（TutorialSteps*.json step ID 末段 PascalCase）与嫌疑键零交集；preset 键 PresetCatalogService 静态写死不含 AutoSignIn；FirstLaunch.*Desc 被现行 *SlotDesc（OnboardingTemplateService switch）替代。
- 本地 commit：`e5c87ef`（报告）、`226dca8`（NEXT.md 上轮勾选收口 + 核查结论）。

### 坑
- 审计报告 HTML 提键：`html.index('父前缀误判嫌疑区')` 命中顶部汇总表导致跨节误读 215 键——须用 `<h2>父前缀误判嫌疑区` 锚定小节边界。
- 「族拼接风险」初版标记过吵：同族其它静态键在用≠动态拼接，需独立跑三模式聚焦扫描。

---

## Session (08:0x) — 126 键删除落地（用户裁决通过）

### 做了什么（grill auto-with-guardrails）
- **126 键双语删除**：`Strings.resx` / `Strings.zh-CN.resx` 各 **1077 → 951**。guardrails：精确键名（报告 h2 锚定提取，无前缀通删）、约定动态键保护区不碰、删除前干跑断言（双文件各 126 块在位）、写盘前计数断言、LF 保持。
- **XML 级权威对账**：删除集合==126 键集合；保留键 name→value 与 HEAD 逐对比对**零变化**；无新增键；EN/ZH 键集完全平行；两文件 XML well-formed。git diff 的 86 行 insertion 确认为行配对伪影。
- **3 处测试陈旧引用清理**：`PresetLocalizationKeysTests.cs`（RequiredKeys 移除 `CommandSlot.AutoSignIn`，修正尾部逗号）、`OnboardingVerificationTests.cs:114/125`（两行无效 mock 删除）。
- 验证：build **0 警告 0 错误**（11.8s）；全量 **1484/1484** 与基线一致（38s）。

### 关键决策·坑
- **⚠️ 新坑（重要）**：resx 中存在 `</data>` 后**直接跟 `<data`（无换行）**的相邻块（Strings.resx 两处：`Dialog.ConfigBackup.ImportPasswordHint` 之后、`CommandSlot.AutoSignIn` 之后）。非贪婪块删除正则 `<data\b.*?</data>\n` 在此**跨块吞噬**——首版手术把无辜的 `SettingsPage.Gesture.Title` 连带吞掉（计数断言 950≠951 拦截，fail-safe 未写盘）。**修复：块边界 span 拼接算法**（finditer 逐块判定 + 只吞块后最多一个 LF，不依赖换行假设）。resx 批量删除脚本今后一律用此法，禁用「块+换行」匹配式 re.sub。
- 预览工具注入坑再复发：核查报告 HTML 预览后被注入 `data-page-node-id`（168 处）→ `git checkout --` 还原（提交在前预览在后，零损失）；提取锚点改为容忍属性的 `<h2[^>]*>` 形式。
- 同文件并行 Edit 触发 EBUSY（NEXT.md 两处并发改），串行重试即过——与 09-09 教训一致。

### 相关引用
- `Pulsar/Pulsar/Resources/Strings.resx` · `Strings.zh-CN.resx`（各 -126 键）· `Pulsar.Tests/Presets/PresetLocalizationKeysTests.cs` · `Pulsar.Tests/Tutorial/OnboardingVerificationTests.cs` · `Docs/journal/NEXT.md` · `Docs/reports/2026-09-10-RESX_SUSPECT_ZONE_VERDICT.html`

---

## Session (08:1x) — 半径双路径调查 + ADR-030 提案

### 做了什么
- **半径双路径分歧现场调查完成**（R1 遗留项）：Path A（`CalculateOptimalLayout`，默认槽径 50）vs Path B（`GetLayoutMetrics`，缩放槽径）。语义判定：**Path A 是幽灵半径**（N>8 实际槽径 <50，却按 50 推导半径），Path B 才是正确意图。
- 影响面实锤三处：①运行时死区失真——`GetDeadZoneRadius` 取 Path A、`_currentRadius` 取 Path B，`HitTestAt`（MenuSession:2092-2102）同函数两源混用，死区有效比率漂移 0.6536（N=10）→0.7154（N=12）；②编辑器环半径（Path A）与显示槽径（Path B 语义）自相矛盾且偏离运行时；③R1 后 `SlotLayoutEngine.cs` 需注释守卫双路径。
- **ADR-030 提案**（`Docs/decisions/030-wheel-radius-single-formula.md`，commit `66e277c`）：Path B 胜出——`CalculateOptimalLayout` 改用 `CalculateOptimalSlotSize(N)` 推导半径。运行时环视觉零变化；死区还原设计比率；编辑器自动同源。测试迁移 pose snapshot N=10/12 钉死值 + 新增双路径恒等断言。**Status: Proposed，实施等用户裁决。**

### 关键判断
- N≤9 两路径数值恒等（分歧仅 N=10/11/12）；运行时环视觉零变化是方案可行的关键——唯一可感知变化是 N≥10 死区缩小 3.95–9.58px（回到设计意图）。
- Alternatives 拒绝理由：Path A 胜出会放大真机环且违背缩放初衷；参数化=bug 合法化。

### 相关引用
- `Docs/decisions/030-wheel-radius-single-formula.md` · `SlotLayoutEngine.cs` · `RadialMenuLayoutCoordinator.cs` · `MenuSession.cs:2092/2227` · `SlotWheelEditorViewModel.cs:243` · NEXT.md（新待办条目）

---

## Session (08:4x) — ADR-030 实施（用户裁决通过）

### 做了什么
- **半径单一公式落地**：`SlotLayoutEngine.CalculateOptimalLayout` 改用 `CalculateOptimalSlotSize(slotCount)` 推导半径（ADR-030 D1）——幽灵半径路径（默认 50px 槽径）废除，守卫注释删除；`ISlotLayoutEngine.CalculateOptimalLayout` 加 single-source 契约注释。运行时环视觉零变化（GetLayoutMetrics 语义不动）。
- **测试迁移**：`SlotLayoutEnginePoseSnapshotTests` N=10 钉死值 97.0820→90.6099（deadZone 59.2200→55.2720）、N=12 115.9111→100.4563（71.8649→62.2830）+ 全部槽位坐标表重算（N=8 不变）；文件头双路径说明改为 ADR-030 单源说明。
- **新增 `RadiusPaths_AreIdentical`（N=6..12）**：测试自持同一 `SlotLayoutEngine` 实例注入协调器，断言 `CalculateOptimalLayout(N).Radius == GetLayoutMetrics(N).Radius`——单源保证钉进测试，第二路径回归即红。
- 验证：build **0/0**；全量 **1491/1491**（1484 基线 + 7 恒等断言，精确吻合）。

### 坑
- 恒等断言初版想引用不存在的 `_coordinator.DebugEngine`——改为测试构造 engine 实例并注入协调器（同源比对），不为此暴露 internal 成员。
- 钉死值用 Python 同序 IEEE 复算（与 C# 同为 double、运算序一致），配合 BeApproximately(1e-9/1e-6) 容差双保险。

### 待办
- 用户真机 sanity：N=10/12 各一轮（中心环带命中 / 边缘槽命中 / 编辑器预览对照）→ 通过后 ADR-030 改 Accepted（NEXT.md 有悬挂条目）。

### 相关引用
- `SlotLayoutEngine.cs` · `ISlotLayoutEngine.cs` · `SlotLayoutEnginePoseSnapshotTests.cs` · `RadialMenuLayoutCoordinatorTests.cs` · `Docs/decisions/030-wheel-radius-single-formula.md`（Status→Implemented）
