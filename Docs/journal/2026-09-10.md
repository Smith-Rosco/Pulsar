# 2026-09-10

> 本日前序会话（07:4x–12:3x，resx 治理 / ADR-030 / 两个 tab 修复 / 技能沉淀 / 文档债务治理 / 发布）已轮转至 [archive/2026-09-10.md](./archive/2026-09-10.md)（ADR-021，文件超 15KB 上限）。

---

## Session (12:5x · Agent 工作流消融实验 → 按结论落地)

**做了什么**
- 用户给 8 条思维要点（第一性原理/对抗审查/消融实验/奥卡姆/不自信点/独立思考/批判性/高内聚低耦合），要求融入 agent coding 工作流。先出消融报告（非侵入、只读）：`artifacts/agent-workflow-ablation-20260910.html`。
- 按报告优先级落地 4 项：P0 journal 轮转 · P1 grill 存根（**撤回**）· P2 AGENTS.md 路径矛盾 · 新增 2 个交付前挂载点。

**关键决策 / 坑**
- **P0 轮转（ADR-021）**：6 个文件静态超限，逐日核实轮转史 → **5 个真欠账**（09-05/07/08/09/10）`git mv` verbatim 到 `archive/`（sha256 逐一比对一致）+ 建指针头；**09-06 已合规轮转过**（在库 179 行是新会话累积）→ 改为**追加 7 个新 block 到 archive**（25316→43114B，18 个 Session 零丢失）+ 重建指针头。⚠️ 判据纠正：**ADR-021 是"append 时超限才轮转"，不是"只能轮转一次"**；轮转后新会话增长属正常，需按"在库 vs archive 是否重叠"决定追加还是新建。
- **⚠️ P1 判断错误 → 撤回**：`grill-me`/`grill-with-docs` 各 7 行**不是未合并的存根**，而是**vendored 别名转发器**（`disable-model-invocation: true` + 一行 `Call the Skill tool "grilling"` / `"grilling"+"domain-modeling"`）。三条铁证：① `AGENTS.md:186` 明令 `Do not relocate or delete — re-running setup-matt-pocock-skills recreates them`；② `skills-lock.json` 登记（`productivity/grill-me`、`engineering/grill-with-docs`）；③ `grill-with-docs` 的独有语义＝**编排两个 skill**，`grilling` 无法覆盖。**教训：消融判据不能只看"行数+description 相似度"，必须读内容 + 查引用 + 查 lock。删除会被重建＝无效操作。**
- **P2 修复**：`AGENTS.md:189` 原写 `CONTEXT.md + docs/adr/` 标注为 single-context —— 但 `docs/adr/` 是 `domain.md` 里 **multi-context** 的默认路径（本仓库 single-context 用 `Docs/decisions/`）→ 改 `Docs/decisions/` + 括注"不是小写 docs/adr/"。
- **新增交付门（AGENTS.md §8 `Delivery Gate`）**：只加**可验证**的 2 个动作 ① 最强反方三问（怎么失败/隐含假设/测试盲区）② 三段标注【已验证/推断/假设】（把"禁止编造测试数字"从反向禁止升级为正向标注）。**未把不可验证条目（第一性原理/独立思考）写进去**——它们已由 §2 不变量 / ADR-022 / `grilling` 结构性内建。
- 验证：`dev.ps1 verify-rules` **OK**（AGENTS.md 全部链接 + key files 解析）；`git status` = **5 R + 5 ?? + 2 M**；全部文件 LF 合规。

**相关引用**
- 报告: `artifacts/agent-workflow-ablation-20260910.html`
- ADR: `Docs/decisions/021-journal-rotation-and-parallel-worktree-discipline.md`（轮转依据）
- 文件: `AGENTS.md`（§8 Delivery Gate + :189 路径）、`Docs/journal/archive/{2026-09-05,07,08,09,10}.md`（新轮转）、`Docs/journal/archive/2026-09-06.md`（追加）、`Docs/journal/{2026-09-05..10}.md`（指针头）
- 待办：见 `Docs/journal/NEXT.md`
- 本地 commit（用户批准，2026-09-10 13:2x）：`b28478d` journal 轮转 → `c4cec4c` AGENTS.md Delivery Gate + 路径修正 → `e756311` session-journal Step 3 补轮转判据。**push 由用户执行**。
- 备注：`.opencode/` 在 `.gitignore` 中但 `session-journal` 双副本**已被追踪** → 其修改正常 `git add` 即可（无需 `-f`；git 对已追踪文件的改动不走 ignore 判定）。


---

## Session (13:3x–13:5x · 设置页 UX 五项：1 真 bug + 3 改进 + 1 真 bug)

**做了什么** — 用户 debug/perf 混提四项，加一轮追问后的密码字段 bug，共五项；全部落地 + 验证。

### ① slot 创建保存链修复（真 bug）
- **现象**：点「保存槽位」→ 跳回「自动化槽位」→ 弹「未保存更改」→ 点保存 → 又跳回动态 tab（实际已创建成功）。保存机制不流畅。
- **根因（证据链）**：`SettingsViewModel.CommitCreatedSlotFromTabAsync` 只调 `_slotEditor.CommitCreatedSlot()`（内存）+ `MarkDirty()`，**不落盘**；随后 `DiscardTransientPageAsync`（内部导航到 Slots）+ `OpenTransientPageAsync`（导航回实体 tab）**是两次导航**。第一次离开时 `IsTransient(当前页)` 已为 false（注册被移除）→ 命中 `SettingsShellViewModel:60-65` 的 `userInitiated && !sourceIsTransient` 分支 → 守卫弹「未保存更改」。
- **修复**：提交后立即 `await Save()`（落盘 → `ResetDirty()`），守卫后续全 no-op；删除多余跳转，只保留**单次**导航到实体编辑 tab；成功通知从"提交时"移到"落盘后"（消除假成功）。文件：`ViewModels/SettingsViewModel.cs`。

### ② 进程选择器加刷新按钮
- `ProcessPickerViewModel` 新增 `IsRefreshing` / `CanRefresh` / `RefreshCommand`（重枚举 `GetActiveWindowsAsync`，保留过滤词，按 ExePath+Title 还原选中）；XAML 搜索框右侧加按钮（刷新中换 `ProgressRing`，`ArrowClockwise24` 靠 `InverseBoolToVisibilityConverter` 隐藏）。新增 resx 键 `Dialog.ProcessPicker.Refresh`。

### ③ 外观页「主题预设」卡片取消折叠
- **用户澄清是外观页（Appearance）这张卡片**——该卡片实为「色块网格」；`设置→常规` 的「右键手势」卡片才是用户描述的「一个开关+一个按钮」。按澄清改外观页主题预设。
- **实现要点（坑）**：`Wpf.Ui.Controls.Card` 在 4.3.0 里**只有 Content/Footer，没有 Icon/Header**（查 `Wpf.Ui.xml` 确认），不能替代带图标的卡片外观。改用 `CardExpander IsExpanded="True" IsEnabled="False"` 锁死展开；`IsEnabled=False` 会向下继承灰化 → 正文 `ListBox` 必须显式 `IsEnabled="True"` 重新点亮。

### ④ 子动作行默认折叠
- `SubSlotEditorRow.IsExpanded` 默认 true → false；`ReloadSubActionRows` 显式 `row.IsExpanded = false`（载入已保存配置的行）；`AddSubAction` **保持展开**（用户主动添加）。新增测试 `SubActionRows_LoadedFromSlotShouldStartCollapsed`。

### ⑤ 密码字段 placeholder 重叠 + 选择器 tooltip 语义错（真 bug）
- **现象**：密码类型 slot 编辑页，密码字段 placeholder「两个叠在一起」；左侧打开密码选择器的按钮 tooltip 也不对。
- **根因＝WPF 属性优先级陷阱（不是布局问题）**：`SlotParameterFieldTemplate` 的 `Grid.Column="1"` 中 secret 分支有两个元素同格竞显 —— `ui:TextBox`（`PlaceholderText`）与 `Border`（`DisplayValue`）。TextBox **同时**带 (a) 标签上的**本地** `Visibility="{Binding IsBoolType, Converter=BoolToVisInverse}"` 与 (b) `Style` 内 `IsSecretSelector` 的 `DataTrigger`。**WPF DP 优先级：本地值 > Style Trigger** → (b) 永久失效 → TextBox 可见 ∧ Border 也可见 → 重叠。
- **修复**：删标签上的本地 `Visibility`，全部条件统一收进 `Style.Triggers`（`IsBoolType=True`→Collapsed、`IsSecretSelector=True`→Collapsed）+ `<Setter Value="Visible"/>` 兜底。**两份重复模板同改**：`SlotConfigurationDialogContent.xaml`（~129）、`AddSlotContent.xaml`（~194）。
- **选择器按钮语义化**：原硬编码 `FolderOpen16` + `Dialog.PluginSettings.Browse` → `SlotParameterEditorModels` 新增 `PickerButtonIcon`（Process→`WindowConsole20` / File→`FolderOpen16` / Secret→`Key24`）+ `PickerButtonTooltip`（按 `PickerIntent` 映射）；新增 3 对 resx 键 `Dialog.AddSlot.Pick{Process,File,Secret}`。
- **⭐ 新增静态守卫锁定缺陷类**：`SettingsLayoutGuardTests.No_element_declares_visibility_both_locally_and_in_its_own_style`（扫「本地 Visibility ∧ 自身 Style 内有 Visibility Setter」）。**做了证伪测试**：临时注回旧缺陷 → 守卫**如期失败**并精确报 `AddSlotContent.xaml:241`；还原后通过 → 守卫非「恒真」已证。

**验证**
- build **0 警告 / 0 错误**；全量 **1494/1494**（基线 1493 + 新增 1 守卫）。⚠️ 行尾：工作区**全部文件本即 CRLF**（含未改动的 `App.xaml`），`.gitattributes` 在 check-in 归一化 → 本次**未引入行尾回归**（`git diff --numstat` = 47/0、12/0、18/6，非全文件重写）。
- **⚠️ 环境坑**：调试期间用户**正在运行** `bin\Debug\...\Pulsar.exe`（PID 18416）→ 锁死输出文件，`dotnet test` 报 `MSB3027/MSB3021`（重试 10 次失败）；终止后恢复。**跑 build/test 前先查 `Get-Process -Name Pulsar`。**

**改动文件（14，含测试与 journal）**
`ViewModels/SettingsViewModel.cs`、`ViewModels/Dialogs/SlotEditorViewModel.cs`、`ViewModels/Dialogs/SubSlotEditorRow.cs`、`ViewModels/Dialogs/ProcessPickerViewModel.cs`、`Models/SlotParameterEditorModels.cs`、`Views/Dialogs/Contents/{ProcessPickerContent,SlotConfigurationDialogContent,AddSlotContent}.xaml`、`Views/Pages/SettingsAppearancePage.xaml`、`Resources/{Strings,Strings.zh-CN}.resx`、`Pulsar.Tests/ViewModels/DialogSlotEditorViewModelTests.cs`、`Pulsar.Tests/UI/SettingsLayoutGuardTests.cs`。

**未决（非阻塞）**：③ 的 `IsEnabled=False` 对 CardExpander 标题栏前景色灰化是否被标题 TextBlock 显式 `Foreground` 抵消 —— 无头测不到，需真机目视。**未 commit**（等用户裁决）；push 由用户执行。


---

## Session (19:1x · 架构审查候选 W1：WinSwitcher 启动走 IProcessLauncher seam（grill auto + TDD）)

**做了什么** — 上午架构审查（improve-codebase-architecture，聚焦 WinSwitcher）产出的 4 候选，用户选定 W1：`WinSwitcherPlugin.LaunchApplicationAsync` 直调 `Process.Start`（原 L328）→ 改走既有 `IProcessLauncher` seam（ADR-012），ExecuteAsync 决策树首次可 Moq 测试。grill auto 定 4 决策后 TDD 落地。

**关键决策 / 坑**
- **注入点 = `Initialize(IServiceProvider)` + GetService 且 fail-fast**（缺 `IProcessLauncher` 即抛，同 `IWindowService` 模式）——否决构造注入：13 处测试/调用点全走 `new WinSwitcherPlugin()` + Initialize，构造注入会全面翻修且同文件混两风格（CommandPlugin 的 ctor 风格保持不动）。
- **⚠️ 事实发现**：`IWindowActivationService.LaunchApplicationAsync`（`WindowService.cs:342`）**零调用方**（全仓 grep 仅接口声明 + 实现）→ 判 W1 范围外（seam 注入零收益），记为接口删除候选（NEXT.md）。候选 W2/W3/W4 未开。
- 验证前置检查保持纯函数留在插件内（`Path.IsPathRooted`/`File.Exists`/扩展名白名单）；`File.Exists` 是只读探测不属 AGENTS.md §8 变异清单 → 不设 IFileSystem seam（单 adapter = 假想 seam）。异常→严重级映射原样保留。
- 红绿证据：新增 15 用例先跑 **6 失败**（launcher 被绕过 / Initialize 未 fail-fast，恰为预期红集）→ 接缝改造后 25/25。

**验证**
- build **0/0**；全量 **1556/1556**。数字对账：journal 基线 1521 + `0a43c31`（SecretFill ID 迁移，session 后入库）+20 + 本次 +15 = 1556（`git show --stat 0a43c31` 实证其新增测试文件）。CS8601 新告警出现后即以 `(... as IProcessLauncher)!` 模式消除，终态 0 警告。

**改动文件（2）**：`Pulsar/Plugins/Core/WinSwitcher/WinSwitcherPlugin.cs`（+字段/Initialize fail-fast/`_processLauncher.Launch(startInfo)`）、`Pulsar.Tests/Plugins/Core/WinSwitcherPluginBehaviorTests.cs`（+15 用例 + 2 处既有 Initialize 测试改用共享 helper）。**未 commit**（用户处理）。

---

## Session (20:4x · 架构审查候选 W2：Discovery Exclusion Policy 单一所有者（grill auto + TDD）)

**做了什么** — W2 落地：排除策略（`ExcludeProcesses`/`ExcludeRules`/`EnableSwitchDiagnostics` 三键）此前 3+1 个写者、2 个消费面、注册表死写路径不重放 → 新增 `IDiscoveryExclusionPolicy`/`DiscoveryExclusionPolicy`（`Services/WindowSwitching/`）单一所有者；`IWindowDiscoveryService` shed 4 成员回归纯枚举。

**关键决策 / 坑**
- **⚠️ 事实纠正（报告两处偏差）**：① 报告 Writer 2（ProcessRegistryService 写 ExcludeProcesses）实为**死代码**——`UpdateBlacklistAsync`/`SetBlacklistStatusAsync`/`GetBlacklistedProcessesAsync`/`SyncToProfilesConfigAsync` 零生产调用方，"evaluator 黑名单陈旧"是潜在而非活体 bug → 四者随 Q4 删除；② 实际第 4 写者是 **WindowInspectorViewModel**（运行时 push + 自持久化 ExcludeRules，绕过插件影子副本）→ 改道 `policy.SetRulesAsync`（先应用后持久化，失败规则仍生效）。
- **写者收敛**：设置 UI 走既有 ConfigEditSession+UpdateSettings 链 → 插件 `UpdateSettings = policy.ApplyFromConfig(settings)` 一行（影子状态删除，插件不再是 relay）；内核激活/设置保存/启动引导三路共用；bootstrap 在 `LoadCoreAsync` 前调 `InitializeFromConfig()`（顺序测试钉死）；诊断开关状态移入 policy，WindowService 只读。
- **行为变更（Q3）**：OnDisable 不再清黑名单——消除"清黑名单留规则"不对称，策略比插件活得久（Core 插件 `CanDisable=false`，仅 unload 触发）。**偏差记录（Occam）**：Q6 中 `SetBlacklistAsync`/`SetDiagnosticsAsync` 持久化变体未实现——Q4 后零调用方，不为无调用方造 API。
- 接口 shed 爆炸半径：唯一实现 WindowService + 插件/Inspector 两消费方 + 3 测试文件；依赖链 Manager→Plugin→DialogVM→Inspector 全走尾部可选参数（零破坏）。

**验证**
- 红绿：DiscoveryExclusionPolicyTests 13 用例先行（CS0246 红）→ 22/22；build **0/0**；全量 **1569/1569**（40s）。对账：基线 1556 +13 policy +1 plugin guard −2 orchestration 转发（删）= 1568，实测 1569 → 差 1【推断】为基线计数口径，非本轮引入（0 失败为门）。顺带修：Inspector 持久化失败提示旧会被成功消息覆盖。

**相关引用**：审查报告 W2（Temp）；CONTEXT.md 新术语 **Discovery Exclusion Policy**；改动 17 改 + 3 新（接口/实现/测试）。**未 commit**（用户处理）。

---

## Session (21:4x · 架构审查候选 W3：Slot 参数块三重收拢（grill auto / full-auto + TDD）)

**做了什么** — W3 落地：WinSwitcherPlugin 三动作（switch/launch/activate）5 个手写参数块收拢为唯一参数规范。新增 `WinSwitcherPlugin.Metadata.cs`（partial，226 行）：GetMetadata + 3 个动作工厂 + 参数工厂（`AppParameter`×2 调用点 / `FallbackPathParameter` + `ExecutablePathParameter` 双变体 / `ArgumentsParameter(group, desc, placeholder, hint?)` 4 值旋钮）；主文件 GetMetadata 块（~224 行）移出，601 → ~340 行。

**关键决策 / 坑**
- **⚠️ 事实纠正（审查偏差 ×2）**：① "path ≈ copy of launch's path" 言过其实——实为有意双变体（必填 QuickEdit"Executable Path" vs 可选 DialogOnly"Launch Path"，9 字段差异）→ 收拢 = 具名双工厂而非单定义强并；② "155→~60 行"偏乐观（SlotParameterMetadata 20 属性，默认吸收有限）——真实收益是防漂移 + 可导航性，非行数。
- **唯一真实重复 `app` ×2 已漂移 1 词**（"find a running window" vs "find the target window"）→ 统一为后者（仅槽编辑器英文文案；Label 本地化走 `SlotParam.{AlphaNumOnly}` 约定，4 键 resx 全覆盖 → 零 resx 变更）。
- **partial 拆分而非 Command 式 static class**：GetMetadata 读 7 个实例身份属性，partial 免 9 参透传（CommandPluginMetadata 的 static Create 是 ctor 注入被迫的形状，不跟）。
- 红绿证据：参数级 canonical spec 测试（`BuiltInPluginMetadataTests.WinSwitcherMetadata_Parameters_ShouldMatchCanonicalSpec`）先行——`BeEquivalentTo(activate.app, switch.app)` 首跑**恰好抓到那 1 词漂移**（红）→ 重构后绿。此前参数级元数据**零测试覆盖**，该测试同时成为永久防漂移网。

**验证**
- build **0/0**；全量 **1570/1570**（37s）。对账：W2 基线 1569 + 1（本测试）= 1570，精确吻合。

**相关引用**：审查报告 W3（Temp/architecture-review-20260910-winswitcher.html）；`Docs/lessons/` 无新增（纯元数据重构）。**未 commit**（用户处理）。W4 未开。

---

## Session (22:3x · 接口删除候选轮：7 个零调用 facade 成员出清（W4 机械首刀）)

**做了什么** — NEXT.md 既有删除候选落地 + 顺带 facade 活跃度复查扩容：`IWindowActivationService.LaunchApplicationAsync`（W1 判范围外遗留）删除；全仓 grep（生产/测试/Simulator 三层）复查 `WindowService` 全部 26 个公共成员 → 新发现 6 个零调用 facade 成员，经用户裁决（"删 #1-#6，#7 另行"）成对删除（接口成员 + 实现）：`FocusWindow` / `GetForegroundWindow`(WindowInfo 版) / `TryGetCachedActiveWindows` / `PreWarmWindowInventory` / `GetFocusRestoreMode` / `RestoreFocus`，涉及 `IWindowActivationService` / `IWindowDiscoveryService` / `IWindowFocusContextService` 三接口。

**关键决策 / 坑**
- **复核即证伪"测试即活性"**：`PreWarmWindowInventory` 的 facade 转发零调用，但预热行为本身活着——走的是 `IWindowInventoryCoordinator.PrewarmOnMenuDismiss`（MenuSession 直连，有测试钉死）；`MenuSessionTwoPhaseOpenTests` 测试名含 "PreWarm" 易误读为 facade 活性证据，实际 mock 的是 coordinator。
- `RestoreFocus`/`GetFocusRestoreMode` 纯转发 `IFocusManager`（`ReleaseAsync()`/`RestoreMode`），SecretFill 的 `InjectionStepType.RestoreFocus` 枚举为同名异物，不构成调用。
- 删除 `LaunchApplicationAsync` 顺带消灭 `WindowService` 最后一个直调 `Process.Start` 的启动路径——审查 W1 "两处直调" 全部收口（插件侧 W1 走 seam、facade 侧本轮删除）。
- **#7 留存待裁决**（NEXT.md 候选二）：`GetRunningProcessNamesAsync` 疑似整链路死（IWindowDiscoveryService 成员 + WindowService 转发 + IWindowInventoryService 成员 + WindowInventoryService 实现 + 1 个直测用例），删内层涉及测试删除故单独立项。
- 副产物观察：`FocusManager.RestoreMode` / `ReleaseAsync()` 是否还有其他消费方未深挖——facade 删除不影响其存活性判定，留待 IFocusManager 收口时一并看。

**验证**
- build **0/0**；全量 **1570/1570**（两轮各一次，删除前后均与基线一致）——零测试引用 = 零调用方的实证闭环。

**追加（22:5x · #7 裁决落地）** — `GetRunningProcessNamesAsync` 整链路 5 件删除（用户裁决"整链路删除"）：`IWindowDiscoveryService` 成员 + `WindowService` 转发 + `IWindowInventoryService` 成员 + `WindowInventoryService` 实现（存活兄弟 `GetRunningProcessesAsync` 之上的纯 LINQ 投影，零逻辑损失）+ 1 个测试（同文件 `GetRunningProcessesAsync_WhenStructuralFilter...` 的严格弱化重复——弱在少 `EvaluateSnapshot Times.Never` 断言，零覆盖损失，行为覆盖由 :52 存活测试继续钉住）。验证：build **0/0**，全量 **1569/1569**（对账精确：1570 − 1 删除测试）。

**相关引用**：NEXT.md 候选划线；改动 4 接口（Activation/Discovery/FocusContext/Inventory）+ WindowService.cs + WindowInventoryService.cs + WindowInventoryServiceTests.cs。**未 commit**（用户处理）。

---

## Session (23:1x · 架构审查候选 W4：facade 收缩重指向（grill auto-with-guardrails，审查四卡收官）)

**做了什么** — W4 落地：Explore 子代理做 IWindowService 消费方全量普查（39 消费方 = 生产 15 + Simulator 1 + 测试 23），据此重指向生产 10 直连消费方到窄接口：S×4（RadialMenuWindow / SendKeysInjectionExecutor / VbaRunner / BookmarkletRunner，GetService 式改 `IWindowShellService`）、F×2（MenuSession ctor / `PulsarContext.Capture` 静态方法参数）、A×1（WinSwitcherPlugin Initialize fail-fast，消息同步改）、D×4（WindowInspectorViewModel 及 PluginViewModel 三级透传链），字段/参数重命名诚实化。DI 零改动（4 转发注册早已就绪，App.xaml.cs:220-223 + Simulator）。

**关键决策 / 坑**
- **Q2 裁决修正（grill 推荐被事实推翻）**：原推荐把 SlotStrategies 两策略拆成 F+A 双窄接口；实施前构造点盘点发现 WindowSwitchSubMenuStrategy 也是 F+A（且喂 WindowSwitchStrategy），拆分将令全部构造点出现"同实例双参"丑态 → 回归审查原设计：真 facade 消费方保留 aggregate。facade 消费方清单钉进 `IWindowService` 注释（策略 3 + 透传 2 + Simulator + DI 锚点）。教训：**推荐前先数构造点**。
- **Q4 坑："测试零改动" 只对 ctor 注入成立**：`Mock.Of<IWindowService>()` 可赋窄接口参数（接口继承）→ ctor 式测试零改动 ✓；但 GetService 式插件的 `Mock<IServiceProvider>` 只 setup 了 `GetService(typeof(IWindowService))`，窄接口请求返回 null → fail-fast 21 红（WinSwitcher 18 + BookmarkletRunner 3）。修法 = fixture setup 键改窄接口类型（6 处），mock 实例不变（IWindowService 实现全部窄接口）。
- 普查纠错一处：子代理漏了 WindowSwitchSubMenuStrategy 的生产文件（只报了其测试），终检 grep `IWindowService` 抓到——**消费方普查必须以终检 grep 收口，不能全信子代理清单**。
- `PulsarContext.Capture` 参数重类型后，PulsarContextTests 里多余的 GetProcessWindowsAsync/SelectTargetWindow setup 成为无害残留（loose mock 忽略），未清理（follow-up 级）。

**验证**
- build **0/0**；全量 **1569/1569** 与基线一致（纯重类型零行为变化；一轮 21 红→修 fixture→绿）。编译器即审计：窄接口缺普查外成员则编译失败。

**相关引用**：审查报告 W4（Temp）；`IWindowService.cs` 注释更新（facade 消费方清单）；`IWindowService` 接口解散 = 新独立候选（零业务消费方，仅 DI 锚点）。**未 commit**（用户处理）。**架构审查 W1-W4 四卡全部收官。**

**追加（23:3x · 解散候选裁决关闭 + 前提勘误）** — `IWindowService` 解散候选经用户裁决**关闭（保留）**。**勘误**：本块上文及 NEXT.md W4 条目所记"零业务消费方，仅 DI 锚点"**不实** —— 终检 grep 实证 5 个业务消费方（SlotStrategies 两策略 + WindowSwitchSubMenuStrategy + ProcessPageProvider + PageProviderFactory，均为 W4 Q2 合法保留的 F+A 真 facade 消费方）+ Simulator + DI 锚点。解散需 5 文件重类型（双窄参数 = W4 Q2 已否决的同实例双参丑态）或造第 5 个组合角色接口（单一消费簇过早抽象）——收益仅表面纯度，裁决保留。ADR-010 "facade can shrink" 已兑现：收缩至真 facade 消费方核心即不可再缩。**教训：记录"零消费方"结论前必须跑一次全仓 grep 对账，不能沿用会话内的口头总结。**

---

## Session (23:3x–23:5x · v1.14.0 发布（release 模式 + publish.py bugfix）)

**做了什么** — 按 publish skill 的 release 模式发布 v1.14.0：质量门槛 → 版本决策 → `set-version` → `changelog` 固化 → release notes → commit/tag → push → CI/Release 验证。中途修掉 `scripts/publish.py` 一个「报 PASS 但实际没干对」的 bug。

**关键决策 / 坑**
- **跳号发版（用户裁决）**：远程最新 tag 仍是 `v1.12.0`，而 CHANGELOG 里 `1.13.0` / `1.13.1` 两段**只写未发**（无 tag → 无 CI → 无 Release），csproj 也停在 1.13.0。用户裁决：发 **1.14.0**（含 feat + 插件 ID 破坏性迁移 → minor），notes **覆盖 v1.12.0..HEAD 全部 73 提交**（否则 1.13.x 的变更将永远不出现在 Release 页）。CHANGELOG 的 1.13.x 段保持原样不改写（append-only）。
- **🐛 `changelog` 子命令误伤 HTML 注释模板**：`CHANGELOG.md` 顶部模板注释里有一个缩进 2 空格的示例 `## [Unreleased]`，旧实现的 `re.search(r"## \[Unreleased\]\n...")` 与 `str.replace(..., 1)` 都命中它 → **注释模板被改写成 `## [1.14.0] - 2026-09-10`，真实 `[Unreleased]` 段纹丝不动，脚本照报 PASS**（7 条"真实条目"其实是注释里的 `- ...` 模板）。
  - 修法 v1（**仍有 bug**）：屏蔽 HTML 注释 + `^` 行首锚定。但 `re.M` 下 lookahead `(?=^## |\Z)` 会把行尾 `\n` 划进 body，重建串没补回去 → 段标题粘连成 `## [Unreleased]## [1.13.1]`，**净删 17 行**。
  - 修法 v2（最终）：定位同上，替换改为**只改标题行本体**（`text[:m.start()] + 新版标题 + text[m.start()+len(heading):]`），body 含全部换行原样保留。最终 diff = **2 插入 / 0 删除**。
  - 教训：**脚本报 PASS ≠ 动作正确**；`changelog` 之后必须 `git diff --numstat` 核对（正常应为「少量插入、0 删除」），删除侧出现数字即为异常。防护已写进 `.agents/skills/publish/SKILL.md` 排障段。

**验证**
- 门槛：build **0 警告 0 错误**；全量测试 **1569/1569**（42s）。
- 提交：`a852d8b` fix(publish) / `dbfcef9` docs(release notes) / `9cc608c` chore(release): bump version to 1.14.0。
- push：`main` 1e55722→9cc608c + 新 tag `v1.14.0`（tag message 首三字节 `###`，无 BOM）。
- CI：run **34497855266** `completed / success`（4m15s）；Release `draft=False`，资产 5 项齐全——`Pulsar-1.14.0-full.zip` 69.6MB、`Pulsar-1.14.0-portable.zip` 5.2MB、`Pulsar-v1.14.0-Setup.exe` 69.4MB、`Pulsar-v1.14.0-Standalone-win-x64.zip` 69.6MB、`SHA256SUMS.txt`；body 首行为 `### 新功能`（完整中文 notes 已生效）。
- 本次**未**本地构建打包（release 模式交给 CI，与 SKILL 一致）。

**相关引用**：`Docs/ops/RELEASE_NOTES-1.14.0.md`；`scripts/publish.py`（`_mask_html_comments` + `cmd_changelog`）；`.agents/skills/publish/SKILL.md` 排障段。

---

