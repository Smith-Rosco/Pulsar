# 2026-09-04

## Session (11:52)

**做了什么**
- 架构审查（/improve-codebase-architecture）：聚焦最近 80 次提交热点（插件运行时生命周期），产出 7 候选 A-G 的 HTML 报告（`%TEMP%\architecture-review-20260904-100022.html`）；按投入×收益紧迫性定位，用户选定候选 B（外部插件生命周期时序泄漏进 UI）。
- 候选 B 经 grilling 三轮 16 问全部定案后落地（commit `b4af4e1`，10 files，+786/−103，全量 993 测试通过）：
  - 提取 `IPluginPackageManager` seam（Inspect/Install/Uninstall + OperationProgress）
  - 新增 `IExternalPluginLifecycleOps` + `ExternalPluginLifecycleOps`（时序唯一 owner：安装 refresh→grant→activate、卸载 revoke→deactivate→delete；SemaphoreSlim(1,1) 串行化；部分成功带 `ExternalPluginOpPhase` 阶段）
  - 迁移 `ExternalPluginManagerViewModel` / `ExternalPluginViewModel`（只留 UI 决策），`App.xaml.cs` 注册新接口
  - 新增 11 个时序 mock 测试（`ExternalPluginLifecycleOpsTests`）
- 更新 HTML 报告：候选 B 标记「已实施」+ 落地详情，Top recommendation 改为「下一步回到 A」。
- grilling skill 改造（commit `2e7a1ee`）：新增三 mode——interactive / auto-with-guardrails（默认采纳推荐，仅 4 条 guardrail 命中才暂停）/ full-auto；结束交付审计日志（Q + 推荐 + 理由 + 决策）。用候选 B 16 问回放演练验证：16/16 全 auto 0 误伤，高风险反例全暂停。
- 排查「每次对话开始从零开始」：定位到 session-journal 机制从未生效——skill 存在于 `.opencode/skills/session-journal/`，但 `Docs/journal/` 目录从未创建；根因是工具链错位（opencode 的 skill 对豆包环境不可见）。已把 skill 复制到 `.agents/skills/session-journal/` 并创建本篇日志。

**关键决策 / 坑**
- 外部插件生命周期时序收敛为运维模块（CONTEXT.md 已加术语 **External Plugin Lifecycle Operator**）。
- grilling 三 mode + guardrails（外部契约 / 难逆转 / 用户已表达偏好 / ADR·安全不变量）+ 审计日志；auto 模式 shared understanding 判定改为「审计已审、无否决」。
- 坑：PowerShell 5.1 不支持 `&&`；内联管道 Group-Object 统计静默无输出（需先 Out-File 落盘）；shell state 不跨 Bash 调用保留（用绝对路径 + `git -C` 单命令）。
- 坑：AGENTS.md 承诺的 session-journal（read at session start / append at end）是空头支票——目录和落地记录都不存在，直到本次修复。

**下一步**
- [ ] 候选 A（运行时门面拆三个窄 seam）：注意 AGENTS.md 已写入 ADR-012（`IPluginRegistry` / `IPluginExecutor` / `IPluginRuntimeOps`），先确认 ADR-012 实际落地状态再决定是否跟进。
- [ ] 候选 C（manifest 三份解析收敛）/ D（熔断通知移过 seam，AGENTS.md 已提到 ADR-013 纯状态机）/ E（GC 细节封装）/ F / G 待定。
- [ ] 可选：`scripts/dev.ps1` 封装 build/test/commit 高频命令（规避 PS 5.1 `&&`）。
- [ ] 下次会话开头：读本篇 journal + 检查 `.agents/skills/session-journal` 是否被豆包环境发现。
- [ ] 工作区未跟踪、未提交：`.tools/`、`Docs/media/`、`Docs/从 vibe coding 到 spec coding：我用 Trellis 的实践总结.md`（非本次产物，勿误提交）。

**相关引用**
- 文件: Pulsar/Pulsar/Services/ExternalPluginLifecycleOps.cs · Services/Interfaces/IExternalPluginLifecycleOps.cs · Services/Interfaces/IPluginPackageManager.cs · Models/ExternalPluginLifecycleResult.cs
- 文件: Pulsar/Pulsar.Tests/Plugin/ExternalPluginLifecycleOpsTests.cs
- 文件: .agents/skills/grilling/SKILL.md
- 文件: .agents/skills/session-journal/SKILL.md · .opencode/skills/session-journal/SKILL.md
- 文件: `%TEMP%\architecture-review-20260904-100022.html`（架构审查报告）
