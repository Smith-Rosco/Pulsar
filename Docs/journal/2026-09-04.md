# 2026-09-04（当日）

> 本日 00:00–23:32 的详细记录体积超过 15KB 上限，已原样移入归档：`Docs/journal/archive/2026-09-04.md`（ADR-021）。
> 本文件用于当日剩余时段的会话记录。会话仪式默认只读本文件尾部（最新 Session block）+ `NEXT.md`，详情读归档。

---

## Session (23:53 · 落地 journal 轮转 + worktree 并行纪律，全程经 worktree 实施)

**做了什么**
- **ADR-021 落地**（commit `09ddae8`，10 文件 +846/−626）：会话仪式瘦身——只读 `Docs/journal/NEXT.md` + 最新日文件**尾部**（最后 Session block），不再整文件读；单日文件 ~15KB 上限，超限 `git mv` 原样归档至 `Docs/journal/archive/`。
- 归档 09-03（24.6KB）/ 09-04（47KB）→ archive（150/416 行原样保留），重建两个指针文件；新建 `NEXT.md` 持久待办单一权威（去重 4 项待办）。
- AGENTS.md v3.3.0：§8 仪式改 tail + NEXT；新增 **§10 并行 Agent & Worktree 纪律**（一 agent 一 worktree 一分支、journal + CHANGELOG 仅 main 提交、各 worktree 独立 bin/obj 构建隔离免费）。
- session-journal skill 双份镜像（`.agents/skills/` + `.opencode/skills/`）同步：Step 1 tail 读 / Step 2 ≤25 行 + 下一步归 NEXT.md / Step 3 轮转。
- **本任务全程在独立 worktree 实施**（分支 `docs/journal-worktree-discipline`），ff 合并 main 后清理 worktree 与分支——新纪律本身实测可行。

**关键决策 / 坑**
- 决策：轮转 = `git mv` 原样移动（永不删除/截断，git 保历史）；日级归档进 `Docs/journal/archive/`，月级仍走 `Docs/archive/`（CONTRIBUTING）。
- 决策：本次会话记录按新纪律在 main 上追加（journal 仅 main 提交），结构性改动（指针文件/NEXT 骨架/ADR）随分支合并落地。
- 坑：PowerShell 控制台读 UTF-8 中文乱码是 GBK 显示问题，文件本身无损（Read 工具验证）。
- 坑：git 进度输出走 stderr，PowerShell 渲染为红色"错误"行——`worktree add` / `merge` 实际成功（以 exit code 与结果为准）。

**相关引用**
- ADR: `Docs/decisions/021-journal-rotation-and-parallel-worktree-discipline.md`
- 文件: `AGENTS.md` §8/§10 · `.agents/skills/session-journal/SKILL.md`（镜像 `.opencode/skills/…`）· `Docs/journal/NEXT.md`
- 下一步统一见 `Docs/journal/NEXT.md`。
