# Journal NEXT（持久待办，单一权威）

> **用途**：跨会话「下一步」的单一权威清单。每个 `## Session` block 不再重复罗列待办，只在本文件更新——完成即划线 `~~…~~`，新增即在末尾追加（ADR-021）。
> **会话仪式**：Session start 必读本文件（小体积）；涉及具体某天的「做了什么/坑」才去读对应 `Docs/journal/YYYY-MM-DD.md` 尾部或归档。

## 待办

- [x] ~~Fan 位置问题收尾（change `2026-09-05-cascade-submenu-fan-qa`，QA 暂停中）：用户打完游戏后按 `submenu-radius-geometry-amendment-draft.md` §5 拍 4 张现象照 → 草案定稿 → ADR-011 追加 Amendment（推荐方案 A：Fan 移根环外侧 r≈120；Ring 视现象）→ 实施 + 新增「子项互不重叠/不撞中心圆」回归断言 → A3/A4 + B–H 全部用例 → 3.2/4.x 收口归档。当前真实配置仍是 QA fixture（`Profiles.json.bak-before-fan-qa` 还原点）；E2E 待用户有空再跑。~~ **已过时（2026-09-06）：**ADR-024 D1 已定义 Fan 几何（R+gap=160、±30°、同心），本会话经 GEOMETRY-TRACE + DPI 换算实测与规格吻合；三 bug 修复已 E2E PASS。旧草案（r≈120）不再适用。
- [x] 观察 1-2 个会话：AGENTS.md 瘦身后 agent 是否经 §3 指针去 `Docs/lessons/` 取坑位全文（防"全表靠内联"回潮，ADR-022 后续）。
- [x] 观察若干会话：确认无 harness 再向 `.workbuddy/memory/` 写正文（若复发 → 考虑 Junction 收口，ADR-019 后续）。**2026-09-05 检查：合规**——仅一行指向 journal 的指针（183B），无正文复发。
- [x] ~~子轮盘交付待用户真机验收：三 bug 修复（`artifacts/bug1-after-fix` · `bug2-after-fix` · `bug3-geometry`）+ ADR-024 v1.2.1 两项几何优化（Fan 扇区约束 orb 外缘收进扇区 / Ring 中心=父 Slot，`artifacts/fan-sector-constraint-2` · `ring-center-visible-4`），E2E 均已 PASS。~~ **真机验收通过（2026-09-09 23:0x 用户确认）**——R3（SubMenuTransitionController + CenterIdentityPolicy）随之解锁。

- [x] ~~架构审查 S1（附注）：`RadialMenuLayoutCoordinator` / `CommandPageProvider` / `ProcessPageProvider` 仍 0 直接测试（ADR-023:53 承诺的 page-provider tests 未兑现），后续候选。~~ **已全部还清（2026-09-09）**：Coordinator 8 用例由 R1 补齐；Command/Process page-provider 16 用例由本轮补齐（`PageProviderTests.cs`：排序/分页/策略分配/Badge/中心文本/seed 快路径/三态渲染），全量 1459/1459，build 0/0。
- [x] 观察 1-2 个会话：AGENTS.md 瘦身后 agent 是否经 §3 指针去 `Docs/lessons/` 取坑位全文（防"全表靠内联"回潮，ADR-022 后续）。
- [x] 观察若干会话：确认无 harness 再向 `.workbuddy/memory/` 写正文（若复发 → 考虑 Junction 收口，ADR-019 后续）。**2026-09-05 检查：合规**——仅一行指向 journal 的指针（183B），无正文复发。**2026-09-09 22:35：发现 09-09 文件已被多会话写满正文 → 已收敛为指针 + 硬事实**（本会话实施）。

- [x] ~~**主题重构（ui-ux-pro-max，2026-09-09 中午）**：`Theme.Dark/Light.xaml` 按设计系统「Code dark + run green」（Slate 色阶）重构——键名契约不变，语义组重排；Orb #2D2D2D→#334155、激活通道→Sky 系、深色危险 hover→#EF4444；`RadialThemeTokenSetTests` 钉死值同步；全量 1361/1361、build 0/0；`Docs/design-system/pulsar/MASTER.md` 持久化 + `artifacts/theme-refactor-preview.html` 预览。~~ **已于 2026-09-09 12:23 入库（`e7c4cfe`），非待办——本条目为划线状态同步遗漏（2026-09-10 盘点时核实）。**
- [x] **图标圆角化（2026-09-09 12:20）**：`build_radial_ico.py` 加 36px 圆角矩形遮罩，light/dark ico（7 尺寸帧）+ 256 masters + 根 Pulsar.ico 全部圆角透明；build 0/0、1361/1361。
- [x] **⚠️ 提交被安全软件拦截（阻塞中）**：git.exe 在 E 盘写 loose object 恒定 `Permission denied`（PS/py/cmd 正常、C 盘仓库正常、E 盘任何仓库均失败）→ 判定为安全软件（勒索防护/自定义防护）对 git 在 E 盘的进程级拦截。**待用户放行 git（或确认处置方式）后重试 `git add` + 两次提交（主题重构 / 图标圆角）。** **已解除（2026-09-09 下午核实）**：`e7c4cfe`/`abea161` 已入库，git 恢复正常。
- [x] **轮盘面板 UX 轨 U1-U6 已落地（2026-09-09 下午，grill auto-with-guardrails + TDD）**：U1 hover enter 300→120ms（`SlotOrb.HoverEnter/ReleaseDuration` 钉死）/ U2 磁吸上限 120→400px/s / U3 翻页 nudge BackEase→QuarticEase / U4 键盘扇区导航（←→↑↓ 选槽 + 双键对角 + Enter 执行 + 1-9 直达 + PgUp/PgDn 翻页；新增 `RadialKeyboardNavigator`）/ U5 标签激活展开 40→72 / U6 死注释清理 + flick-out 0.2→0.16s。新增 27 测试，全量 **1388/1388**，build 0/0。~~**真机验收通过（2026-09-09 23:0x）：U2 手感 / U4 键盘 / U5 标签展开均 OK**，UX 轨收口。UIA ControlType 变更顺延（E2E 依赖 Custom peer）。调研报告 `Docs/reports/2026-09-09-RADIAL_PANEL_UX_RESEARCH.html`；~~重构轨 R1-R4（MenuSession 拆分）未启动~~（R1 已落地，见下条）。
- [x] **重构轨 R1 已落地（2026-09-09 14:15，grill auto-with-guardrails）**：新增 `Services/WheelGeometry.cs` 单一真相源（500 画布/250 中心/50 默认槽径），收口 `SlotLayoutEngine` / `RadialMenuLayoutCoordinator` / `SlotWheelEditorViewModel` 三处 (250,250) 硬编码（纯重构零行为变化）；S1/ADR-023 测试债清偿——新增 `SlotLayoutEnginePoseSnapshotTests`（9：8/10/12 槽全参数快照 + 全槽位坐标 + HitTest sanity）与 `RadialMenuLayoutCoordinatorTests`（8：GetLayoutMetrics/RebuildSlots/RefreshAnimationTargets/ApplyConfigSlotCountChange），全量 **1405/1405**，build 0/0。**⚠ 已记录未修**：半径双路径分歧（CalculateOptimalLayout 固定-50 → N=10 97.08 vs GetLayoutMetrics 缩放 → 90.61），收敛留待后续 ADR。
- [x] **半径双路径分歧收敛（ADR-030 已 Accepted，闭环 ✅）**：`Docs/decisions/030-wheel-radius-single-formula.md`。~~Path B 胜出方案已实施（2026-09-10 08:4x）~~：`CalculateOptimalLayout` 改用 `CalculateOptimalSlotSize(N)` 推导半径（幽灵半径路径废除，N=10: 97.08→90.61、N=12: 115.91→100.46，N≤9 不变）；pose snapshot 钉死值迁移 + 新增 `RadiusPaths_AreIdentical`（N=6..12 双路径恒等，+7 用例）；build 0/0，全量 **1491/1491**。**真机 sanity 通过（2026-09-10 11:30 用户确认），ADR-030 → Accepted，R1 遗留彻底闭环。**
- [x] **右键手势延迟优化（搁置，2026-09-09 用户裁定不深入，后续有机会再做）**：手势开启时普通右键响应慢 = pending-swallow 设计固有代价（DOWN 被吞、松键后 mouse_event 回放）+ LL 鼠标钩子同步执行订阅者（回调阻塞拖慢全系统输入，UI 繁忙时放大）。非回归（R2 前后 replay 调用点逐字一致）。优化候选方向：(a) 钩子回调极速化/独立线程；(b) 评估回放时序（如 down 即乐观透传 + 特例召回，需重审 LEAK-FIX 语义）。另：Gesture 临时页注册竞态（页面打不开仅警告）与 `e.Handled=true` 崩溃韧性策略亦未决策，随本项一并择机处理。崩溃防御本体已修（`5a2c930`）。
- [x] **重构轨 R2 已落地（2026-09-09 15:45，grill auto-with-guardrails）**：`GestureInputRouter`（新增）迁出右拖手势 claim/promote/replay 编排（MenuSession **3609→3357 行**；公共面 `FeedRightDragGesture`/`FeedGlobalMouseMove` 不变）；新增 15 用例（编排策略首次脱离完整 harness 可测），既有 Leak/Isolation 套件原样通过作行为守护；全量 **1420/1420**，build 0/0。flick-out 判定（一行决策，依赖子菜单 pose）明确不迁（ROI 过低）。**R4 已落地（17:15）：MenuWatchdog 迁出（3357→3317 行），+7 用例，全量 1427/1427，build 0/0。剩余：~~R3 SubMenuTransitionController + CenterIdentityPolicy~~ **R3 已落地（2026-09-09 24:0x，见 journal）——MenuSession 拆分全部完成（R1/R2/R3/R4 ✅，3609 → 2874 行）**。**

- [x] ~~resx 孤儿键独立审计（低成本 change 候选）：全量扫描 1166 键发现 ~296 疑似孤儿，须先排除约定查找键（`SlotParam.*`/`SlotAction.*`/`PluginPermission.*` 等动态拼接前缀）；范围纪律见 ADR-029 Addendum。**审计报告已出（2026-09-09 22:4x，`Docs/reports/2026-09-09-RESX_ORPHAN_AUDIT.html`）**：A 组 83 键高置信死键（PluginPermission.* 51 / Dialog.Permission.* 12 / Settings.Marketplace.* 20）、B 组 6 键（Dialog.PluginLogViewer.*）、父前缀误判嫌疑区 126 键、约定动态保护 56 键。~~ **删除已落地（2026-09-09 23:10，grill auto-with-guardrails）**：A 组 83 + B 组 6（复核确认旧前缀死键，现行用 `PluginLogViewer.*`）= 89 键双语删除，两文件各 1166→1077；全量 1459/1459，build 0/0。~~父前缀嫌疑区 126 键留待下轮逐键核查。~~ **嫌疑区核查已完成（2026-09-10，`Docs/reports/2026-09-10-RESX_SUSPECT_ZONE_VERDICT.html`）：126/126 全部判定死键**（0 存活 / 3 仅测试陈旧引用；动态拼接风险排除——TutorialStepLoader 派生集合实测仅 8 键零交集；数据驱动/ preset 静态键反查零命中）。
- [x] ~~**resx 嫌疑区 126 键删除（待用户裁决）**：核查报告已出，等裁决后按上轮 89 键同流程双语复删（两文件 1077 → 951）+ 全量回归；3 处测试陈旧引用（PresetLocalizationKeysTests.cs:25、OnboardingVerificationTests.cs:114/125）可随批清理。~~ **删除已落地（2026-09-10 08:0x，grill auto-with-guardrails，用户裁决通过）**：126 键双语删除，两 resx 各 1077→951（XML 级对账：删除集合=键集、保留 value 零变化、EN/ZH 键集平行）；3 处测试陈旧引用同步清理；build 0/0，全量 **1484/1484** 与基线一致。resx 孤儿键治理全程收口（审计→A/B 组 89 键→嫌疑区 126 键）。
- [x] **tab 指示器「切到第一个 tab 无动画」修复（2026-09-10 11:5x）**：根因 = 事件驱动的延迟重定位（`RepositionNavIndicator`，DPI / 窗格开合 / 面板尺寸 / 导航项重建的公共入口）**缺 `_isNavAnimating` 门**——`InitializeNavIndicator` 的 `clearHeldAnimations:true` 把在跑的 stretch 动画整段摘掉（追踪：`PHASE1` 后 **1ms** 出现 `[SNAP] caller=InitializeNavIndicator clearAnim=True animating=True`）→ 指示器 1 帧瞬移；**「第一个 tab」是伪线索**（真变量 = 哪次导航恰好带出布局事件；用户环境为 `Window.DpiChanged` 23ms 内连发 7 次）。同轮修次生缺陷：phase1/2 改**显式 `From`**（原 `From=null` 隐式 origin 会被并发写入的基础值改写 → 先跳后滑），顺带消除 `Height` NaN 取值崩溃通道；两条 in-animation 退化路径显式解锁 `_isNavAnimating` 后再 defer。验证（自驱动、无头可用）：E2E 真实鼠标驱动 8 次导航 + `SetWindowPos` 注入 280 次 `SizeChanged`（与追踪逐条对应），`clearAnim=True animating=True` **3→0**、`PHASE2 atH=22.0`（零位移）**2→0**、守卫命中 **36/280**、8/8 走完两段；build 0/0、全量 **1492/1492**、拆除诊断后最终产物 E2E PASS。`Docs/lessons/WPF_INDICATOR_ANIMATION_TORN_BY_LAYOUT_EVENT.md`（新增）。**真机验收通过（2026-09-10 12:0x 用户手动确认：切 tab 指示器动画正常，瞬态未复现）→ 修复闭环。** **未决观察（非阻塞）**：用户环境 `Window.DpiChanged` 在**目的地是含轮盘预览的页**时连发（`→ Slots` 7 次 / `→ slot-editor` 2 次；出发页无关：`Slots → Plugins/Appearance/Analytics` 均 0 次），同机自动化跑同样点击恒 0 次 → 差异候选＝自动化窗口未激活（未验证）；来源未解（已排除「页面尺寸撑大窗口」；修复后无害，动画期间被丢弃，仅剩 7 次空转回调）。收口方式：`OnWindowDpiChanged` 记 `e.OldDpi/e.NewDpi`，一次复现即可判伪事件。
- [x] **技能同步收口（2026-09-10 12:2x，用户裁定）**：**OpenCode 直接读 `.agents/skills/`**（官方文档列为 project「agent-compatible」路径，与 `.opencode/skills/`、`.claude/skills/` 并列）→ 无需第三份镜像；`.opencode/skills/` 现存 `openspec-*` / `session-journal` 属历史遗留，不跟风。`AGENTS.md` §4 路由表已加一行入口指针（页脚不动，沿纯 §4 改动惯例）；`references/pulsar-harness.md` 增 §7「技能发现路径」。**结论固化：`pulsar-ui-runtime-verification` 现有两份副本（`~/.workbuddy/skills/` + 仓库 `.agents/skills/`），改一处须同步另一处且 LF。**

- [x] **E2E 低干扰模式落地（2026-09-10 15:5x，用户批准计划后实施）**：新增 `--low-interference`（opt-in，默认关闭）——① `UiaDriver.ClickElement` 优先 UIA `InvokePattern`（`DataItem`/`ListItem` 回退 `SelectionItemPattern`），不移动物理光标、不需前台激活；② `DebugCommandServer.open-settings` 以 `ShowActivated=false` 显示，不抢焦点；③ ⚠️ **实测发现 UIA 模式无法触发鼠标事件驱动的导航**（`NavigationItem_PreviewMouseLeftButtonUp` 不在 UIA 通道）→ 新增调试命令 `nav-settings`（`SettingsWindow.NavigateToPageAsync` 复用 `_shellViewModel.NavigateAsync` 汇聚点），工作流导航步骤由 `click` 改 `command`。验证：build 0/0；全量 **1500/1500**（1494+6）；低干扰 E2E `settings-analytics-data-dark` **PASS 24.0s**（14 步）；InvokePattern 对 Button 类控件实证 PASS（临时探针，已清理）。新增 `Docs/lessons/E2E_LOW_INTERFERENCE_MODE.md`。**未 commit**（push 由用户执行）。

- [x] **插件使用详情「下钻」对话框落地（2026-09-10 16:3x，用户批准计划后实施）**：分析页使用统计行支持**整行点击** + 行内**详情按钮**（用户选定「整行可点 + 详情按钮」与「复用迷你柱状图放大」）打开单插件详情对话框——头部（图标/名称/ID/版本/作者/描述/层级）、4 项汇总度量、近 7 天趋势柱（`BarHeight ×5` → 72px）、槽位分布（按槽位号排序，占比以 `AnalyticsItem.TotalExecutions` 为分母）、模式分布、单插件推荐。数据全部复用分析页内存快照（零新查询/零副作用）；元数据与推荐为**可选注入**，缺失时优雅降级隐藏区块。新增 `PluginAnalyticsDetailViewModel` + `PluginAnalyticsDetailContent`（XAML 内容 + Host，注册 `DialogTemplates.xaml`）、9 个双语 resx 键、测试 21 例（详情 VM 18 + 分析页下钻命令 3）。验证：build 0/0；全量 **1521/1521**（1500+21）；低干扰 E2E `settings-analytics-detail-dark` **PASS 27.6s**（16 步）——UIA 树确认对话框全内容渲染（54 次/96.3%/120ms/4d ago、7 根趋势柱、#1 30 56% / #2 24 44%、Task(30)）。
  - ⚠️ **两点环境观察（非代码缺陷）**：① E2E `screenshot` 步骤（`Capture.CaptureScreen` GDI）在**桌面会话不可用/锁屏**时抛 `Win32Exception(6) 句柄无效`，同轮 `OpenInputDesktop` 预检也会拦下——`settings-analytics-detail-dark` 已**移除 screenshot 步骤**（UIA dump 才是权威断言），避免非功能步骤造成假 FAIL；② 重跑时需桌面已登录且未锁屏。

- [x] **PKI 模块重命名为 Secret Fill（2026-09-10 17:4x，grill auto-with-guardrails，ADR-031）**：`PKI` 全称 **Public Key Infrastructure**，而本模块是 DPAPI 凭据存储 + SendKeys 注入（零证书、零非对称密钥对）——名字语义错误。**层 1/2/4 改名，层 3 逐字冻结**：目录 `Plugins/Core/{Pki→SecretFill}/`、命名空间 `Pulsar.Plugins.Core.SecretFill`、`PkiPlugin→SecretFillPlugin` / `IPkiSecretStore→ISecretStore` / `PkiExecution*→SecretFillExecution*` 等（70 文件）；**`com.pulsar.pki` 保持逐字不变**（用户 `Profiles.json` 5 处 slot `plugin` 值 = 冻结契约，`secrets.json` 0 处无需迁移）。**历史记录不改写**（`CHANGELOG`/`RELEASE_NOTES`/`Docs/archive`/`Docs/journal`/`openspec/changes`）；`CONTEXT.md` 术语表新增 `Secret Fill`（`_Avoid_: PKI`）+ `Plugin Id` 条目。验证：build **0/0**；全量 **1521/1521**（与基线一致）；**层3 三重计数恒等**（源码树 59→59 / 用户 Profiles.json 5→5 / PluginUsageStats 1→1）。新增 `Docs/decisions/031-secret-fill-rename-plugin-id-frozen.md`。**未 commit**（push 由用户执行）。
  - **层3（`com.pulsar.pki` → 新 ID）明确不做**：收益为零（用户只看得到 `AutoFill` 显示名），成本是配置迁移 + 旧 ID 别名读路径；等有插件市场/外部消费者时再议（是待办候选）。

- [x] **架构审查候选 W1 已落地（2026-09-10 19:1x，grill auto + TDD）**：`WinSwitcherPlugin.LaunchApplicationAsync` 直调 `Process.Start` → 改走既有 `IProcessLauncher` seam（ADR-012；`Initialize` GetService + fail-fast）；ExecuteAsync 决策树（launch 验证/异常映射、switch-or-launch 回退、activate）新增 15 用例首次 Moq 可测（红 6 → 绿 25/25）。build 0/0，全量 **1556/1556**。`IWindowActivationService.LaunchApplicationAsync` 实证**零调用方**，判范围外 → 删除候选见下条。W2/W3/W4 候选未开。
- [x] **架构审查候选 W2 已落地（2026-09-10 20:4x，grill auto-with-guardrails + TDD）**：新增 `IDiscoveryExclusionPolicy`/`DiscoveryExclusionPolicy`（`Services/WindowSwitching/`）为排除策略单一所有者（三键解析/应用/持久化一处）；`IWindowDiscoveryService` shed 4 成员（UpdateBlacklist/UpdateEligibilityRules/SetSwitchDiagnosticsEnabled/GetEligibilityRules）回归纯枚举；WinSwitcherPlugin 去 relay（影子状态删除）；Inspector 改道 `SetRulesAsync`；registry 黑名单死路径 4 方法删除（零调用方，报告 Writer 2 实为死代码——事实纠正见 journal）；bootstrap 于 `LoadCoreAsync` 前（顺序测试钉死）；OnDisable 不再清黑名单（不对称消除）。+13 policy 用例 +1 guard，build 0/0，全量 **1569/1569**。CONTEXT.md 新术语 **Discovery Exclusion Policy**。审查候选 W3/W4 未开。**未 commit**（用户处理）。
- [x] **架构审查候选 W3 已落地（2026-09-10 21:4x，grill auto / full-auto + TDD）**：WinSwitcher 三动作 5 个手写 Slot 参数块收拢为唯一参数规范——新增 `WinSwitcherPlugin.Metadata.cs`（partial：GetMetadata + 3 动作工厂 + 参数工厂），主文件 601 → ~340 行；`app` ×2 的 1 词描述漂移统一（仅编辑器英文文案，`SlotParam.*` 4 键 resx 零变更）。审查两处偏差纠正（path 实为有意双变体非拷贝；行数收益 155→~200 非审查宣称的 ~60）。参数级 canonical spec 测试红先行（BeEquivalentTo 恰好抓到既有漂移）→ 永久防漂移网。build 0/0，全量 **1570/1570**（基线 1569 + 1）。**未 commit**（用户处理）。W4（Speculative，建议与 W2 遗留捆绑评估）未开。
- [x] ~~**接口删除候选（低成本轮）**：`IWindowActivationService.LaunchApplicationAsync` 零调用方——连同实现一起删 + 顺带复查 facade 活跃度。~~ **已落地（2026-09-10 22:3x，用户裁决"删 #1-#6，#7 另行"）**：`LaunchApplicationAsync` + 复查新发现 6 个零调用 facade 成员一并删除（`FocusWindow`、`GetForegroundWindow`(WindowInfo 版)、`TryGetCachedActiveWindows`、`PreWarmWindowInventory`、`GetFocusRestoreMode`、`RestoreFocus`，接口成员 + WindowService 实现成对删）——W4 facade 收缩的机械部分首刀。全部经全仓 grep（生产/测试/Simulator）证实零调用；build 0/0，全量 **1570/1570** 与基线一致（零测试引用 = 零调用方实证）。详见 journal。
- [x] ~~**接口删除候选二（#7，待裁决）**：`IWindowDiscoveryService.GetRunningProcessNamesAsync`（接口成员 + WindowService 转发）零调用方；内层 `IWindowInventoryService.GetRunningProcessNamesAsync`（`WindowInventoryService.cs:142`）同样零生产调用方（仅 `WindowInventoryServiceTests` 1 个用例直测）——可能整链路死（两层接口 + 两实现 + 测试）。删内层需连同其测试一起删。~~ **整链路删除已落地（2026-09-10 22:5x，用户裁决）**：5 件出清（两接口成员 + WindowService 转发 + WindowInventoryService 纯投影实现 + 弱化重复测试）；实现是存活兄弟 `GetRunningProcessesAsync` 之上的纯 LINQ 投影（零逻辑损失），被删测试是同文件 ：52 存活测试的严格弱化重复（零覆盖损失）。build 0/0，全量 **1569/1569**（= 1570 − 1 删除测试，对账精确）。详见 journal。
- [x] **架构审查候选 W4 已落地（2026-09-10 23:1x，grill auto-with-guardrails）**：facade 收缩重指向——生产 10 直连消费方全部改挂窄接口：S×4（RadialMenuWindow / SendKeysInjectionExecutor / VbaRunner / BookmarkletRunner）、F×2（MenuSession / `PulsarContext.Capture` 参数）、A×1（WinSwitcherPlugin Initialize fail-fast）、D×4（WindowInspectorViewModel + PluginSettingsDialogViewModel + PluginViewModel + PluginManagerViewModel），字段/参数重命名诚实化（`_shellService`/`_windowFocusContext`/`_discoveryService`/`_windowActivationService`）。**裁决修正（Q2）**：SlotStrategies 两策略 + WindowSwitchSubMenuStrategy 实证为 F+A 真 facade 消费方，双窄接口拆分方案（原推荐）会令所有构造点出现同实例双参丑态 → 回归审查原设计保留 aggregate（`IWindowService` 注释已钉死 facade 消费方清单 = 策略 3 + 透传 2 + Simulator + DI 锚点）。**坑**：Q4 "测试零改动" 只对 ctor 注入式成立——GetService 式插件测试 fixture 需补窄接口 setup（WinSwitcher 3 处 / BookmarkletRunner 3 处，21 红一次修净）。build 0/0，全量 **1569/1569** 与基线一致。`b5945cc` 已入库。
- [x] ~~**`IWindowService` 接口解散候选**~~ **已关闭（2026-09-10 23:3x，用户裁决"保留"）+ 前提勘误**：候选原记"零业务消费方，仅 DI 锚点"**不实**——终检 grep 实证 5 个业务消费方（F+A 真 facade 消费方）+ Simulator + DI 锚点。解散需 5 文件重类型（同实例双参丑态）或第 5 个组合角色接口（过早抽象），收益仅表面纯度 → 裁决保留，ADR-010 "facade can shrink" 已兑现至不可再缩核心。**教训：记录"零消费方"前必须全仓 grep 对账。**

- [ ] **⚠️ 轮盘贴边校正（2026-09-11 落地）真机验收**：开关关掉后中心严格贴光标（不再跳变）、边距滑块手感、以及**已知边界**——视口窗口 = 工作区（`rcWork`），故关闭校正/边距过小时靠任务栏一侧的轮盘会被**窗口裁切**（不是屏幕外看不见，是被窗口边界切掉）。验收判据：① 关校正 + 在屏幕四角与任务栏边缘唤出 → 观察轮盘裁切是否可接受；② 边距 260（默认）与旧行为逐帧一致；③ 边距 120 → 更贴边但可能裁切。若 ① 不可接受 → 追加独立改动：视口改用 `rcMonitor`（影响面仅 `IsPointInActiveViewport` 的点击吞并区域）。build 0/0、全量 **1576/1576**（+7）。已本地 commit（push 由用户执行）。
- [ ] **（可选）openspec change 与 spec 条目**：本次是新增用户可见能力（视口定位策略），按仓库惯例（`gesture-isolation-filter` / `flick-out-cancel` 等均有 spec + archive change）本应走 `/opsx-propose`。本次直接实现未建 change —— 待用户决定是否补建提案，以及是否在 `openspec/specs/radial-menu/spec.md`（或新建 viewport 类 spec）登记「贴边校正 SHALL 可配置」需求。

## 已完成（历史保留）

