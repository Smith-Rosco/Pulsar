# 2026-09-12 Journal

> ADR-019：会话正文的唯一来源。本文件只记「做了什么 / 关键决策与坑 / 验证 / 引用」，细节见 CHANGELOG 与各 ADR。

## Session (00:2x · 架构审查 round 10 —— `improve-codebase-architecture` skill，grill auto，无 HTML 报告)

**用户指令（授权自主推进）**
- 跳过 HTML 报告环节，直接 `grill auto` 完成**收益最大的几项**。
- 减少中途提问；**关键步骤各自 commit**，全程可回溯。
- 承接昨日 round 9 的收尾状态：#5 已落地、#47/#48 真机验收通过、#87 已修；NEXT 仅剩 #6（orb pose 抽出视觉树）/ #7（导航与页面回收搬出 SettingsWindow code-behind）/ Secret 删除语义待裁定。

**本轮取舍**
- 按 skill 要求先探索热点（`git log` + 子代理并行扫描 + 自我验证），再自行裁决推荐序（auto 模式），不逐项请示。
- 实施纪律沿用本仓既有：先侦察后动工、删除类改动必须有「零读取点 / 零调用点」实证、纯删除走「编译器枚举 + 写回一处」红证明、迁移类保留守卫、每步 build 0/0 + 全量测试精确对账。

## Session (08:0x · 架构审查 round 11 —— 落地下轮候选 #1 / #2)

**用户指令**
- 「继续推进 1、2」= round 10 推荐序的前两项：① PluginSettingViewModel 校验规则三份复制（Strong）② PluginSettingsDialogViewModel 跨接缝泄漏 + async void OnSettingChanged（Strong）。

**做了什么**
- **#1 校验骨架收敛（39a61d6）**：String / Secret / Path 三个子类 `Validate()` 各持一份同构阶梯（String 与 Secret 连 resx 键组都逐字相同），四个文本 getter（`StringValue`/`SecretValue`/`PathValue`/`SelectedOption`）也是同一取值形态的复制。收敛为基类 `ValidateText(...)`（结构单 owner，键组与 checkPattern 作参数）+ `TextValue`，净删 39 行。守卫 7 例。
- **#2 接缝修复（bd07122）**：① 对话框 VM 改构造注入 `IDialogService?`，`PluginViewModel.DialogService` 公开属性删除（唯一读者就是挖取点）；② `async void OnSettingChanged` 拆为「事件薄壳（整体 try/catch）+ `PersistSettingAsync`（可 await）」，ProcessBlacklist 确认路径两个调用点改 `await`——顺带修掉 fire-and-forget 与 `LoadSettings` 重建的竞态。守卫 3 例（含 async void 清单反射钉）。

**关键决策 / 坑**
- **行为保持测试无法自己证明会咬 → 补变异验证**：#1 的 7 例在旧代码上也会绿（钉的是行为等价），故做了一次变异（短路 MinLength 检查）确认恰好 5 条失败且全部落在 MinLength 依赖链上——「测试会咬」的唯一证据。
- **#2 守卫的标准红证明路径**：先写守卫测试 → 在旧代码上窄跑 → 恰好 2 条失败（构造签名 / 公开属性）→ 改代码 → 复绿。第三例（async void 清单）改前即绿，因为钉的是「事件壳存在且唯一」这个终态。
- **⚠ bash 里误用 PowerShell 的 `*>` 重定向**：`*> log` 在 bash 中 = glob 展开 + `>`，把当前目录全部条目追加成 `dotnet test` 的参数 → MSB1008「只能指定一个项目」，且错误消息里「响应文件追加的开关」极具误导性（根本不是 rsp 文件的问题）。bash 一律 `> log 2>&1`。
- **持久化顺序修复的行为语义**：Configure 里改 `await PersistSettingAsync` 后，确认路径会等两次持久化完成再 `LoadSettings` 重建——这是有意的行为修复（原 fire-and-forget 与重建交错），非副作用。
- **#2 的行为级测试（搭 PluginViewModel 全家桶 mock）判为成本过高放弃**：descriptor + metadata + registry + runtimeOps + 真 ConfigService 的搭建远超收益，结构守卫（构造签名反射）+ 编译器（Task 必须被 await 或 CS4014）已覆盖主要回潮面。

**验证**
- build 0 警告 0 错误；全量 **1636/1636**（基线 1626 + 10 新守卫，精确对账）。
- 红证明：#2 改前恰好 2 条失败；#1 变异恰好 5 条失败（全在 MinLength 依赖链）。

**相关引用**
- 修改：`ViewModels/Settings/PluginSettingViewModel.cs`（+61/−100）、`ViewModels/Settings/PluginViewModel.cs`（+25/−6）、`ViewModels/Dialogs/PluginSettingsDialogViewModel.cs`（+6/−4）；新增 `Pulsar.Tests/ViewModels/Settings/PluginSettingValidationTests.cs`、`PluginSettingsDialogSeamTests.cs`；`CHANGELOG.md`。
- 未决 / 待办：下轮候选剩 ③ 临时页组合 id `slot-editor:<ctx>:<no>` 手写 6 处（Worth exploring）、④ #6 orb pose 抽出 / #7 SettingsWindow code-behind / Secret 删除语义。commit 10 个未 push（push 归用户）。
