# 2026-09-06

> 本日前序会话（10:28–16:47，cascade 子轮盘重构 / D10-D11 / CS8604 清理等）已轮转至 [archive/2026-09-06.md](./archive/2026-09-06.md)（ADR-021，文件超 15KB 上限）。

---

## Session (17:42 · openspec repositioning Phase 2：宣传截图 E2E 管线 + 发布资产目录)

**做了什么**
- 完成 Phase 2：可复现宣传截图 E2E 管线 + 4 张首屏用图 + `Docs/media/release/` 发布资产目录约定。
- 菜单居中三次迭代：FlaUI `Mouse.MoveTo` 本环境不生效 → Win32 `SetCursorPos` 仍不居中（`_invocationPointScreen` 被 `IsVisible=false` setter 重置 / 光标被 UI 操作移回）→ 最终给 `DebugCommandServer` 加 `set-invocation-point` 命令直接设定召唤屏幕坐标。promo-release-9 PASS 30.7s，像素质心 (1260,734) 确认居中。
- 干净背景：纯黑壁纸 + MinimizeAll 一体化脚本 `run-promo-wallpaper.ps1`（设壁纸→MinimizeAll→跑 E2E→finally 恢复原壁纸），自检中心像素 (13,13,13)。WinForms 全屏垫底窗口被 Chrome 恢复盖住，用户明确否决。
- 产出 4 张 1920×1080 PNG：01 主界面（Plugins 页，Phase 1 新显示名）/ 02 轮盘呼出（8 办公槽居中）/ 03 Excel 跑宏瞬间（slot 1 悬停高亮 + "一键跑宏"提示）/ 04 窗口切换子菜单（Task 模式 6 槽）。
- `.gitignore` 从 `Docs/media/` 改为 `Docs/media/*` + `!Docs/media/release/` 例外；`Docs/media/release/README.md` 明确三方边界（release/ 入 git / Docs/media/ 其他子目录忽略 / E2E artifacts/ 不入 git）+ 命名约定 + 再生步骤 + review checklist。
- tasks.md 勾选 2.1/2.2/2.3；CHANGELOG 追加 Phase 2 条目；本日志超 15KB 轮转至 archive/（ADR-021）。

**关键决策 / 坑**
- 菜单居中可靠路径 = `set-invocation-point` 直接设 `_invocationPointScreen`，而非移动光标。`IsVisible=false` setter 会重置 `_invocationPointScreen=null`；E2E 步骤间光标可能被 UI 操作（click nav / Alt+F4）移回。set-invocation-point 在 menu-open 前 1ms 执行，值不被重置。
- 像素质心分析比肉眼判断位置可靠——缩放显示导致视觉偏差（菜单看似左上，实际 centroid 居中）。
- E2E 整屏 `CopyFromScreen`（2560×1440 物理，150% DPI）无法排除桌面背景，必须前置清背景。

**相关引用**
- 变更: openspec/changes/2026-09-05-repositioning-narrative-rollout/
- 文件: Docs/media/release/README.md, Pulsar/Pulsar.E2E/Fixtures/promo/office-workbench-dark.json, Pulsar/Pulsar.E2E/Workflows/promo-release-screens.json
- E2E 基建: Pulsar/Pulsar/Services/DebugCommandServer.cs (set-invocation-point), Pulsar/Pulsar/ViewModels/RadialMenuViewModel.cs (DebugSetInvocationPointScreen), Pulsar/Pulsar.E2E/Driver/InputDriver.cs (MoveTo/SetCursorPos)

---

## Session (18:2x · openspec 四 change 安全批次：更新纯逻辑层 + 手册 + installer 脚本 + README 补齐)

**做了什么**
- **in-app-auto-update 第 1、2 节 + 4.1（全程 fake 单测，零真实网络/零唤起应用）**：`IUpdateHttpGateway`（`Services/Interfaces/`）+ `Services/Updates/` 五件套——`UpdateVersionComparer`（三态语义比较，容忍 v 前缀/脏字符串）、`GitHubReleaseInfoParser`（REST JSON + Atom XML + 302 Location 三解析）、`UpdateCheckService`（API 5s→Atom 6s→302 探测 5s 顺序降级短路）、`UpdateDownloadService`（官方+镜像故障转移、Accept 头隔离、SHA256/大小/未验证三档完整性、失败删残留）、`HttpClientUpdateGateway`（双 client：跟随/不跟随重定向；OwnedContentStream 转移 response 所有权）。测试 5 文件 84 用例全绿。
- **user-manual 1.1–1.4 + 2.1–2.2**：`Docs/manual/`（目录 + zh/en 各 5 章：安装首启/跑宏/登旧系统/切窗口/FAQ，截图复用 release 资产）；`Docs/ops/RELEASE_CHECKLIST.md`；接线 = About 页新增「用户手册」行（`OpenUserManualCommand` + resx 双语键）+ 首启引导完成页超链接（`TutorialStep.LinkUrl/LinkTextKey` + 卡片 Hyperlink + step6 接线 + `Tutorial.OpenUserManual`）+ README 文档区链接。
- **installer 1.1/1.2/2.1–2.3 + 4.1/4.2**：csproj 新增 `RuntimeIdentifier` 条件化 publish 属性（禁裁剪注释）；`dev.ps1` 新增 `publish` 子命令（publish→Standalone zip→ISCC 存在时产 Setup.exe→SHA256SUMS.txt，幂等清 stage）；`scripts/installer/pulsar.iss`（autopf、可选自启、卸载保留 `%AppData%\Pulsar` 且文案明示）；BUILD_AND_RUN 新增「Distribution Packaging」章节；ADR-026（Inno vs WiX 选型 + 禁裁剪）。
- **repositioning 3.1 + 4.1/4.2/4.3 + 5.3**：三支 demo 视频脚本（分镜+台词，`Docs/media/release/videos/demo-video-scripts.md`）；README 中英补齐差异化话术（Quicker/Flow 口径取自重定位方案 §1.2）、4 张真实截图替换占位、AI 愿景 roadmap 节、手册链接。
- **ADR-025**：三级容灾更新架构（出处注明 StarPie MIT）。
- **统一验证（bash + env 前缀 dotnet）**：build 0 警告 0 错误；全量测试 **1227/1227 通过**（基线 1143 + 新增 84）；Release publish 实跑验证见下。

**关键决策 / 坑**
- `TryTierAsync` 状态码守卫初版 `<200 or >=300` 会把 302 探测层一刀切掉——改为 `<200 or >=400`，302 放行给 redirect 层自行判定；REST/Atom 层收到 3xx 会因 body 解析失败自然降级。
- fake gateway 按「URL 片段」路由在镜像场景失效：镜像 URL 内嵌完整官方 URL，任何官方片段都匹配得到镜像请求。改为两段式路由：先 Host 精确匹配，再 PathAndQuery 片段匹配。
- GitHub Atom 条目 id 是 `tag:github.com,2008:https://...` 形式（tag: URI），`Uri.Segments` 解析不可靠，改 `/releases/tag/` 子串定位；`TryParseRedirectLocation` 同样要求该 marker，防 `https://example.com/other` 被误判为版本。
- xUnit1012：InlineData 传 null 需参数声明 `string?`。
- 教程完成页外链选择扩展 `TutorialStep` 模型（`LinkUrl`/`LinkTextKey`）而非新增 PrimaryAction 枚举值——不触碰 E2E 依赖的按钮流程。

**未完成 / 遗留**
- user-manual 3.1（Release 说明成稿，待 Change 4 产物定名后再写）与 3.2–3.4（需真实发布动作）。
- in-app-auto-update 3.x（DI/启动/About UI 集成）与 4.3（真实网络 QA + 测试 tag 全链路）。
- installer 1.3/2.4/3.1–3.3（需干净环境/真实安装/双实例冒烟，全部被「不唤起应用」约束排除）。
- repositioning 3.2/3.3（视频录制与入库）。
- ISCC 未安装时 Setup.exe 路径未实机验证（脚本逻辑非致命跳过）。

**相关引用**
- ADR: Docs/decisions/025-in-app-auto-update-three-tier-check.md, 026-installer-inno-setup-no-trim.md
- 代码: Pulsar/Pulsar/Services/Updates/（5 文件）, Services/Interfaces/IUpdateHttpGateway.cs, AboutViewModel.cs, SettingsAboutPage.xaml, Features/Tutorial/（Step/Card/Loader）
- 脚本: scripts/dev.ps1 (publish), scripts/installer/pulsar.iss
- 文档: Docs/manual/（11 文件）, Docs/ops/RELEASE_CHECKLIST.md, BUILD_AND_RUN.md, README.md / README_EN.md, Docs/media/release/videos/demo-video-scripts.md
- 测试: Pulsar.Tests/Services/Updates/（5 文件 84 用例）

---

## Session (18:55 · 设置页 UI 精简：保存按钮图标+文字 + 长文本描述入 Tooltip)

**做了什么**
- NEXT.md 中用户手动添加的待办：保存按钮改为图标+文字"保存"；长文本描述精简或藏入 tooltip。
- `SettingsWindow.xaml`：TitleBar 右侧保存按钮新增 `Content="{lex:Locale SettingsWindow.SaveChanges}"`（Save Changes / 保存更改），原 Icon 保留，Badge 小红点不变。
- `SettingsGeneralPage.xaml`：4 处 SettingsRow 新增 `DescriptionToolTip`——QuickSwitchTimeout / RightDragSwitcherModifier / RightDragActionModifier / Gesture.EnableToggle。
- `Strings.resx` + `Strings.zh-CN.resx`：上述 4 处 Description 精简为短语（EN 6–8 词 / ZH 10–14 字），原完整描述移入新增的 `*DescriptionDetail` 键。其余 SettingsRow 已有 Description+DescriptionToolTip 模式，独立 TextBlock（SlotsPerPage/CleanCache/Gesture 副标题）主文本已简洁且 ToolTip 装详情，保留不动。
- build 0 警告 0 错误；全量测试 1227/1227 通过（24s）。

**关键决策 / 坑**
- 沿用仓库已有 `Description`（短）+ `DescriptionToolTip`（详情）双键模式，不引入新控件或新布局。
- 保存按钮在 TitleBar.TrailingContent 中，加 Content 后按钮变宽但 1000px 窗口 TitleBar 右侧空间充足；Badge 用 Grid 叠加 HorizontalAlignment=Right，不受按钮宽度影响。

**相关引用**
- 文件: Pulsar/Pulsar/Views/SettingsWindow.xaml, Pulsar/Pulsar/Views/Pages/SettingsGeneralPage.xaml, Pulsar/Pulsar/Resources/Strings.resx, Strings.zh-CN.resx
- commit: `3b9155b`

---

## Session (19:02 · MenuSession.cs Debug 跟踪日志清理)

**做了什么**
- NEXT.md 可选清理项：移除 `MenuSession.cs` 中全部 7 条 `_logger?.LogDebug` 跟踪日志。
- CLICK-TRACE ×3（HandleGlobalMouseClickAsync 入口/布局/槽位解析）、EXEC-TRACE ×2（ExecuteSelectionAsync 入口/槽位解析）、GEOMETRY-TRACE ×1（级联布局 pose 输出）、VISUAL-TRACE ×1（子菜单 bloom 动画中心状态）。
- 删除后确认无残留未使用变量（parentCenterX/Y/parentSlot 为方法参数，在动画逻辑中仍使用；cascade/pose/positions 在布局计算中仍使用）。
- build 0 警告 0 错误；全量测试 1227/1227 通过（27s）。

**关键决策 / 坑**
- 仅删日志调用，不触碰任何业务逻辑；trace 中引用的变量均为方法参数或前置计算结果，删除后无副作用。
- GEOMETRY-TRACE 日志字符串中含一个非 ASCII 字符（"掳"，应为 ° 的编码异常），删除后一并消除。

**相关引用**
- 文件: Pulsar/Pulsar/ViewModels/MenuSession.cs
- commit: `31c4b3c`
