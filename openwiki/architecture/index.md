# Files

- [Configuration & Persistence System](config-system.md) - How Profiles.json acts as the single source of truth — the ProfilesConfig model, deep-copy snapshot reads, revision-guarded ConfigEditSession writes, the validation pipeline, and versioned ZIP backup/restore with portable password-sealed secrets.
- [Dialog System Architecture](dialog-system.md) - How Pulsar hosts every dialog in one FluentWindow — the DialogService show/close contract, DialogHostViewModel footer/wizard mode, DialogSizeConstraints presets, implicit DataTemplate resolution for content view models, per-dialog theme injection after InitializeComponent, and the slot/secret/picker dialog catalog.
- [Plugin System Architecture](plugin-system.md) - The tiered plugin runtime — core plugins fail fast and cannot be disabled while extension plugins are crash-isolated behind a circuit breaker — the three narrow DI seams over the single PluginRuntimeKernel implementation, the validated lifecycle state machine, the ordered execution pipeline, plugin contracts and manifest metadata, and collectible-ALC loading for external packages.
- [Radial Menu Session & Interaction Runtime](radial-menu-session.md) - How one radial-menu invocation runs as a pure-logic MenuSession state machine — hotkey and right-drag summon sources, hover/paging/submenu-morph state, cascade submenu strategies and page providers, layout geometry engines, viewport placement, and the dismiss-fade timing contract that prevents re-triggering hooks during plugin execution.
- [Settings Shell & Slot Editor](settings-and-slot-editor.md)
