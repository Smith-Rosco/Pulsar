## Design Context

### Users

Windows 高频生产力用户（power user）。每天唤起径向菜单数百次，靠肌肉记忆和空间定位操作，而非阅读界面。

- **使用场景**：全屏工作中途唤起 → 滑向某个方位 → 松开 → 立即回到原任务。单次交互的预算以毫秒计，界面必须在被调用时"已经在那儿"。
- **要完成的任务**：用最短路径触发高频动作（切换窗口、执行命令、注入凭据、运行脚本）。
- **关键约束**：用户不会阅读界面，只会扫视。任何需要"看清再判断"的设计都是成本。

### Brand Personality

**隐形 · 精密 · 顺从系统**

不做独立品牌视觉表达。Pulsar 的目标是成为"最像 Windows 原生的那一个" —— 用户不应该感觉到这是第三方应用。

### Aesthetic Direction

**跟随系统 Fluent Design（Windows 11）**

- 视觉语言完全对齐 WPF.Ui 4.3.0 提供的 Fluent 令牌体系
- 强调色跟随用户系统设置（`SystemAccentColorBrush`），不硬编码品牌色
- 背景材质使用 Mica / Acrylic，与系统窗口观感统一
- 明暗双主题**对等实现**，浅色不是深色的降级模式

**反参考（明确不要）**：VS Code 默认灰阶 · Windows 系统蓝 `#0078D7` 硬编码 · 独立品牌配色 · 彩色 Emoji 装饰 · 玻璃拟态滥用

### Design Principles

1. **系统一致性优先于品牌表达** — 用户已经熟悉 Fluent，发明新语言只会增加学习成本。需要视觉决策时，先问"原生 Windows 会怎么做"。
2. **令牌是唯一真相源** — Fluent 令牌体系外不允许出现裸值（hex、FontSize、Duration、CornerRadius、Spacing）。历史上 `Pulsar.Spacing.*` 与 `Pulsar.Animation.Duration.*` 定义齐全却零引用，这个错误不得重演：先接管道，再做视觉。
3. **动效服务于速度感，不服务于表演** — 一律指数缓动（`ease-out-quart` / `ease-out-expo`），让元素尽快抵达终态。禁止回弹/弹性缓动 —— 用户已经知道答案了，动画不该还在晃。禁止常驻循环动画（用户靠空间记忆定位，不需要闪烁提示）。
4. **明暗主题对等，浅色不是降级** — 任何在深色靠发光实现的反馈，在浅色必须有等效的不依赖发光的通道（描边加粗、填充提亮、外圈环）。曾经 `Theme.Orb.Active.Glow` 在浅色主题是全透明，导致核心交互反馈消失，这类"关掉能力"的做法被禁止。
5. **每个状态变化都必须可感知** — 激活、悬停、按下、禁用、未保存、空、加载、错误，每一种状态都要有明确且不依赖单一通道（不能只靠颜色）的视觉响应。

### 技术基线

- .NET 8.0 WPF + WinForms，WPF-UI (lepoco) 4.3.0
- 多窗口/多页需手动 `IThemeService.ApplyTheme()`（Page 必须在 `InitializeComponent()` **之后**调用）
- 本地化全覆盖：`{lex:Locale Key}` / `_loc["Key"]`，禁止硬编码用户可见字符串
- 按钮样式用 `PulsarPrimaryButtonStyle` / `PulsarSecondaryButtonStyle` / `PulsarDangerButtonStyle`，禁止 `Appearance="Primary"`
