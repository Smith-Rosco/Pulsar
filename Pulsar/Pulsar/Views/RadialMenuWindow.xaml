<Window x:Class="Pulsar.Views.RadialMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Pulsar.Views.Controls"
        xmlns:vm="clr-namespace:Pulsar.ViewModels"
        mc:Ignorable="d"
        Title="Pulsar Overlay"
        Height="400" Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Grid>
        <Canvas x:Name="MenuCanvas"
                Width="400" Height="400"
                HorizontalAlignment="Center" VerticalAlignment="Center">

            <ItemsControl ItemsSource="{Binding Slots}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}"/>
                        <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:JellyOrb 
                            IconKey="{Binding IconKey}"
                            Label="{Binding Label}"
                            IsActive="{Binding IsActive}"
                            Size="{Binding Size}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <controls:JellyOrb 
                Canvas.Left="{Binding CenterSlot.X}"
                Canvas.Top="{Binding CenterSlot.Y}"
                Size="{Binding CenterSlot.Size}"
                IsActive="{Binding CenterSlot.IsActive}"
                Label="{Binding CenterSlot.Label}"
                IconKey="{Binding CenterSlot.IconKey}"/>

            <TextBlock Text="{Binding CenterText}"
                       Canvas.Left="0" Canvas.Top="350" Width="400"
                       TextAlignment="Center"
                       Foreground="#DDFFFFFF"
                       FontSize="16" FontWeight="SemiBold"
                       IsHitTestVisible="False">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.8"/>
                </TextBlock.Effect>
            </TextBlock>
        </Canvas>
    </Grid>
</Window>