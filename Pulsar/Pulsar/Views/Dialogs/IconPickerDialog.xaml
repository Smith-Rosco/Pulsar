<ui:FluentWindow x:Class="Pulsar.Views.Dialogs.IconPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Pulsar.Views.Controls"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Select Icon" Height="550" Width="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">

    <Grid Margin="20,0,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Title="Select Icon" Grid.Row="0"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ui:TextBox x:Name="SearchBox" 
                        TextChanged="SearchBox_TextChanged"
                        PlaceholderText="Filter system icons..." 
                        Icon="{ui:SymbolIcon Search24}"/>

            <ui:Button Grid.Column="1" Click="BrowseFile_Click" Margin="10,0,0,0"
                       Icon="{ui:SymbolIcon FolderOpen24}"
                       Content="Browse .exe/.lnk" 
                       Appearance="Secondary"/>
        </Grid>

        <Border Grid.Row="2" Background="{DynamicResource ControlFillColorDefaultBrush}" 
                BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1"
                CornerRadius="4" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="IconList" ItemsSource="{Binding FilteredIcons}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="8"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:Button Click="Icon_Click" Tag="{Binding Code}"
                                       Appearance="Transparent" Padding="5" Margin="2"
                                       ToolTip="{Binding Name}"
                                       Height="48" Width="48">
                                <StackPanel>
                                    <TextBlock Text="{Binding Character}" 
                                               FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" 
                                               FontSize="24" HorizontalAlignment="Center"
                                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                </StackPanel>
                            </ui:Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Selected:" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,10,0"/>
                <controls:JellyOrb x:Name="PreviewOrb" Size="30" IsActive="False" Margin="0,0,10,0"/>
                <TextBlock x:Name="ResultText" Text="{Binding SelectedKey}" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Cancel" IsCancel="True" Click="Cancel_Click"
                           Width="80" Appearance="Secondary" Margin="0,0,10,0"/>
                <ui:Button Content="Select" IsDefault="True" Click="Select_Click"
                           Width="80" Appearance="Primary"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>