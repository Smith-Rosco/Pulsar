<Window x:Class="Pulsar.Views.Dialogs.ColorPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Pick a Color"
        Height="450" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary>
             <ResourceDictionary.MergedDictionaries>
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Preview & Hex -->
            <RowDefinition Height="Auto"/> <!-- Sliders -->
            <RowDefinition Height="*"/>    <!-- Presets -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>

        <TextBlock Text="Select Color" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <!-- Preview & Hex Input -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Preview Box -->
            <Border Width="60" Height="60" CornerRadius="8" 
                    BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1"
                    Margin="0,0,16,0">
                <Border.Background>
                    <SolidColorBrush Color="{Binding CurrentColor, RelativeSource={RelativeSource AncestorType=Window}}"/>
                </Border.Background>
            </Border>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="Hex Code" Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                <ui:TextBox x:Name="HexInput" 
                            PlaceholderText="#RRGGBB"
                            Icon="{ui:SymbolIcon Color24}"
                            TextChanged="HexInput_TextChanged"/>
                <TextBlock x:Name="ErrorText" Text="Invalid Hex Code" Foreground="{DynamicResource PaletteRedBrush}" FontSize="12" Visibility="Collapsed" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <!-- RGB Sliders -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="R" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#FF5555"/>
                <Slider Grid.Column="1" Minimum="0" Maximum="255" Value="{Binding R, RelativeSource={RelativeSource AncestorType=Window}}" Margin="8,0"/>
                <TextBlock Grid.Column="2" Text="{Binding R, RelativeSource={RelativeSource AncestorType=Window}}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="G" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#55FF55"/>
                <Slider Grid.Column="1" Minimum="0" Maximum="255" Value="{Binding G, RelativeSource={RelativeSource AncestorType=Window}}" Margin="8,0"/>
                <TextBlock Grid.Column="2" Text="{Binding G, RelativeSource={RelativeSource AncestorType=Window}}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="B" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#5555FF"/>
                <Slider Grid.Column="1" Minimum="0" Maximum="255" Value="{Binding B, RelativeSource={RelativeSource AncestorType=Window}}" Margin="8,0"/>
                <TextBlock Grid.Column="2" Text="{Binding B, RelativeSource={RelativeSource AncestorType=Window}}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>

        <!-- Presets -->
        <StackPanel Grid.Row="3">
            <TextBlock Text="Presets" Margin="0,0,0,8" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
            <ItemsControl x:Name="PresetList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Width="32" Height="32" Margin="0,0,8,8" 
                                Click="PresetButton_Click"
                                Cursor="Hand"
                                ToolTip="{Binding Name}">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border CornerRadius="16" Background="{Binding Brush}" BorderThickness="1" BorderBrush="#30000000">
                                        <!-- Hover effect can be added here -->
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Action Buttons -->
        <Grid Grid.Row="4" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="0" Content="Cancel" Appearance="Secondary" HorizontalAlignment="Stretch" Margin="0,0,8,0" Click="Cancel_Click"/>
            <ui:Button Grid.Column="1" Content="Select" Appearance="Primary" HorizontalAlignment="Stretch" Margin="8,0,0,0" Click="Select_Click"/>
        </Grid>
    </Grid>
</Window>
