<Window x:Class="Pulsar.Views.Dialogs.QuickSecretsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Secret" Height="420" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E" 
        Foreground="#DDFFFFFF">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="Label / Alias" FontWeight="Bold"/>
            <TextBox x:Name="TxtLabel" />

            <TextBlock Text="Target Context (Process Name)" FontWeight="Bold"/>
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtProcess" Grid.Column="0" Margin="0" VerticalContentAlignment="Center"/>
                <Button Content="Pick" Width="50" Margin="5,0,0,0" Grid.Column="1" Click="BtnPick_Click"/>
            </Grid>

            <TextBlock Text="Username / Account" FontWeight="Bold"/>
            <TextBox x:Name="TxtAccount" />

            <TextBlock Text="Password" FontWeight="Bold"/>
            <PasswordBox x:Name="TxtPassword" />

            <CheckBox x:Name="ChkAutoEnter" Content="Auto Press Enter after injection" 
                      Margin="0,5,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Cancel" Width="80" Height="30" Margin="0,0,10,0" IsCancel="True"/>
                <Button Content="Save Secret" Width="100" Height="30" Click="BtnSave_Click" IsDefault="True"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>