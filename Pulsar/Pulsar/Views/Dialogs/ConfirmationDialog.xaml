<ui:FluentWindow x:Class="Pulsar.Views.Dialogs.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Confirm" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Pulsar;component/Styles/ButtonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20,0,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="DialogTitleBar" Title="Confirm" Grid.Row="0"/>

        <TextBlock x:Name="MessageText" 
                   Grid.Row="1" 
                   Text="Are you sure?" 
                   TextWrapping="Wrap" 
                   VerticalAlignment="Center" 
                   Margin="0,10,0,20"
                   FontSize="14"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button x:Name="BtnNo" Content="No" Width="80" Margin="0,0,10,0" IsCancel="True" Click="BtnCancel_Click" Style="{StaticResource PulsarSecondaryButtonStyle}"/>
            <ui:Button x:Name="BtnYes" Content="Yes" Width="80" IsDefault="True" Click="BtnOk_Click" Style="{StaticResource PulsarPrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>