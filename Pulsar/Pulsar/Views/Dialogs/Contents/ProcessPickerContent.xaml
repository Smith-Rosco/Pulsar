<UserControl x:Class="Pulsar.Views.Dialogs.Contents.ProcessPickerContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:viewmodels="clr-namespace:Pulsar.ViewModels.Dialogs"
             xmlns:converters="clr-namespace:Pulsar.Core.Converters"
             xmlns:lex="clr-namespace:Pulsar.Core.Localization"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Self-merge ButtonStyles.xaml（同时带入 Tokens.xaml）：App.xaml 刻意
                     不加载 ButtonStyles.xaml，本控件又用到了 PulsarSecondaryButtonStyle，
                     不自带就是运行时资源查找失败。 -->
                <ResourceDictionary Source="pack://application:,,,/Pulsar;component/Styles/ButtonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="{StaticResource Pulsar.Gap.Bottom.LG}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ui:TextBox Grid.Column="0" x:Name="SearchBox" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                        PlaceholderText="{lex:Locale Dialog.ProcessPicker.SearchPlaceholder}" 
                        Icon="{ui:SymbolIcon Search24}"/>

            <!-- 刷新：进程可能在对话框打开期间启动/退出，首屏之后没有别的刷新入口。
                 刷新中换成 ProgressRing，兼作按钮的忙碌反馈。 -->
            <ui:Button Grid.Column="1"
                       x:Name="RefreshButton"
                       Margin="{StaticResource Pulsar.Gap.Left.SM}"
                       Padding="{StaticResource Pulsar.Pad.H10V8}"
                       Style="{StaticResource PulsarSecondaryButtonStyle}"
                       Command="{Binding RefreshCommand}"
                       IsEnabled="{Binding CanRefresh}"
                       ToolTip="{lex:Locale Dialog.ProcessPicker.Refresh}"
                       AutomationProperties.Name="{lex:Locale Dialog.ProcessPicker.Refresh}"
                       AutomationProperties.AutomationId="Pulsar.ProcessPicker.Refresh">
                <Grid>
                    <ui:SymbolIcon Symbol="ArrowClockwise24"
                                   Visibility="{Binding IsRefreshing, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                    <ui:ProgressRing IsIndeterminate="True"
                                     Width="16" Height="16"
                                     Visibility="{Binding IsRefreshing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </Grid>
            </ui:Button>
        </Grid>

        <ListBox Grid.Row="1" ItemsSource="{Binding FilteredProcesses}" 
                 SelectedItem="{Binding SelectedProcess}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter Property="Padding" Value="8"/>
                    <EventSetter Event="MouseDoubleClick" Handler="ListBoxItem_MouseDoubleClick"/>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid Width="32" Height="32" Margin="{StaticResource Pulsar.Gap.Right.MD}">
                            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="{StaticResource Pulsar.Radius.SM}">
                                <ui:SymbolIcon Symbol="AppGeneric24" FontSize="{StaticResource Pulsar.Type.Subtitle}" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            </Border>
                            <Image RenderOptions.BitmapScalingMode="HighQuality">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Setter Property="Source" Value="{Binding AppIcon}"/>
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AppIcon}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                        </Grid>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" 
                                       FontWeight="SemiBold" 
                                       TextTrimming="CharacterEllipsis"/>

                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding FormattedProcessName}" 
                                           FontSize="{StaticResource Pulsar.Type.Caption}" Opacity="0.7"/>

                                <TextBlock Text=" • " FontSize="{StaticResource Pulsar.Type.Caption}" Opacity="0.5"/>

                                <TextBlock Text="{Binding ExePath}" 
                                           FontSize="{StaticResource Pulsar.Type.Caption}" Opacity="0.5" 
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <Grid Grid.Row="1" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" 
              Background="{DynamicResource ApplicationBackgroundBrush}">
            <ui:ProgressRing IsIndeterminate="True" Width="40" Height="40"/>
        </Grid>
    </Grid>
</UserControl>
