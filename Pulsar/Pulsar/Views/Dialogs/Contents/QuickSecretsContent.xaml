<UserControl x:Class="Pulsar.Views.Dialogs.Contents.QuickSecretsContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">

    <UserControl.Resources>
        <ResourceDictionary>
             <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel Margin="0">
        <TextBlock Text="Label / Alias (Required)" FontWeight="Bold" Margin="0,0,0,5"/>
        <ui:TextBox Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="e.g. GitHub Token" Margin="0,0,0,15"/>

        <TextBlock Text="Username / Account (Optional)" FontWeight="Bold" Margin="0,0,0,5"/>
        <ui:TextBox Text="{Binding Account, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="user@example.com" Icon="{ui:SymbolIcon Person24}" Margin="0,0,0,15"/>

        <TextBlock Text="Password" FontWeight="Bold" Margin="0,0,0,5"/>
        <ui:PasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                        PlaceholderText="••••••••" Icon="{ui:SymbolIcon LockClosed24}"/>
        
        <TextBlock Text="Leave blank to keep current password" 
                   FontSize="11" Opacity="0.6" Margin="0,5,0,15"
                   Visibility="{Binding IsEditModeVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <CheckBox IsChecked="{Binding AutoEnter}" Content="Auto Press Enter after injection" Margin="0,15,0,20"/>
    </StackPanel>
</UserControl>
