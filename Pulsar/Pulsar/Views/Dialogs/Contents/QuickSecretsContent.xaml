<UserControl x:Class="Pulsar.Views.Dialogs.Contents.QuickSecretsContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:lex="clr-namespace:Pulsar.Core.Localization"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">

    <UserControl.Resources>
        <ResourceDictionary>
             <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel Margin="0">
        <TextBlock Text="{lex:Locale Dialog.QuickSecrets.LabelRequired}" FontWeight="Bold" Margin="{StaticResource Pulsar.Gap.Bottom.XS}"/>
        <ui:TextBox Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="{lex:Locale Dialog.QuickSecrets.LabelPlaceholder}" Margin="{StaticResource Pulsar.Gap.Bottom.LG}"/>

        <TextBlock Text="{lex:Locale Dialog.QuickSecrets.UsernameOptional}" FontWeight="Bold" Margin="{StaticResource Pulsar.Gap.Bottom.XS}"/>
        <ui:TextBox Text="{Binding Account, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="{lex:Locale Dialog.QuickSecrets.UsernamePlaceholder}" Icon="{ui:SymbolIcon Person24}" Margin="{StaticResource Pulsar.Gap.Bottom.LG}"/>

        <TextBlock Text="{lex:Locale Dialog.QuickSecrets.Password}" FontWeight="Bold" Margin="{StaticResource Pulsar.Gap.Bottom.XS}"/>
        <ui:PasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                        PlaceholderText="{lex:Locale Dialog.QuickSecrets.PasswordPlaceholder}" Icon="{ui:SymbolIcon LockClosed24}"/>
        
        <TextBlock Text="{lex:Locale Dialog.QuickSecrets.LeaveBlankHint}" 
                   FontSize="{StaticResource Pulsar.Type.Caption}" Opacity="0.6" Margin="{StaticResource Pulsar.Gap.Top.XS}"
                   Visibility="{Binding IsEditModeVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </StackPanel>
</UserControl>
