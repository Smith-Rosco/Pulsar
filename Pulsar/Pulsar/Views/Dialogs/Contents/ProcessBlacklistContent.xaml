<UserControl x:Class="Pulsar.Views.Dialogs.Contents.ProcessBlacklistContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:viewmodels="clr-namespace:Pulsar.ViewModels.Dialogs"
             xmlns:lex="clr-namespace:Pulsar.Core.Localization"
              d:DataContext="{d:DesignInstance Type=viewmodels:ProcessBlacklistViewModel}"
              mc:Ignorable="d" 
              d:DesignHeight="450" d:DesignWidth="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

         <StackPanel Grid.Row="0" Margin="{StaticResource Pulsar.Gap.Bottom.LG}">
             <TextBlock Text="{lex:Locale Dialog.ProcessBlacklist.SelectProcesses}"
                        TextWrapping="Wrap"
                        Opacity="0.8"/>
             <CheckBox IsChecked="{Binding EnableSwitchDiagnostics, Mode=TwoWay}"
                       Content="{lex:Locale Dialog.ProcessBlacklist.EnableSwitchDiagnostics}"
                       Margin="{StaticResource Pulsar.Gap.Top.MD}"/>
         </StackPanel>

        <ListBox Grid.Row="1" 
             ItemsSource="{Binding Processes}"
                 VirtualizingPanel.IsVirtualizing="False"
                 VirtualizingPanel.IsVirtualizingWhenGrouping="False"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter Property="Padding" Value="8"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Icon with running indicator -->
                        <Grid Grid.Column="0" Width="32" Height="32" Margin="{StaticResource Pulsar.Gap.Right.MD}">
                            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="{StaticResource Pulsar.Radius.SM}"/>
                            <Image Source="{Binding Icon}" RenderOptions.BitmapScalingMode="HighQuality"/>
                            <!-- Running indicator (green dot) -->
                            <!-- 光学对齐负值:状态点贴齐图标右下角,不进间距刻度 -->
                            <Ellipse Width="8" Height="8" 
                                     Fill="LimeGreen" 
                                     HorizontalAlignment="Right" 
                                     VerticalAlignment="Bottom"
                                     Margin="0,0,-2,-2">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </Grid>

                        <!-- Process name and display name -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding DisplayName}" 
                                       FontWeight="SemiBold" 
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding ProcessName}" 
                                       FontSize="{StaticResource Pulsar.Type.Caption}" 
                                       Opacity="0.7"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <!-- Status text -->
                        <TextBlock Grid.Column="2" 
                                   Text="{Binding StatusText}" 
                                   FontSize="{StaticResource Pulsar.Type.Caption}" 
                                   Opacity="0.7" 
                                   VerticalAlignment="Center"
                                   Margin="{StaticResource Pulsar.Gap.Right.MD}"/>

                        <!-- Blacklist checkbox -->
                        <CheckBox Grid.Column="3" 
                                  IsChecked="{Binding IsBlacklisted}"
                                  VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <Border Grid.Row="1"
                VerticalAlignment="Top"
                HorizontalAlignment="Right"
                Margin="{StaticResource Pulsar.Thk.T8R8}"
                Padding="{StaticResource Pulsar.Pad.H8V4}"
                CornerRadius="{StaticResource Pulsar.Radius.XL}"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <StackPanel Orientation="Horizontal">
                <ui:ProgressRing IsIndeterminate="True" Width="16" Height="16" Margin="{StaticResource Pulsar.Gap.Right.SM}"/>
                <TextBlock Text="{lex:Locale Dialog.ProcessBlacklist.Loading}" VerticalAlignment="Center" FontSize="{StaticResource Pulsar.Type.Caption}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
