<UserControl x:Class="Pulsar.Views.Dialogs.Contents.InputProfileContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:controls="clr-namespace:Pulsar.Views.Controls"
             xmlns:viewmodels="clr-namespace:Pulsar.ViewModels.Dialogs"
             xmlns:lex="clr-namespace:Pulsar.Core.Localization"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Pulsar;component/Styles/ButtonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <!-- Self-contained: dialog content is template-loaded and cannot rely on
                 per-window injected dictionaries at parse time (see WPF_RESOURCES_HYGIENE
                 and the Multi-Headed UI architecture). -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel Margin="0">
        <!-- Profile Name Input -->
        <TextBlock Text="{lex:Locale Dialog.InputProfile.AppProcess}" FontWeight="Medium" Margin="{StaticResource Pulsar.Gap.Bottom.SM}"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <ui:TextBox Text="{Binding ProcessName, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="{lex:Locale Dialog.InputProfile.ProcessPlaceholder}"
                        Icon="{ui:SymbolIcon Apps24}"/>
            
            <ui:Button Grid.Column="1" Margin="{StaticResource Pulsar.Gap.Left.SM}"
                       Icon="{ui:SymbolIcon TargetEdit20}"
                       Command="{Binding PickProcessCommand}"
                       ToolTip="{lex:Locale Dialog.InputProfile.PickProcess}"
                       AutomationProperties.Name="{lex:Locale Dialog.InputProfile.PickProcess}"
                       Style="{StaticResource PulsarSecondaryButtonStyle}"/>
        </Grid>
        
        <TextBlock Text="{Binding ErrorMessage}" 
                   Foreground="{DynamicResource PaletteRedBrush}" 
                   FontSize="{StaticResource Pulsar.Type.Body}" 
                   Margin="{StaticResource Pulsar.Gap.Top.SM}"
                   Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Alias Input -->
        <TextBlock Text="{lex:Locale Dialog.InputProfile.AliasOptional}" FontWeight="Medium" Margin="{StaticResource Pulsar.Thk.T16B8}"/>
        <ui:TextBox Text="{Binding Alias, UpdateSourceTrigger=PropertyChanged}"
                    PlaceholderText="{lex:Locale Dialog.InputProfile.AliasPlaceholder}"
                    Icon="{ui:SymbolIcon Rename24}"/>

        <!-- Icon Selection -->
        <TextBlock Text="{lex:Locale Dialog.InputProfile.ProfileIcon}" FontWeight="Medium" Margin="{StaticResource Pulsar.Thk.T20B8}"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Preview -->
            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" 
                    BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}" 
                    BorderThickness="1" CornerRadius="{StaticResource Pulsar.Radius.LG}"
                    Width="64" Height="64" Margin="{StaticResource Pulsar.Gap.Right.LG}">
                <controls:SlotOrb Size="48" IsActive="False" IsHitTestVisible="False" IconKey="{Binding IconKey}"/>
            </Border>

            <!-- Info -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{lex:Locale Dialog.InputProfile.SelectedIcon}" FontSize="{StaticResource Pulsar.Type.Body}" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="{Binding IconDisplayText}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <!-- Change Button -->
            <ui:Button Grid.Column="2" Content="{lex:Locale Dialog.InputProfile.Change}" 
                       Style="{StaticResource PulsarSecondaryButtonStyle}" 
                       Command="{Binding PickIconCommand}"
                       VerticalAlignment="Center"/>
        </Grid>
    </StackPanel>
</UserControl>
