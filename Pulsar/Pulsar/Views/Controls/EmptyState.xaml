<UserControl x:Class="Pulsar.Views.Controls.EmptyState"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:Pulsar.Core.Converters"
             mc:Ignorable="d"
             x:Name="Root">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- 自合并令牌与按钮样式，保证脱离 Application 加载（单元测试）时不抛 XamlParseException -->
                <ResourceDictionary Source="pack://application:,,,/Pulsar;component/Styles/Tokens.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Pulsar;component/Styles/ButtonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:StringEmptyToVisibilityConverter x:Key="StringEmptyToVisibilityConverter"/>
            <converters:ActionVisibilityMultiConverter x:Key="ActionVisibilityMultiConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="{DynamicResource ControlFillColorDefaultBrush}"
            BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}"
            CornerRadius="{StaticResource Pulsar.Radius.LG}"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="{x:Null}"/>
                <Setter Property="BorderBrush" Value="{x:Null}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="CornerRadius" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding HasBorder, ElementName=Root}" Value="True">
                        <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource SurfaceStrokeColorDefaultBrush}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="CornerRadius" Value="{StaticResource Pulsar.Radius.LG}"/>
                        <Setter Property="Padding" Value="{StaticResource Pulsar.Pad.Hero}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="400">
            <ui:SymbolIcon Symbol="{Binding Icon, ElementName=Root}"
                           FontSize="48"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="{StaticResource Pulsar.Gap.Bottom.LG}">
                <ui:SymbolIcon.Style>
                    <Style TargetType="ui:SymbolIcon">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Icon, ElementName=Root}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:SymbolIcon.Style>
            </ui:SymbolIcon>

            <TextBlock Text="{Binding Title, ElementName=Root}"
                       FontSize="{StaticResource Pulsar.Type.Title}"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Visibility="{Binding Title, ElementName=Root, Converter={StaticResource StringEmptyToVisibilityConverter}}"
                       Margin="{StaticResource Pulsar.Gap.Bottom.SM}"/>

            <TextBlock Text="{Binding Hint, ElementName=Root}"
                       FontSize="{StaticResource Pulsar.Type.Body}"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Visibility="{Binding Hint, ElementName=Root, Converter={StaticResource StringEmptyToVisibilityConverter}}"
                       Margin="{StaticResource Pulsar.Gap.Bottom.MD}"/>

            <ui:Button Content="{Binding ActionText, ElementName=Root}"
                       Command="{Binding ActionCommand, ElementName=Root}"
                       Style="{Binding ActionButtonStyle, ElementName=Root, TargetNullValue={StaticResource PulsarSecondaryButtonStyle}}"
                       HorizontalAlignment="Center">
                <ui:Button.Visibility>
                    <MultiBinding Converter="{StaticResource ActionVisibilityMultiConverter}">
                        <Binding Path="ActionText" ElementName="Root"/>
                        <Binding Path="ActionVisibility" ElementName="Root"/>
                    </MultiBinding>
                </ui:Button.Visibility>
                <ui:Button.Icon>
                    <ui:SymbolIcon Symbol="{Binding ActionIcon, ElementName=Root}"/>
                </ui:Button.Icon>
            </ui:Button>
        </StackPanel>
    </Border>
</UserControl>
