# Pulsar Samples

外置插件样例。`NeonRendererPlugin/` 是一个完整的外置渲染器插件示例（独立 csproj，含 `manifest.json`），用于演示外置插件目录结构与加载方式——**不属于主解决方案构建**。

- 插件开发入门：`PLUGIN_DEVELOPMENT.md`（仓库根）
- 外置插件权限模型：`AGENTS.md` §2 / ADR-007
